# 57周目 報告: **人が読む要約は区別していなかった。1 行足した**

- **基準**: `docs/d_summary_distinguishes_criteria.md`(測る前にコミット `c5c7f69`)
- **ベンチマーク**: `benchmarks/measure_summary_distinguishes.py`
- **合成図面で線を引いた**(取り決め④)。実図面は記録にしか使っていない。

## 結論

**人が読む要約(`IntakeResult.summary()`)は、
「探したが 0 件」と「そもそも探せなかった」を区別していなかった。**

**縮尺の印字があるかないかだけが違う 2 枚を通したとき、
要約は 1 行も違わなかった**(図面の指紋の行を除く)。
**ページごとの記録には理由が残っているのに、人に渡す形で消えていた。**

| | S1 探したが 0 件 | S2 探せなかった |
|---|---|---|
| 数量 | 0 件 | 0 件 |
| ページごとの記録 | **違う**(3 種ずつ、中身が違う) | ― |
| **要約の違う行(C1)** | **0** | ― |

## **測り方の穴を 1 つ踏んだ(先に書く)**

**最初の実装は、S1 と S2 に違う案件名を渡していた。**
案件名は要約の 1 行目に出るので、**理由とは無関係に「違う行」が 1 本立っていた**
(C1 = 1・C2 = 偽)。**これを「違いがある」と読んでいたら、逆の結論になっていた。**

**3 つとも同じ案件名で回すよう直した。直したあとの素の測定が C1 = 0 である。**

**これで測り方の設計ミスは 5 回目**(28・32・36・51・57 周目)。
**毎回、こちらが出す前に気づいて直している**が、**出やすい形だという事実は変わらない。**
今回の形は「**比べる 2 つに、比べたいもの以外の違いを混ぜてしまう**」である。

## 直したもの(**取り決め②のとおり、失敗するテストを先に書いた**)

1. **先に書いたテスト**
   `tests/test_drawing_intake.py::test_the_summary_says_when_a_method_could_not_be_attempted`
   — 書いた時点で `AttributeError` で落ちることを確認してから実装した。
2. **実装(3 か所。どれも足すだけ)**
   - `PageOutcome.unattempted_methods`(**既定は空**なので今までの振る舞いは変わらない)
   - `IntakeResult.pages_with_unattempted_methods`
   - `summary()` に **1 行**:「試せなかった手法があるページ: N 件」

**人が「建具表のページだ」と宣言したので探さなかった場合は入れていない。**
**それは人が自分で決めたことなので、知らないうちに抜けたのとは違う。**

## **判定のしかたは 1 ミリも動かしていない**

- 確定するかどうかの条件・基準値・階層の条件に触っていない
- **全件テスト 979 passed, 1 skipped**(足した 1 件ぶん増えただけ。**落ちた既存テストは 0**)
- 実図面 v2 を通した結果の数量は **7 件のまま**(56 周目と同じ)

**README.md の「判定のしかたを変えない機能追加」に当たるので、
全件テストと CI が通ればマージしてよい範囲。**

## 直したあとの確認(**判定には使わない**)

同じベンチマークをもう一度流すと **C1 = 1** になり、違った行はこれだった。

```
試せなかった手法があるページ: 0 件
試せなかった手法があるページ: 1 件
```

**基準に書いた C2 は「未対応」か「縮尺」の語を探す作りだったので、いまも偽のままである。**
**足した行はどちらの語も使っていない。基準は測ったあとに変えないので、そのままにする。**

## **実図面で何が変わるか**

v2 の要約は、これまで「**抽出に回した 34 / 未対応 0**」とだけ出していた。
**人は「34 ページ全部を見た」と読む。**
だが実際には、**14 ページで開き戸を一度も探していない**(縮尺が読めないため)。

**これからは「試せなかった手法があるページ: 14 件」が要約に出る。**

## この周で変えなかったもの

- 判定のしかた・基準値・階層の条件
- **室の名前も図面の文字も座標も、報告書にも記憶にも書いていない**
