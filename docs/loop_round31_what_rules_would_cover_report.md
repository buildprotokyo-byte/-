# 周31 の報告 — **2,887 件は 6 種類しかない。だが線3 の立て方をまた誤った**

**基準**: `docs/loop_round31_what_rules_would_cover_criteria.md`(測る前にコミット、`c945b90`)
**道具**: `benchmarks/measure_rule_coverage.py`
**当てた紙**: P011 匿名化 **v2**(34 ページ)。パスは設定で渡している。

---

## **この周でいちばん重い発見は、測る前のコード読みで出た**

`app.py` の 547 行目はこうなっている。

> **規則ファイルを渡さないと、入口の数量は 1 件も見積の行に当てはめられず、
> 丸ごと落ちる。**落ちたことは `gaps` に 1 行書かれるだけである。

**つまり周27 の「一本通すと 67 行」は、規則ファイル**無し**で出た行だった。**
**周27〜30 が追っていた「面積待ちの 62 行」と、この 2,887 件は別の話である。**

**「#139 は入口まで届くのに行は 1 つも変わらない」の理由も、ここで変わる。**
**繋がっていないのではない。規則が無いので、当てはめの段が丸ごと飛んでいる。**

---

## 線1 — 2,887 件は何種類の対象に集まるか

**予想は「50 以下」、合格は「100 以下」。実際は 6 種類。**

| 順位 | 件数 |
|---:|---:|
| 1 | 2,488 |
| 2 | 233 |
| 3 | 159 |
| 4 | 5 |
| 5 | 1 |
| 6 | 1 |

**上位 3 種類で 2,880 件(99.8%)。**

**予想の向きは当たったが、桁を 1 つ読み違えていた。**

**ここで止める。**「だから規則は 6 本でよい」とは**書かない**。
基準にそう書いてある——**種類の数は、書かねばならない規則の本数の
「下限」**である。`estimating/rules.py` の `MappingRule` は種類のほかに
**単位の次元・属性・現況か計画か**も条件にできる。

---

## 線3 — **不通過。線の立て方がこちらの誤り**

上位 10 種類の被覆は 1.00、**囮(無作為に 10 種類)も 1.00、差は 0.00。**

**種類が 6 つしかないのに「上位 10 を選ぶ」と書いたので、
本物も囮も全部を選ぶことになり、差が出ようがなかった。**

**基準を読んだときには気づけた誤りである。**「上位 10」と書いた時点で、
**種類が 10 以下なら囮と一致する**ことは決まっていた。
実際、道具の試験にその場合を固定した 1 件
(`test_種類が上位の数以下なら囮は本物と同じになる`)が**既に入っていた。**
**試験は正しく書いておきながら、基準の側でその場合を考えていなかった。**

**線は下げない。線3 は不通過のままにする。**
**「上位 3 種類で 99.8%」は線3 の判定には使わない**(結果を見てから
選び直した数だから)。**表の数字として残すだけである。**

### **これで線の立て方の誤りは 4 種類目**

| 周 | 誤りの種類 |
|---|---|
| 周13・周14 | **落ちようが無い線**(どう転んでも通る) |
| 周25 | **実装の作りに対して厳しすぎる線** |
| 周22 | **効きようが無い囮**(測る前に気づいて差し替えた) |
| **周31** | **母数より大きい選び方**(本物と囮が必ず一致する) |

---

## 線2(対照)— いま在る規則は 2,887 件のうち 5 件にしか当たらない

**規則 4 本、当たり 5 件(0.17%)。予想どおりほぼ 0。**
**合成の見本は、この案件の対象とほとんど噛み合っていない。**
`app.py` 自身が「合成の見本は実案件に使わない」と書いているとおりである。

---

## 線4(安全確認)— **自動確定は 0 件のまま**

架空の規則を渡しても `当てはめで確定した行` は **0**。
**裏返しの危険は起きなかった。**K-29 のとおり、自動確定は 0 件のままである。

---

## 書かないこと(基準に先に書いてある)

1. **「実案件で何行出るか」は測っていない。**渡したのは**架空の見本**である。
2. **金額も数量の値も出していない。**数えたのは件数と種類数だけ。
3. **対象名そのものは書かない。**実案件の図面の中身だから、
   リポジトリには件数と種類数しか置かない(表は共有フォルダ)。
4. **コードを 1 行も直していない。**`app.py`・`intake/`・判定に触っていない。

## 次の周に渡すもの

**種類が 6 つだと分かった。だから次に要るのは「その 6 種類に規則を書くには
ほかに何が要るか」**である。**この周では「参考」の欄で 1 つだけ数えた**
(下の表)。**判定には使っていない。**
