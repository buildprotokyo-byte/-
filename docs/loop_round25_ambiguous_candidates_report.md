# 周25 の報告 — **対照が落ちた。だから本題の数字は読まない**

**基準**: `docs/loop_round25_ambiguous_candidates_criteria.md`(測る前にコミット、`88d0c00`)
**道具**: `benchmarks/measure_ambiguous_candidates.py`
**紙**: P011 匿名化 v2 / **種**: 20260925 / **平面図とみなしたページ**: 5 ページ(#139 を含む枝)

---

## 結論を先に — **基準に書いた止め方に従って止めた**

基準にこう書いてある。

> **線2 が 9 割に届かなければ、線1 は読まない。**
> **道具が働いていないのに中身を語らない。**

**線2(対照)は不通過だった。だから線1 の数字は結論にしない。**

**線1 の数字は報告には載せる**(隠さない)が、
**「縮尺を手がかりにすれば候補を絞れる」とは書かない。**

---

## 線2(対照)— 読めた寸法は、そのページの縮尺と合うか

| | 読めた寸法 | そのページの縮尺と ±1% で合う |
|---|---:|---:|
| **合計** | **79 件** | **51 件(64.6%)** |
| 横 | 29 件 | 20 件 |
| 縦 | 50 件 | 31 件 |

**合格は 9 割以上。不通過。**

**これ自体が、この周でいちばん重い数字である。**

**#139 は「記入寸法から出した縮尺が、表題欄の印字と 15 ページ中 14 ページで
±0.1% で一致した」と報告している。それは正しい。**
**ただしその一致は、互いに一致した読みの組の中での話である。**
`page_scale_from_dimensions()` は**一致しない読みを `outlier_values_mm` に
出して残す作り**になっている。

**5 ページの読み 79 件を、そのページの縮尺に当てると、28 件が 1% 超で食い違う。**

**「79 件は読めたであって正しいではない」という #139 の但し書きに、
具体的な数が付いた。**

### **対照の線の立て方が、実装の作りと食い違っていた(こちらの落ち度)**

**9 割という線は、「縮尺が出たなら読みのほとんどが縮尺と合うはずだ」
という思い込みで置いた。**
**実装は、はじめから外れが出ることを前提にしている。**
**だから 9 割という線は、この道具の作りに対しては高すぎる。**

**それでも線は動かさない。**結果を見てから線を下げれば、
**どんな結果でも通せることになる。**
**周13・周14 と同じ「線の立て方の失敗」を、3 度目として記録する。**

**ただし前の 2 回とは違いがある。**周13・周14 は
**「どんな図面でも成り立つ線」を置いた**(不通過になりようがなかった)。
**今回は不通過になった。線が厳しすぎたほうの失敗である。**

---

## 線1 — **読まないが、隠さずに載せる**

| | 件数 |
|---|---:|
| 「候補が複数」で落ちた横の数字 | 33 |
| ちょうど 1 つの候補が合う | 25 |
| 囮(値だけ入れ替え) | 10 |
| 1 つも合わない | 3 |
| 2 つ以上合う | 5 |

**この数字を根拠にしない。**対照が落ちているので、
**「合う」という物差し自体が 79 件中 28 件で当てにならない。**

### 周24 の 25 件と、ここの 33 件は別の数え方である

- **周24 の 25 件**: **空きの中に入っていた**落とし物のうち、
  理由が「候補が複数」だったもの(重複を除く)
- **周25 の 33 件**: そのページの**横向きの数字すべて**のうち、
  「候補が複数」で落ちたもの(空きの中かどうかを見ていない)

**数え方が違うので、25 と 33 は食い違いではない。**

## 線3 — 候補はいくつ残っているのか(**判定は置いていない**)

| 候補の数 | 件数 |
|---|---:|
| 2 個 | 27 |
| 3 個 | 0 |
| 4 個以上 | 6 |

**3 個はちょうど 0 件である。**

## 書かないこと

1. **「縮尺を手がかりにすれば候補を絞れる」とは書かない。**対照が落ちている。
2. **`_pick_span` を直す根拠にしない。**
3. **「#139 の 79 件は間違っている」とは書かない。**
   **28 件が「そのページの縮尺と 1% 超で食い違う」だけである。**
   **どちらが正しいか(読みが外れているのか、縮尺が外れているのか)は
   測っていない。**正解が無いので、この紙では測れない。

## 次の周に渡すもの(**分からなかったものだけ**)

**食い違う 28 件は、読みが外れているのか、縮尺のほうが外れているのか。**
**この紙では正解が無いので決められない。**
**決めるには、同じページに人が入れた基準の長さ(スタートキット)が要る。**

**これは次の周の案であって、測っていない。根拠にはしない。**

## 何に触っていないか

`pdf_dimensions` の定数は **1 つも動かしていない。**`_pick_span` にも触っていない。
`find_room_outlines`・`intake/`・`app.py` にも触っていない(K-29)。
**自動確定は 0 件のまま。**`human_reference_point` は `calibrated=False` のまま。

## 全件テスト

**1,919 件パス(305.46 秒)。** 周25 で足したのは
`tests/test_measure_ambiguous_candidates.py` の 11 件。
