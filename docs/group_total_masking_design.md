# 群合計制約の矛盾検出が、停止した要素のせいで効かなくなる問題 — 現象の確認と設計案

作成: 2026-09-22 / 現象の確認に使ったコミット: `816e95b`
**実装済み（2026-09-22、おーちゃんの判断を受けて）。** 実装した内容は7節。
再現テスト: `tests/test_group_total_masking.py`
実装: `arbitration/group_total.py`、`arbitration/consistency_solver.py`（`Variable.detection_range`）、
`killer_question/firewall_bridge.py`

---

## 1. 何を確かめたか

Codex がトライアル16の保存データを commit `c0c9d13` の本番ファイアウォールで
再判定して、次の現象を報告した。

- 本番ファイアウォールは階層2に「強い軸1つ + 異なる弱い軸2つ以上」を要求するため、
  旧判定より多くの要素を停止する
- 停止した要素は範囲が広がるため、群合計制約が UNSAT から SAT に変わる
- その結果、旧処理では群ごと停止していた「強い2軸が同じ誤値で一致」型の誤りが
  自動確定されるようになった
- 誤自動確定の合計は 456件から541件に増加（中心値検査は一度も発火しておらず、無関係）

このリポジトリの本番経路（`AxisQualityFirewall.assess()` →
`killer_question/firewall_bridge.py` → `ConsistencySolver`）で同じ形を組み直し、
**現象が再現することを確認した。** トライアル16の保存データは Codex 側にあり
本リポジトリには無いので、同じ性質の群（同一単位の10要素、群合計30）を新規に構成した。

---

## 2. 現象は本番経路でも起きる（実測）

同一単位の10要素からなる群（建具表に「合計30本」と書かれている想定）。
`door_1` は独立した強い軸2つが**同じ誤った値**（1、正解3）で一致している。
この形は

- 積集合が空にならないので、軸間矛盾としては検出できない
- 中心値も完全に一致するので、`CENTER_TOLERANCES` の中心値検査も発火しない

したがって**群合計制約だけが唯一の検出経路**である。

| 条件 | `door_0` の状態 | solver 上の範囲 | 群合計の判定 |
|---|---|---|---|
| 停止なし | 階層1 | (3, 3) | **unsat（検出できる）** |
| 停止・範囲そのまま | 階層3 | (2, 4) | **unsat（検出できる）** |
| 停止・精密モードで開き直し | 階層2 | (0, 6) | **sat（検出が消える）** |
| 停止・使える強い軸が0（既定） | 階層3 | **未登録** | **制約を書けない** |

最後の行が既定の挙動である。`firewall_bridge` はバグ①の修正として、使える強い軸が
1つも無い階層3の要素を `registered=False` で返し、solver に変数を作らない。
すると**その要素を参照する群合計制約は `solve()` の中で `KeyError` になる**ので、
呼び出し側は制約ごと落とすしかない。停止した1要素を守るために、**同じ群の他の9要素の
検査がまとめて消える。**

3行目（精密モード）が、Codex の言う「範囲が広がるため UNSAT が SAT に変わる」に
そのまま対応する。標準モードでは範囲が `(2, 4)` のままなので同じ群が unsat のままで、
**同じデータに対して、精密モードのほうが誤りを見逃す。**

### 2-1. 検出力は連続的に落ちる

停止した要素の幅が十分に大きければ、どんな誤りも吸収できてしまう。これは実装の
不具合ではなく論理の帰結で、群合計制約で否定できるのは

    群の誤差の合計 > 停止した要素の幅の合計

の場合だけである。したがって「停止した要素があっても群合計で必ず検出できること」を
要件にはできない。要件にできるのは次の2つ。

1. **群合計制約を立てられること。** 停止した要素が変数として登録されず、制約を
   書くことすらできない、という状態にはしない。
2. **「停止した要素が吸収したから sat になった」を sat と区別すること。**
   吸収が起きた群では、群の中の階層1の要素を自動確定のままにしない。

### 2-2. 併せて見つかったこと（この問題とは別件）

群合計が unsat になったとき、`unsat_core` は群の**全要素**の `range::` 制約と
`group_total` を並べて返す（実測）。矛盾の核が絞り込まれないので、
「どの要素を人に確認すればよいか」はこの核からは決まらない。
`benchmarks/run_trial789_reproduction.py` の `_variables_in_conflict()` は核に
出た変数を全部人に確認する作りなので、群合計を使うと**群の全要素を人に投げる**
ことになる。等式クラスタ（2変数）では問題にならなかったが、群合計では効いてくる。

---

## 3. 原因の切り分け

3つの別々の原因が同じ症状を出している。直す場所も別なので分けて書く。

**原因A: 停止した要素が変数として登録されない（既定の挙動）。**
`firewall_bridge.add_target_to_joint_solver()` は、使える強い軸が0の階層3要素を
登録しない。安全側の判断として正しい（棄権した証拠の番兵値 `(0, 0)` がハード制約の
範囲を書き換えるのを防ぐ、バグ①の修正）。しかし**「変数が無い」は「変数が自由」より
悪い。** 自由変数なら制約は書けて、誤差が幅を超えれば検出できる。変数が無いと
制約自体が書けない。

**原因B: 「人に質問できるようにする」と「共有solver の定義域」が同じ1つの範囲に
同居している。** 精密モードの開き直し（`mode is PrecisionMode.PRECISE` の分岐）は、
階層2の要素を直接確認の対象にするために読みの和集合まで範囲を戻す。目的は
**質問の候補を広げること**であって、群全体の検査を緩めることではない。ところが
同じ範囲が群合計制約の定義域になるので、副作用として検査が緩む。

**原因C: 群合計制約が「検査」として扱われていない。** いまの経路では、群合計は
solver に入る制約の1つでしかなく、sat だったときに「この検査は何かを否定できたのか」
を誰も問わない。停止した要素の幅で吸収されただけでも、sat は sat として通る。

---

## 4. 設計案

4つを層にして提案する。A案とB案が原因A・原因Bへの直接の手当て、C案が原因Cへの
手当て、D案が運用への織り込み。**C案がこの問題の中心**で、A案はその前提になる。

### 4-1. C案（中心）: 群合計の残差中心検査

群合計が sat になったとき、**停止した要素に押し付けられた値（残差）が、その要素
自身の読みの中心からどれだけ離れているか**を見る。離れていれば、その sat は
「誤りが無い」ではなく「停止した要素が吸収した」である。

    停止していない要素の合計の区間     [Σlow_fixed, Σhigh_fixed]
    残差（停止した要素が担うべき値）   R = [T − Σhigh_fixed, T − Σlow_fixed]
    停止した要素自身の中心の合計       C = Σ center(停止した要素)
    許容幅                             W = 単位ごとの許容差 × 停止要素数

    R と [C − W, C + W] が交わらなければ「吸収された」と判定する

`center()` は、その要素に使える強い軸があればその積集合の中心、無ければ棄権して
いない読みの和集合の中心を採る。単位ごとの許容差は**既にある
`CENTER_TOLERANCES` をそのまま使う**（個数 ±1、連続量 ±5%）。中心値検査と同じ
道具を、要素単体ではなく群の残差に当てるだけなので、許容差の議論
（v8 10章12項）を二重に持たずに済む。

**試作して確かめた**（`/tmp` の試作。本番コードには入れていない）。上の10要素の群で:

| 条件 | solve | 残差 | 停止要素の中心 | 許容 | 吸収判定 |
|---|---|---|---|---|---|
| 誤りなし・停止あり（精密） | sat | 3 | 3 | ±1 | **なし**（誤報なし） |
| `door_1` が −2 誤り（精密） | sat | 5 | 3 | ±1 | **あり（検出）** |
| `door_1`/`door_2` が −2 誤り（精密） | unsat | 7 | 3 | ±1 | あり |
| 誤りなし・強い軸0を和集合で登録 | sat | 3 | 3 | ±1 | **なし**（誤報なし） |
| `door_1` が −2 誤り・強い軸0を和集合で登録 | sat | 5 | 3 | ±1 | **あり（検出）** |
| `door_1` が +1 だけずれ（精密） | sat | 2 | 3 | ±1 | なし（**死角**、下記 5節） |

いまは自動確定で素通りしている2行目・5行目が、この検査で止まる。誤りが無い条件
（1行目・4行目）では発火しないので、自動化率をむやみに下げない。

**吸収が判定されたときの扱い（要判断）。** 群合計は「どの要素が誤っているか」までは
言わない。言えるのは「この群は、停止した要素を自分の読みの近くに留めると合計が
合わない」ことだけである。候補は3つ。

- (a) 群の中の階層1の要素を階層2（仮採用＋抜き取り監査）へ落とす
- (b) 群全体を階層3（人が確認）へ落とす ← 旧処理の挙動に近い。安全だが人手が増える
- (c) 群の中でキラークエスチョンを1問立てる（合計の残差を最も動かす要素を選ぶ）

**(c) を推す。** 群合計は「1問で群全体が決まる」形の制約なので、キラークエスチョン
エンジンが最も効く場所である。ただし (c) は実装が最も重い。**(a) を先に入れて
(c) を後から足す**のが段取りとしては安全だと考える。ここはおーちゃんの判断が要る。

### 4-2. A案（前提）: 停止した要素も必ず変数として登録する

`firewall_bridge` の `registered=False` をやめ、階層3の要素も必ず変数にする。

- 定義域は、**棄権していない証拠のうち使える強い軸の積集合**。それが無ければ
  棄権していない読みの和集合。それも無ければ `arbitration/units.py` が持つ
  その単位の許容域（`max_value`）
- `requires_confirmation=True` は従来どおり立てる（確定済みとして扱われない）
- **番兵値の混入はしない。** バグ①の実害は「棄権した証拠の `(0, 0)` が下限を
  0 まで引き下げた」ことで、棄権を除外すれば起きない（既に bridge は除外している）
- 新しいフラグ（仮に `domain_is_unvalidated`）を変数に持たせ、「この定義域は
  校正を通った読みに裏づけられていない」ことを下流が読めるようにする

これで群合計制約が常に書けるようになり、C案の残差検査も常に動く。
**A案だけでは足りない**（幅が広がれば吸収されるので、C案とセットで意味がある）。

### 4-3. B案: 質問の候補を広げることと、共有solver の定義域を分ける

精密モードの開き直し（`firewall_bridge` の `PrecisionMode.PRECISE` の分岐）は、
共有solver の定義域を広げるのをやめ、**狭い `confirmed_range` のまま
`requires_confirmation=True` を立てる**。読みの和集合は、キラークエスチョン
エンジンが自分の仮説空間として持つ（`engine.py` の候補列挙側）。

これで「精密モードのほうが誤りを見逃す」という逆転が消える。
`allow_provisional_domain=True` も同じ扱いにできるが、こちらは
`docs/killer_question_report.md` 3節の絞り込みの挙動に関わるので、
既定が False のまま（v8 10章14項で「意図的に着手しない」と決めた項目）なら
当面そのままでよい。

### 4-4. D案: 群合計制約の検出力を数字で出す

群合計を検査として使うとき、その群の**吸収余地**

    S = Σ (停止した要素の幅)

を計算して報告に載せる。S が、その群で捕まえたい最小の誤差より大きければ、
**その群合計は何も否定できない**ので「検査あり」と数えない。
`arbitration/provisional_audit.py` が `hit_rate=None` で「照合できない対象」を
区別しているのと同じ考え方で、「検出力の無い検査」を「通った検査」と混ぜない。

---

## 5. この設計案の死角（正直に書く）

1. **許容差の中に隠れる誤りは捕まえられない。** 上の表の最終行がそれで、
   `door_1` が +1 だけずれた場合、残差は 2 で中心 3 から ±1 の中に入るので
   発火しない。要素単体の中心値検査と同じ死角であり、同じ原因（許容差を
   置く以上、その幅の中は見えない）である。
2. **停止した要素が多い群では効かなくなる。** 許容幅 W は停止要素数に比例して
   広がるので、10要素中8要素が停止した群では W = ±8 になり、ほとんど何も
   否定できない。D案の S を見て「検出力なし」と判定するしかない。
3. **停止した要素の中心そのものが誤っている場合、誤報になる。** 停止した要素の
   読みが大きく外れていると、残差が中心から離れて「吸収された」と判定される。
   このとき止まるのは正しい（その要素は確かに怪しい）が、**止まる理由が
   「他の要素が誤っている」ではない**ので、(c) のキラークエスチョンで
   どの要素かを絞る価値がここにもある。
4. **群合計そのものの読み取りが誤っている場合は扱えない。** 建具表の「合計30本」
   を読み違えていれば、この検査は群全体を疑い続ける。群合計の値自体を
   何本の軸で読んだかは、いまどこにも記録していない。

---

## 6. 残っている確認事項

- **トライアル16の保存データでの再現。** 本文書の再現は同じ性質の群を新規に
  構成したもので、Codex の「456件 → 541件」という数値そのものは再現していない。
  Codex が用意している評価データ（同じ単位の10要素の群）が届いたら、C案の
  試作をそれに当てて効果を測る
- **4-1節の (a)/(b)/(c) の選択**（おーちゃんの判断）
- **群合計制約の API。** いまリポジトリには群合計の専用の入口が無く、
  `add_relation` にラムダを渡して書くことになっている。C案を入れるなら
  「この制約は群合計である」と宣言できる入口が要る（残差検査は、どの変数が
  1つの群を成すかを知らないと書けないため）

---

## 7. 実装したもの（2026-09-22）

おーちゃんの4条件（1 検出用と確定用の範囲を分ける / 2 sat を自動確定の根拠に
数えない / 3 UNSAT の安全装置を弱めない / 4 テスト）に沿って実装した。
4-1節の扱いは **(a) を採用**した（吸収が起きた群の階層1の要素を階層2へ落とす）。
(c) のキラークエスチョンは入れていない。

### 7-1. 検出用の範囲と確定用の範囲を分けた（条件1・原因A・原因B）

`ConsistencySolver.Variable` に `detection_lower` / `detection_upper` を足し、
`solve(use_detection_ranges=True)` でそちらを定義域に使えるようにした。
確定用の範囲より広い検出用の範囲は登録時に弾く（広い側で吸収が起きるため）。

`firewall_bridge` の変更は3点。

- **停止した要素も必ず変数として登録する。** 変数を作らないのは「意味のある範囲が
  存在しない」2つの場合だけにした。単位・粒度が混在している場合（違う物差しの
  数字は足せない。バグ①の上限 246 はこれだった）と、棄権していない証拠が
  1つも無い場合。それ以外は `_reading_based_range()`（棄権を除いた読みの積集合。
  空なら和集合）で登録する
- **精密モードで開き直すのは確定用の範囲だけ**にし、`detection_range` は
  `confirmed_range` のまま据え置いた。これで「精密モードのほうが見逃す」逆転が消える
- `allow_provisional_domain=True` も確定用の範囲にしか効かない

### 7-2. 群合計を「検査」として扱う入口を作った（条件2・条件3・原因C）

新規 `arbitration/group_total.py`。

- `GroupTotalConstraint` … 群を宣言する入口（6節の残件だったもの）。
  要素が1つでも未登録なら `apply()` がその場で失敗する。**黙って読み飛ばすと
  「制約があるのに何も検査していない」状態になる**ため
- `check_group_total()` … 群合計が実際に何を確かめたかを返す。判定はすべて
  `use_detection_ranges=True` で行う
  - **unsat なら群を止める**（条件3。`targets_requiring_audit` に群の全要素）
  - sat なら要素ごとに、**群合計ありで値が1つに決まり、群合計なしでは
    決まらない**要素だけを `verified_targets` に入れる。自分の読みだけで
    既に幅0だった要素は `unverified_targets`（＝**その要素について群合計は
    何も確かめていない**。条件2のおーちゃんの判定基準そのもの）
  - 停止した要素が自分の読みの中心から `CENTER_TOLERANCES` を超えて押し出されて
    いれば `absorbing_targets`。このとき群の階層1の要素を
    `targets_requiring_audit` に挙げる（4-1節の (a)）
- `group_total_absorption_slack()` / `group_total_has_detection_power()` …
  停止した要素が吸収できる幅の合計を出し、捕まえたい最小の誤差と比べる（4-4節 D案）。
  **単位ごとの妥当な最小誤差は決まっていない**（許容誤差の本決め、v8 10章12項）ので、
  既定値は置かず引数を必須にした

**4-1節で書いた残差の区間演算は使わなかった。** 同じ判定を solver の
`use_detection_ranges=True` の解そのものから読めるので、中心と許容差の
計算を2箇所に持たずに済む。判定の結果は試作と一致する。

### 7-3. 確かめたこと

- 全 584 件パス（修正前の 575 件 + 新規 9 件）。`rm -rf __pycache__` と
  `PYTHONDONTWRITEBYTECODE=1` を付けて実行
- 先に書いた失敗テスト3件は、`xfail(strict=True)` の印を外して通常のテストとして通る
- **実装を5通りに壊して、それぞれテストが落ちることを確認した。**
  ①`detection_range` を無視する ②階層3（強い軸なし）を登録しない
  ③精密モードで `detection_range` も広げる ④吸収を報告しない
  ⑤「群合計が確かめた要素」を「幅0になった要素」と取り違える

### 7-4. 既存の回帰テストを1件だけ書き換えた（明記しておく）

`test_bug1_no_strong_axis_does_not_create_a_hard_variable` は
「変数を作らないこと」を主張していたが、これが原因A そのものだった。
`test_bug1_no_strong_axis_does_not_widen_a_hard_range` に改め、主張を
**「範囲を 0〜246 に広げないこと」**（バグ①の実害）に変えた。
単位混在から変数を作らないことは
`test_bug1_mixed_units_still_create_no_hard_variable` を新設して押さえている。

### 7-5. 計測スクリプトの数え方も直した（2026-09-22）

**この修正は、既にある計測スクリプトの数え方を壊す。** `run_with_tiers` /
`run_with_policy` は「人が1件ずつ確認する要素」を
**「変数として登録されなかった要素」**として数えていた。これは
`firewall_bridge` が階層3の一部を登録していなかったことに依存した代理指標で、
停止した要素も必ず登録するようになった時点で成り立たなくなる。

数えるべきは **`requires_confirmation` が立ったまま残った要素**である
（質問で解決された要素は `mark_confirmed()` でフラグが降りるので、
`session.question_count` との二重計上にはならない）。
`benchmarks/simulate_tier1_center_agreement.py` と
`benchmarks/run_trial789_reproduction.py` の両方を直した。

これは [[jidou-sekisan-measurement-script-rot]] と同じ落とし穴の2件目である。
**実装を変えたら、その実装を測っているスクリプトの数え方も一緒に見る。**
気づいた経緯は、修正後に `docs/center_agreement_effect_result.json` の
30試行を測り直したら中心値検査の効果が消えて見えたこと。テストはこの JSON を
読むだけなので、全件パスしても変化を検出できなかった。

---

## 8. 切り分けの結論（2026-09-22）— 実装側にも欠陥があった

7-3節で「全件パス」と書いたあと、`docs/center_agreement_effect_result.json` に
凍結してある30試行を測り直したら、中心値検査の効果が消えて見えた
（`weak/overlapping` の「本実装」で 96.1% → 62.1%）。**原因は2つあり、
どちらも本物だった。** 少ない試行（3シード × 3条件）で切り分けた。

### 8-1. 計測スクリプトの数え方（7-5節で直した）

`run_with_policy` は「人が確認する要素」を**「変数として登録されなかった要素」**
で数えていた。停止した要素も登録するようにした時点で、この数は常に 0 になる。
19要素・seed 1 の実測で、精度 0.8947（修正前）→ 0.3158（修正後・旧の数え方）。
**ただしこれは見かけの分だけで、直しても 0.6842 までしか戻らなかった。**

### 8-2. 停止した要素の読みが、他の要素を誤った値に確定させていた（本物の欠陥）

残りは実装の欠陥だった。要素ごとに追うと、はっきり出た（seed 1、kitchen 群）。

| 要素 | 状態 | 登録範囲 | 解 | 正解 |
|---|---|---|---|---|
| `kitchen_0` | 停止（校正済みの強い軸が0） | (3, 3) | (3, 3) | 4 |
| `kitchen_1` | 同上 | (3, 3) | (3, 3) | 4 |
| `kitchen_3` | 同上 | (3, 3) | (3, 3) | 4 |
| `kitchen_2` | 階層2 | (3, 5) | **(3, 3)** | 4 |

停止した3要素の**誤った読み**が、クラスタの等式を通って `kitchen_2` を
(3, 3) に潰していた。修正前は変数として登録されていなかったので、この伝播は
起きなかった。**群合計制約を書けるようにするために変数を作ったことで、
校正を通っていない読みに「他の要素を狭める権限」を与えてしまっていた**
（v8 3-2節が弱い軸に与えないと決めている権限そのもの）。

`detection_range` の分離は**矛盾を見つける方向**だけを直していて、
**他を狭める方向**は塞げていなかった。

### 8-3. 直し方: 方向で分ける

実測校正を通った強い軸が1つも無い要素について、

- **矛盾を見つける方向**（外れたら人に回る、安全側）… 読み取り値が支持する
  狭い範囲（`detection_range`）を使う。群合計制約もクラスタの等式も、
  ここで矛盾を検出できる
- **他の要素を狭める方向**（黙って確定する、危険側）… **何も主張させない。**
  定義域をその単位の許容域（`arbitration/units.py` の `max_value`）まで開く。
  ハード制約としては何も排除しないので、他の要素の範囲を狭められない

`allow_provisional_domain=True` を明示したときだけ、従来どおり読みの和集合を
定義域にする（v8 10章14項の判断は変えていない）。

### 8-4. 直したあとの実測（3シード × 3条件、方針は「本実装」）

| 条件 | seed | 修正前 71420fd | 修正後（8-3の形） |
|---|---|---|---|
| overlapping / weak | 0 | 1.0000（コスト17・質問2） | 1.0000（コスト17・質問2） |
| | 1 | 0.8947（コスト17・質問3） | **1.0000**（コスト17・質問5） |
| | 2 | 1.0000（コスト16・質問4） | 1.0000（コスト15・質問4） |
| overlapping / strong2 | 0 | 1.0000（コスト10） | 1.0000（コスト10） |
| | 1 | 0.9474（コスト11） | **1.0000**（コスト10） |
| | 2 | 0.8947（コスト10） | **1.0000**（コスト11） |
| disjoint / weak | 0 | 0.8421（コスト13） | **1.0000**（コスト17） |
| | 1 | 1.0000（コスト17） | 1.0000（コスト18） |
| | 2 | 1.0000（コスト15） | 1.0000（コスト15） |

**9条件すべてで修正前以上**になった。定義域を開いたことで停止した要素が
質問の対象になり（seed 1 の質問数 3 → 5）、誤った伝播が消えた。

**これは9点の標本であって、効果の測定ではない。** 30試行の測り直しは
Claude Code では行わない（9節）。

---

## 9. Codex 側で測ってほしい項目

**Claude Code では重い計測を行わない**（2026-09-22 のルール変更）。以下は
そのまま Codex の固定ベンチマークに渡すための指定である。

### 9-1. 凍結数値の作り直し（必須）

`docs/center_agreement_effect_result.json` は**修正前のコードで作られており、
もう再現しない。** 8節のとおり、計測スクリプトの数え方と実装の両方が変わった。

- **何を**: `benchmarks/simulate_tier1_center_agreement.py --trials 30`
  の4条件（`weak/overlapping`、`weak/disjoint`、`strong2/overlapping`、
  `strong2/disjoint`）、9方針すべて
- **どのコードで**: 修正後（このブランチ）。修正前 `71420fd` でも同じ手順で
  1回走らせ、**同じ環境で**並べること
- **なぜ**: `tests/test_center_agreement_effect.py` はこの JSON を読んで
  固定値を突き合わせるので、作り直さないと中心値検査の効果を主張する根拠が
  無い状態が続く
- **注意**: 1条件あたり30〜36分かかった（CPU 4コアで4条件を同時に回した場合。
  `strong2/disjoint` はさらに重く、53分でも終わらなかった）。
  **並列にしても速くなるとは限らない**ので、時間見積もりは直列で立てること

### 9-2. 今回の修正の効果（おーちゃんの依頼分）

- **何を**: 修正前 `71420fd` と修正後を、同じ環境・同じ評価データで比べる
- **評価データ**: Codex が用意している「同じ単位の10要素の群」
- **見る数字**: 誤自動確定の件数（Codex の再判定で 456件 → 541件 に増えた指標）。
  あわせて人の確認件数（コスト）も出すこと。**精度だけを見ると、コストを
  増やして精度を買っただけの変更と区別がつかない**

### 9-3. トライアル7・8・9の再現値（影響を受ける）

`docs/trial789_reproduction_report.md` のコスト・階層2母集団・監査件数は、
7-5節の数え方の変更の影響を受ける（「人が確認する要素」の数え方が変わった）。
**精度の数値は影響を受けるか未確認。** 上記と同じ手順で測り直すこと。

### 9-4. 測らなくてよいもの

- 並列化の実時間比較（1本 / 2本 / 4本）。**2026-09-22 におーちゃんが
  「今は行わない」と判断済み**（処理速度の改善は後回し）
