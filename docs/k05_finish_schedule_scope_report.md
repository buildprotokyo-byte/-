# 【報告】内装仕上表から工事の有無を読む(K-05)

作成日: 2026年9月23日
基準: `docs/k05_finish_schedule_scope_criteria.md`(**測る前にコミット済み**)
結果: `docs/k05_finish_schedule_scope_result.json`

---

## 0. 先に結果

P011 匿名化v2 の 3・4 ページの内装仕上表 **69 行すべてが区分に割り当たった。**
どの区分にも割り当てられなかった行は **0 件**。数量は **1 件も作っていない**。

| 部品の区分 | 件数 | 工事区分(5つ)への対応 |
|---|---:|---|
| 工事なし | 16 | 既存のまま |
| 仕上だけやり替え | 12 | 改修 |
| 撤去して新設 | 8 | 改修 |
| 下地からやり替え | 17 | 改修 |
| 問い | 16 | 区分未定 |
| **合計** | **69** | |

工事区分でまとめると **既存のまま 16 / 改修 37 / 区分未定 16**。

**この 5 つの数は、おーちゃんが示した内訳と 1 件も違わない。**

| おーちゃんの内訳 | 件数 | この部品の読み |
|---|---:|---|
| 下地も仕上も「既存」 | 6 | 工事なし |
| 下地「既存」、仕上に材料名 | 12 | 仕上だけやり替え |
| 下地にも材料名 | 17 | 下地からやり替え |
| 下地が「交換」 | 8 | 撤去して新設 |
| 下地が「ー」 | 5 | 工事なし(該当なし) |
| 下地「既存」で仕上が空欄 | 5 | 工事なし |
| 下地が空欄の継続行 10 + 下地空欄で仕上材料だけ 6 | 16 | 問い |

**ただし、この一致をこちらの正しさの証明として読まないこと。** 下地欄が
どの列かを調べる段階で同じ集計を一度しており、そのことは基準にも書いてある。
**独立しているのは「おーちゃんの内訳がこちらのコードと別に数えられたもの
である」という一点だけ**である。

## 1. 基準に対する判定

| 記号 | 指標 | 線 | 実測 | 判定 |
|---|---|---|---:|---|
| M1 | 割り当てられなかった行 | 0 件 | **0** | 通過 |
| M2 | 区分ごとの件数 | 内訳と完全一致 | **完全一致** | 通過 |
| M3 | 作った数量 | 0 件 | **0** | 通過 |
| M4 | 根拠にページと行番号がある割合 | 100% | **69/69** | 通過 |

対照も通った。

| 記号 | 対照 | 結果 |
|---|---|---|
| C1 | 合成の仕上表(6 通りを 1 行ずつ) | 6 行とも表どおり |
| C2 | 下地の列が無い仕上表 | 全行が問い。工事の有無を名乗らない |
| C3 | 壊し試験 2 通り | 2 通りともテストが落ちた(下記) |

壊し試験は `git worktree` に複製した別の木でやり、`__pycache__` を消して
`PYTHONDONTWRITEBYTECODE=1` で走らせている。

| 壊し方 | 落ちたテスト |
|---|---|
| 読み方の表から「交換」と「ー」の 2 行を抜く | 3 件 |
| 下地欄が空欄の継続行を「仕上だけやり替え」に倒す | 8 件 |

## 2. 測る途中で分かったこと(3 件)

### 2-1. 下地の欄は 1 列ではなく、**2 列にまたがっていた**

実図面の仕上表は「下 地 類」の見出しが 2 列に横結合されていて、
**右側の列には見出しが無い。** 下地の列を 1 列だけ読むと、区分の決め手に
なる「既存」「交換」「ー」の欄を丸ごと落とす。

そこで、**見出しが空の列を、すぐ左の「下地」の続きとみなす**ようにした
(`_base_column_group()`)。広げるのは見出しが空の列だけで、ほかの役割に
当たった列や文字のある見出しでは止める。

区分の判定に使うのは、**その組のいちばん右の列**である。左の列には工法
(「〜組新設」の形)が入っていた。**この左右の決め方は 1 つの案件でしか
確かめていない。** 左の列だけに文字があって右が空、という行は P011 の
69 行には 1 件も無かった。

### 2-2. 読み取り側は、問いになる行の一部を**落としていた**

`axes/image_axis/schedule_tables.py` は「部位も仕上も空」の行を行として
読まない。おーちゃんの言う「下地が空欄の継続行」のうち **10 件がこれに
当たる**(材料名がメーカー名・品番の欄にだけ書かれている行)。
落としたままだと**問いが 16 件ではなく 6 件**になる。

この部品は `skipped_rows` から拾い直している。**ただし、文字が 1 つも無い
行は拾わない。** 仕上表には罫線だけ引いてある空の行が 51 行あり、これを
継続行として拾うと問いが 67 件に膨らんだ(最初の実行がそうなった)。

### 2-3. 「下地からやり替え」17 件のうち **16 件は、左の列も「新設」と
言っていた**

2-1 の左の列(工法)に「〜組新設」と書かれている行が 16 件ある。これは
右の列の材料名から出した読みと同じ向きを指している。**ただし同じ表の
同じ行なので、独立した 2 つ目の根拠ではない。** 階層 1 には効かない。

## 3. 5 つの工事区分への対応づけ(**おーちゃんの判断を仰ぎたいところ**)

対応づけは `docs/principles/scope_of_work_diff.md` 4-1節の 5 区分
(撤去 / 新設 / 既存のまま / 改修 / 区分未定)に対して、こう置いた。

| 部品の区分 | 工事区分 | 置いた理由 |
|---|---|---|
| 工事なし | 既存のまま | 現況と計画が同じ |
| 仕上だけやり替え | 改修 | 部位は残り、属性(仕上)だけが変わる |
| 撤去して新設 | 改修 | 部位は残るが、下地を撤去して新設する |
| 下地からやり替え | 改修 | 同上。下地から替わる |
| 問い | 区分未定 | 根拠が無ければ区分不明(原則・第1段階) |

**気になるところが 3 つある。**

1. **3 つの読みが「改修」1 つに畳まれる。** 仕上だけやり替えるのと下地から
   やり替えるのでは、単価も工程も違う。畳んだ結果しか下流へ行かないと
   その差が消えるので、**読みのほうも別の欄に残したまま運んでいる**
   (`FinishScopeAssignment.reading`)。
2. **「撤去して新設」は 1 件の中に撤去と新設の両方が入っている。**
   5 区分は 1 件に 1 つの区分しか持てないので、片方が見えなくなる。
   消えたほうを `alternatives` と注記に残してあるが、**見積の行にするとき
   2 本になるのか 1 本なのかは、この部品では決めていない。**
3. **下地欄が「ー」の 5 件を「既存のまま」に置いたのは、言い過ぎかもしれない。**
   おーちゃんの読みは「該当なし。工事なし」で、**その部位がそもそも無い**
   という意味に読める。「既存のまま」は「あるが手を入れない」なので、
   意味が 1 つずれる。要素を作らずに落とす手もあるが、**落とすと
   「工事が無い」と「読み落とした」の区別が消える**ので、いまは要素を作って
   理由に「該当なし」と書いてある。

## 4. この部品がしないこと(意図して外したもの)

1. **数量を出さない。** 面積と長さは人の入力から来る決まり(原則 3-1・4)。
   69 行すべてで `value_range` も `unit` も None である。
2. **知らない書き方を材料名とみなさない。** 「未定」「要確認」のような語を
   材料名として読むと、ありもしない工事が 1 件増える。表に無い書き方は
   問いへ回す。**この語の一覧は校正していない。** P011 の 69 行には
   このどれも出てこないので、入れても外しても実案件の数は変わらない。
3. **下地の列が無い表では工事の有無を名乗らない。** 「下地が既存だから
   仕上だけ」という読みは、下地欄があって初めて成り立つ。
4. **本番の入口からはまだ呼ばれていない。** この部品は
   `intake/drawing_intake.py` に繋いでいない。繋ぐかどうかは、3節の
   対応づけが決まってからにしたい。

## 5. 変えたファイル

| ファイル | 何を |
|---|---|
| `estimating/scope_diff.py`(新規) | 工事区分の 5 つの値と、要素 1 件の入れ物。**設計 6節のうち埋められる欄だけ** |
| `estimating/finish_schedule_scope.py`(新規) | 読み方の表と、行から要素・問いを作るところ |
| `axes/image_axis/schedule_tables.py` | 内装仕上表に**下地の列**を足した。横結合の見出しに対応。行の升目の文字を `row_texts` に残すようにした |
| `benchmarks/measure_finish_schedule_scope.py`(新規) | 数えるだけ。**図面のパスは引数** |
| `tests/test_finish_schedule_scope.py`(新規) | 17 件。**実装より先に書いた** |

**判定に触る変更なので、おーちゃんの指示どおり PR のまま置く。マージしない。**
