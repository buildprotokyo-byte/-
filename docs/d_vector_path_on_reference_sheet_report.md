# 55周目 報告: **正解データのある紙では、ベクターの経路は 1 件も動かない**

- **基準**: `docs/d_vector_path_on_reference_sheet_criteria.md`(測る前にコミット `1b0c7e6`)
- **ベンチマーク**: `benchmarks/measure_vector_path_on_reference_sheet.py`
- **実装は 1 行も変えていない。**

## 結論

**「ベクターの円弧が正解を何件当てているか」は、実図面では測れていない。**
**当たっているとも外れているとも言えない。**

| | 古いほう(**正解データのある紙**) | 匿名化 v2(**90 件を拾っている紙**) |
|---|---|---|
| ページ数 | 34 | 34 |
| 文字数の合計 | **0** | (measure 済み・29,003) |
| **ベクターの図形があるページ数** | **0** | — |
| 縮尺が読めたページ数 | **0** | 20 |
| **拾った円弧の合計** | **0** | **90** |

**理由は「縮尺が読めないから」ではない。ベクターの図形が 1 ページにも無いからである。**
**スキャンなので、探す相手がそもそも存在しない。**

## 対照(全部通った)

| 対照 | 結果 |
|---|---|
| 1 同じ関数を v2 に当てた合計 | **90 件**(35 周目と同じ)。**経路は生きている。紙のせいだと切り分けられた** |
| 2 古いほうのページ数 | **34** |
| 3 反復 3 回 | **0・0・0** |

## 52 周目からの 4 周で分かったこと

| 周 | やったこと | 結果 |
|---|---|---|
| 52 | v2 の 8 ページ目で正解と比べようとした | **対照1 で停止**(図面番号の文字が無い) |
| 53 | v2 の紙を室名で特定しようとした | **対照4 で停止**(目次が 1 位に並ぶ) |
| 54 | 箱の画素が収まる紙を探した | **正解データは古いほうのファイルのものと確定** |
| 55 | その紙に同じ経路を当てた | **ベクターの図形が無いので 0 件** |

**4 周かけて出た答えは「測れない」である。**
**これは失敗ではない。「測った」と書けるはずのないものを、書かずに済んだ。**

## **既にある報告書は嘘をついていたか — いいえ**

`docs/real_drawing_eval_report.md` の 11-3 節は、**先にこう断ってある。**

> **この「過検出 0・見落とし 0」を成績として読まないこと。** 正解はこの実装の
> 出力を目視で確認して作ったものなので、一致するのは当たり前である。

**だから「当てた」とは書いていない。**
**足りなかったのは、参照データ(`HINGED_DOOR_ARCS`)がどちらのファイルのものかの記録**である。
**そこを但し書きで足す。**

**あわせて 1 か所、言い方が不正確だったので直す。**
11-3 節の表は v1 の 0 件を「**縮尺が読めないので**何も返さない」と書いているが、
**正しくは「ベクターの図形が 1 つも無いので」である。**
(同じ報告書の 82 行目には「ベクターの描画オブジェクト **全ページ 0 個**」と書いてあり、
**報告書の中で食い違っていた。**)

## 変えたもの(**どちらも記録だけ。判定のしかたにも基準値にも触っていない**)

1. `docs/real_drawing_eval_report.md` 11-3 節に但し書きを追加
2. `benchmarks/real_drawing_fixtures.py` の**説明書きに、どちらのファイルのものかを明記**
   (**データは 1 件も変えていない。コメントだけ。**)

## この周で変えなかったもの

- 製品コード **0 行**
- 判定のしかた・基準値・階層の条件 **触っていない**
- **51 周目の結論は変えない**(箱の座標を使っていないため)
- **室の名前も図面の文字も座標も、報告書にも記憶にも書いていない**
