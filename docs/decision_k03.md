# 決定の記録: K-02 の判断待ち 53 件への回答(2026-09-23、おーちゃん)

K-02 で 1 つの一覧(53 件)にまとめた判断待ちに、おーちゃんが [K-03] で答えた。
一覧の原文は `/mnt/project-files/reports/K-02_回答.md`(共有フォルダ。リポジトリには入れていない)。

## 推奨どおり(「これで」)

1〜19、22〜36、39〜51、53。

このうち取り込み(1〜9)は同じ日に実施した。内訳は下の「9 番②の実施」。

## 推奨と違う判断、または条件が付いたもの

| 番号 | 何を決めたか | 決定 |
|---|---|---|
| 6 | #89(確認済みにしてもデータ源の数を消さない)を入れるか | **入れる。** ただし今後、判定に触る変更を「実案件では何も変わらないから」という理由で自分で判断しない。**変わらないことを測って示してから、おーちゃんに聞く。** |
| 20 | 凡例と線の種類を読む実装をいつ入れるか | **後。ただし仮の判断。** 推奨の理由(13 周目に記号の名前は自動では取れないと分かり、人の入力に切り替えた)は Claude の推論で、測っていない。**人の入力に切り替えた判断が正しかったかは、22 件のデータがそろってから測り直す。** |
| 21 | 部屋の認識をどの順番で入れるか | **後。ただし仮の判断。** 理由は 20 と同じ(面積を人の寸法から出す方向に移ったという推論)。**22 件のデータがそろってから測り直す。** |
| 37 | 会社のルールの中身 3 つ(墨出し・竣工時清掃の値、荷上費、駐車場代) | **後回し。** おーちゃんが思いつきで答えると 1 件の癖が会社のルールとして固定されるので、**知識を担当するスレッドが作っている質問リストに合流させ、まとめて答える。** |
| 38 | 比較実験の封印フォルダ 3 つを消すか | **消さない。** 比較実験の汚染の検証に要る。**試験 C は封印フォルダを残したまま始められる形を考える。** |
| 52 | 2 件目の案件の原価表と図面は同じ建物か | **おーちゃんが調べる。分かるまで 2 件目の組み立ては進めない。** |
| 53 | 建具表の見出しの当て方(#95)を Codex-A に実図面で測ってもらうか | **頼んでよい。ただし測定担当は今ほかの作業が詰まっているので、順番待ちに入れる。** |

## 同時に出た最優先の指示

比較実験(読み方の A/B)が記憶の索引を通じて汚染していた疑いがあるので、調査が終わるまで
**比較実験を根拠にした新しい判断はしない。**

## 9 番②の実施(採用した周だけ本線へ、不採用の周は報告書を残して PR を閉じる)

- 本線へ入れたもの: #62・#68・#72・#73・#74・#75(1 つの束。#74・#75 は 10 番の「実装を残す」、
  #62 は 11 番の「判定は不採用、実装は残す」による)、#76(#83 に含まれる)、#81、#82、#83。
- **報告書だけ本線へ入れ、PR を閉じたもの: #77・#78・#79・#80。** 実装(`axes/image_axis/legend_region.py`、
  `benchmarks/measure_real_drawing_symbols.py`、`benchmarks/measure_legend_one_per_row.py` ほか)は
  本線に入れていない。それぞれのブランチに残っている(ブランチは消していない)。
  報告書の中で「残す」と書かれている実装も、本線ではなくブランチに残っているという意味になる。
- **#62 の判定は「保留」から「不採用」に変わった**(11 番)。根拠は #77(10 周目)の実図面での測定。
  実装は本線に残す。
