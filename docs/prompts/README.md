# マスタープロンプト一式(`docs/prompts/`)

## いまの状態: **一式がこのリポジトリに無い**

おーちゃんの K-31(2026-09-24 15:11)は、一式の 7 つの文書を直す指示だった。
ところが、**そのうち 6 つがこのリポジトリのどこにも無い。**

| 番号 | ファイル(おーちゃんが呼んだ名前) | いまの状態 |
|---|---|---|
| 00 | マスタープロンプト統合版 | **無い。**原則 2-2 を足せない |
| 01 | `01_認識AI_見る.md`(v1) | **無い。**`_v1_旧.md` として退避できない |
| 01 | 認識AI(読む)v2 | **置いた** → [`01_認識AI_読む_v2.md`](./01_認識AI_読む_v2.md) |
| 02 | 理解AI | **無い。**境目の記述を合わせられない |
| 03 | 最適解AI | 無い(ただし K-31 では変更不要) |
| 04 | 異常検知AI | **無い。**疑う対象を足せない |
| 05 | 技術選択のルール | **無い。**手順の先頭に足せない |
| 06 | ループ作業マスタープロンプト | **無い。**ループの決まりを足せない |

### 探した範囲(2026-09-24)

- `docs/prompts/` — フォルダ自体が無かった(このコミットで作った)
- リポジトリの**全部の枝の全部のコミット**(`git log --all --name-only`)
  — 名前に `prompt` / `認識` / `マスター` を含むファイルは
  `docs/claude_handoff_prompt.md` と `drawing_ai/prompts.py` の 2 つだけ。どちらも別物
- 本文検索(`理解AI` / `最適解AI` / `異常検知AI`)— 該当なし
- 共有フォルダ(`handbook/` `knowledge/` `reports/` `uploads/`)— 該当なし
- この案件に上がっているテキストの添付 7 件 — 設計書・試験の報告で、一式ではない
- 元のプロジェクトの会話 — このプロジェクトは引き継ぎ元が無い(`upgraded_from_project: false`)

**つまり一式は、リポジトリの外(おーちゃんの手元か、別の場所)にある。**

## どうしたか

1. **01 v2 は原文のまま置いた。**差し替えではなく新規。v1 の退避はできていない
2. **00・02・04・05・06 に足す原文は、[`K31_適用待ち.md`](./K31_適用待ち.md) に
   そのまま写してある。**元の文書が届いたら、そこから貼るだけで済む
3. **こちらで一式を書き起こさなかった。**無いものを想像で埋めると、
   おーちゃんが直した版と食い違ったまま両方が使われる

## おーちゃんにお願いしたいこと

**00・01(v1)・02・03・04・05・06 の原文を貼ってください。**
届いた順に、`K31_適用待ち.md` の指示を当てて PR にします。

## なぜ「一式をリポジトリに入れる」のを先にするか

K-31 の本文にこうある。

> 個別の指示で塞いでも、**大元が古いままでは戻ります。**

一式がリポジトリの外にあるかぎり、**直した版がどれかを機械が確かめられない。**
直っているかどうかが、そのつど人の記憶に戻る。これは K-31 が名指しした構図と同じ形で、
一段上にある。だから**直す前に、直す対象を版管理に入れる**のを先に置いた。
