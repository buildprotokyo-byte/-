# 34周目 報告: 向きで分ける揃え方を、実装に入れた(**採用**)

2026-09-23。基準は `docs/b_item_key_normalisation_criteria.md`(測る前にコミット済み、`372d885`)。
**ここまでの 2 周(32・33)は測っただけで、この周で初めて実装に入った。**

## 先に落ちるテストを書いた(決まり②)

足場は「**いまの `multi_path_reconciler.PathItem.item_key` が文字列そのままで比べている**」
振る舞いをそのまま関数にしたものにした。**つまり落ちた 8 件は、いまのコードで起きていること
そのものである。**

| | 件数 |
|---|---|
| **C1 実装前に落ちた** | **8**(語順・中黒・括弧・末尾の空白・送り仮名が揃わない 5 件 / 確定させる向きでも記号と長音が揃わない 1 件 / 知らない向きがエラーにならない 1 件 / 空の鍵がエラーにならない 1 件) |
| 実装前に通った | 3(何も揃えなければ当然通る。**実装後も通り続けることが大事な 3 件**) |
| **C2 実装後** | **12 件すべて通る** |

コミットを分けてある。`ac630a3` が赤、その次が緑である。

## C3 33 周目の数が、実装でも同じに出た

| 向き | 真の一致 | 33 周目 | 偽の一致 | 33 周目 |
|---|---|---|---|---|
| 確定(狭い) | 49 / 124 = **0.395** | 0.395 | **0** | 0 |
| 人へ(広い) | 123 / 124 = **0.992** | 0.992 | **4** | 4 |

内訳(動作が違うのに同じ鍵 0 件 / 部位が違うのに同じ鍵 4 件)も 33 周目と同じである。

## C5 壊し試験: **最初は 5 / 6 だった**

| 壊し方 | 1 回目 | テストを足したあと |
|---|---|---|
| 1 既定の向きを広いほうに変える | 落ちた | 落ちた |
| 2 確定させる向きでも語順を無視する | 落ちた | 落ちた |
| 3 知らない向きを黙って通す | 落ちた | 落ちた |
| 4 送り仮名を落とす処理を消す | 落ちた | 落ちた |
| 5 括弧の中身を外に出す処理を消す | 落ちた | 落ちた |
| **6 ひらがなをカタカナにする処理を消す** | **落ちない** | **落ちた** |

**6 番が落ちなかった。その処理を確かめるテストが 1 つも無かった。**

**中身を見て、処理のほうではなくテストのほうが足りないと判断した。**
「とい」(樋)は内訳書標準書式ではひらがなだが、図面や見積ではカタカナで書かれることがある。
**同じ行である。** 揃えるのが正しい。だから**処理を消さず、テストを 1 件足した。**

**この判断は、測ったあとに測るものを変えた形である。** 15 周目・22 周目に同じ場面があり、
そのときも「落ちない壊し方の原因を直して測り直し、両方の数を書く」形にした。
**1 回目の 5 / 6 を隠していない。**

足したテストは、**当たり判定を緩めるものではなく、確かめていなかった振る舞いを
確かめるものである**(緩めるなら壊し試験は通らなくなる)。

## C4 全件テスト

****977 件パス**(1 件 skip、205.96 秒)。この周で足したテストは 12 件(965 → 977)。この枝は main から出しているので、9 周目の 28 件を含む PR #83 側とは数が違う。**

## 判定

**採用。** C1 = 8 > 0、C2 は 12 件すべて通り、C3 は 33 周目と一致、C4 は通り、
C5 はテストを足して 6 / 6。

## 作ったもの

`arbitration/item_keys.py`。

- **向きを指定したら揃え方が決まる。呼ぶ側が揃え方を選べない。**
  選べるようにすると、いつか誰かが確定させる向きに広いほうを渡す。
- **既定は狭いほう。** 何も指定しなければ安全側に倒れる。
- **知らない向きは `ValueError`。黙って広いほうに倒さない。**
- 壊れる 4 件(壁の天井下地 / 天井の壁下地)を **docstring に書いてある。**

## まだ配線していない(**わざと**)

**突き合わせの層からは、まだ呼ばれていない。** 足すだけの変更である。

理由を数で書く。いまリポジトリには**枝の上に枝を積んだ PR が 10 本ある。**

> #62 ← #68 ← #72 ← #73 ← #74 ← #75 ← #77 ← #78 ← #79 ← #80

**CI は `main` を土台にした PR にしか走らない**ので、**この 10 本には 1 本もテストが
走っていない。** 突き合わせの層(`arbitration/multi_path_reconciler.py`)は #72 の中にある。
**ここに 11 本目を積むと、また CI の走らない PR が増える。**

揃え方そのものは突き合わせの層に依存しないので、**単独で `main` の上に置いた。
テストが走る。** 配線は #72 がマージされたあとの 1 行で済む。

**この 10 本の話は、おーちゃんに 1 回だけ伝える**(新しい頼みごとではなく、
いま止まっている場所の報告として)。

## 次の周(35 周目)

**配線は #72 のマージ待ちなので、別の題に移る。**

マスタープロンプト 9 章の優先順で、いま着手できるのは:

- 1)A-1 … 2 件目待ち
- 2)A-2 … 2 件目待ち(31 周目で決着)
- 3)トライアル C … フォルダを消す許可待ち
- 4)**トライアル B の続き** … 配線は待ちだが、**突き合わせそのものを実図面で測っていない**
  (PR #72 の「測れていないこと」に「合成した 1 案件での測定のまま」と書いてある)
- 5)A-3 … 中身はおーちゃんの確認が要る

**35 周目は 4)にする。** 実図面(匿名化 v2)から実際に取れる経路
(建具表・仕上表・ベクトルの記号・印字の寸法)を並べて、
**どの経路がどれだけ重なり、どれだけ食い違うか**を測る。基準は測る前に書いてコミットする。

**ただし 29 周目の申し送りがある。** `read_door_schedules` は匿名化 v2 の 34 ページで
**0 ページ・0 行**だった。**経路が 1 本すでに折れている。** そこも含めて測る。

## この周で変えていないもの

判定のしかた・基準値・階層の条件には触れていない。**足すだけの変更である。**
本番の経路からはまだ呼ばれていない。
実案件・正解ファイル・実図面は**一切使っていない。テストは合成データである。**
