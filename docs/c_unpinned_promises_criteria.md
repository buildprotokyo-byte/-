# 46周目 基準(トライアルC): コードが自分で宣言した前提のうち、**守られていないのは何件か**

**このファイルは測る前に書いてコミットする。測ったあとで基準を変えない。**

2026-09-23。45 周目の「次の周」に書いたとおり。

## なぜこの題なのか

45 周目に 1 件見つけた。`intake/drawing_intake.py:931` の
「**表の読み取りは縮尺に依存しない**」というコメントは**正しかった**が、
**それを固定したテストが 1 件も無かった。** 壊して全件テストを流したら
**既存 977 件が全部通った。**

**コメントが正しいことと、コメントが守られていることは別である。**

**見つけ方は 1 回きりの思いつきではない。** 同じやり方でほかの前提も測れる。
**測れるなら数えるべきである。** 1 件だけなら偶然、複数あるなら**穴の形**である。

## 候補の選び方(**測る前に決める。あとで足さない・減らさない**)

`intake/` `axes/` `arbitration/` の**製品コード**(テストは含めない)のコメントと
docstring から、**振る舞いの約束を言い切っている**もの(「〜しない」「〜に依存しない」
「絶対に〜しない」「〜だけ」)を拾った。**そのうち、壊し方を 1 か所の文字の置き換えで
書けるものを 8 件選んだ。** 下の表が全部で、**この 8 件で固定する。**

| 記号 | 場所 | 宣言している前提 | 壊し方(**1 か所の置き換え**) |
|---|---|---|---|
| **C1** | `axes/image_axis/pdf_vector_symbols.py:301` | 呼び出し順に依存しないよう位置で並べる | `out.sort(...)` の行を消す |
| **C2** | `arbitration/consistency_solver.py:595` | 単位が違うレンジは比較しない | `if advisory.unit and match_unit and advisory.unit != match_unit:` → `if False:` |
| **C3** | `arbitration/method_policies.py:130` | vtracer の床面積はハード制約に**絶対に昇格しない** | `METHOD_FLOOR_AREA` の `max_strength="weak"` → `"strong"` |
| **C4** | `arbitration/method_policies.py:134` | **人の入力もそれだけを根拠に自動確定させない** | `METHOD_HUMAN_REFERENCE_POINT` の `calibrated=False` → `True` |
| **C5** | `axes/image_axis/schedule_tables.py:250` | 建具番号が空の行はそもそも行にしない | `if not mark:` → `if False:` |
| **C6** | `arbitration/consistency_solver.py:281` | 弱い軸は z3 の変数にならず、ハードな解に影響しない | `if strength == "weak" or reading.status != "confident":` → `if False:` |
| **C7** | `intake/drawing_intake.py:159` | 建具表の数量と開き戸の円弧は**別の対象**にしてある | `TARGET_DOOR_QUANTITY_PREFIX = "建具数量::"` → `"開き戸::"` |
| **C8** | `arbitration/method_policies.py:131` | 図面に印字された面積は階層1の根拠にならない | `METHOD_TEXT_AREA` の `calibrated=False` → `True` |

**C4 と C8 は記憶に「危ない裏返し」として名指ししてある 2 つである。**
壊したときに全件テストが黙るなら、それは**いちばん黙ってほしくない場所で黙っている**ことになる。

## 測るもの

各候補について、**壊す → `__pycache__` を消す → 全件テストを流す → 戻す**。

| 記号 | 何を測るか |
|---|---|
| **X1** | 試した前提の数(= 8) |
| **X2** | **壊して全件テストが落ちた**数 |
| **X3** | **落ちなかった**数 |
| **X4** | X3 のうち、**壊れが実際に出力を変えることを示せた**数 |
| **X5** | X3 のうち、示せなかった数 |

**X4 が「いま守られていない前提の数」である。**

## **28 周目で踏んだ穴を先に塞ぐ**

**壊したつもりで何も壊れていない候補は、全件テストが通って当たり前である。**
それを「気づかなかった」に数えると、**数が勝手に増える。**

だから**落ちなかった候補は、それだけでは X4 に数えない。**
**壊した側と元の側で値が違うことを 1 つでも示せたときだけ** X4 に数える。
**示せなければ X5(壊せなかった)** に入れる。**X5 は穴の証拠にならない。**

## 結論の形(**測る前に決める。結果を見てから変えない**)

- **X4 ≧ 3** → **穴は 1 件ではなく形である。** 前提を固定するテストを足す周に進む
  (**テストだけ。判定のしかた・基準値には触らない**)。
- **X4 = 1 または 2** → **45 周目の 1 件を含めても少数。** 単発として扱い、
  見つかった分のテストだけ足して、**トライアル C の本体(段取り待ち)へ戻る。**
- **X4 = 0** → **45 周目の 1 件が例外だった。** 何も足さずトライアル C の本体へ戻る。

## 対照

| 対照 | 中身 | 通る条件 |
|---|---|---|
| **1(効くことの確認)** | `intake/drawing_intake.py` の `_apply_area_basis` で、回答が無いときに `available[0]` を既定として採るようにする(**取り決めで禁じられている形そのもの**) | **全件テストが落ちる。** 落ちなければ**測り方のほうが壊れている**ので、結論を出さない |
| **2(素の値)** | 何も壊さずに全件テストを流す | **978 passed, 1 skipped** |
| **3(戻せたか)** | 8 件ぜんぶ戻したあとの `git diff` | **空** |

**対照1 が落ちなければ結論を出さない。**

## この周で変えないもの

- **判定のしかた・基準値・階層の条件には触らない。** 壊しは全部その場で戻す。
- 足すとしてもテストだけ。**製品コードの振る舞いは 1 つも変えない。**
- 実図面は使わない(**この周は全件テストだけで完結する**)。
