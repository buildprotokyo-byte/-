# K-43: AI に設計のプロの立場を与えて、改装平面図(8 ページ)を読ませる — **測る前の基準**

おーちゃんの K-43(2026-09-26 11:07〜11:09 JST)。この文書は**測る前に**コミットする。結果を見てから直さない。
品目名・金額は書かない。立場の文面はおーちゃんの原文を一字も変えずに使う(共有フォルダ `reports/K-43/立場_原文.txt` に置く)。

## 渡すもの

- **P011 匿名化 v2 の 8 ページ(改装平面図)の画像だけ。**全体 1 枚(200dpi)と、4 分割の拡大 4 枚(300dpi)。
- 機械が抽出した線・座標・文字の一覧、仕上表・見積・正解ファイルは渡さない。正解(1,925×1,485)も見せない。
- 読む役ごとに別のフォルダに画像を置き、「渡した画像のほかは、どのファイルも開かないこと」と書く。**これは指示による隔離で、破らなかったことを機械で確かめる手立ては無い**(K-21 と同じ限界)。

## 比べる条件(各 3 回、互いに独立)

| 条件 | 立場 | 問い |
|---|---|---|
| **P 設計のプロ** | おーちゃんの立場の文面(原文のまま) | おーちゃんの問い 1〜5(原文のまま) |
| **N1 前提なし(原文どおり)** | なし | 「この図面に何が描かれていますか」とだけ |
| **N2 前提なし(問いはそろえる)** | なし | P と同じ問い 1〜5 |
| **D 囮** | P と同じ立場 | P と同じ問い。**画像は左右反転したもの**(読む役には反転したことを伝えない) |
| **機械** | — | いまの台帳と壁探しの結果(K-40・K-41。新しくは走らせない) |

- **N2 を足した理由(仮の判断)**: 原文の「前提なし」は問いも短いので、P との差が立場のせいか問いのせいか分けられない。変えるのが立場だけの条件を足す。**主の比べ方は原文どおりの N1**、N2 は分けるための補助。
- 答えの形: 問い 1〜5 の文章に加えて、数えるために次の形の JSON を付けさせる(N1 は文章だけ。JSON は求めない)。
  - 壁: `{位置(室名と辺。辺は画像の上・下・左・右で言う), 種類(躯体・界壁・間仕切り・ふかし壁・外壁・分からない), 撤去(できる・できない・分からない), 根拠}`
  - 室: `{室名, 範囲}`、書斎の縦横: `{横_mm, 縦_mm, 根拠}`(分からなければ null)
- 「答えられないことは分からないと書く。埋めない」を P・N2・D に書く。

## 測ること(書斎で検算)

1. **書斎の縦横**: 横(画像の左右方向)が 1,925 の ±5%(1,829〜2,021)、縦が 1,485 の ±5%(1,411〜1,559)に**両方**入るか。向きを取り違えたら外れとする。
2. **書斎の 4 辺**: 何が指されたか。**比べる答えは K-41 の私の読み(上と左は凡例 k の新設の軸組=間仕切り、下は LDK 側に開いている、右は電動ブラインドで仕切る)で、正解ファイルではない。確かめていない読み**として並べる。
   - 上と左を「間仕切り(新設)」と指せたか。下と右を「壁ではない(開口・ブラインド)」と言ったか、壁と言ったか。
3. **壁の種類が仕上表・見積と整合するか**(採点は私。読む役には仕上表・見積を見せない):
   - ふかし壁を、LDK の西(戸境壁=界壁の側)に置いたか。
   - 灰色の塗りの壁を、躯体(外壁・界壁を含む)としたか。
   - 新設の間仕切りを、書斎・WIC・廊下物入のまわりに置いたか。
4. **3 回の一致**: 壁の(室名・辺・種類)の組、書斎の縦横(3 回の値が互いに ±5% 以内なら同じ)、室の数ごとに、3 回とも同じ・2 回・1 回だけ、を数える。**3 回とも同じものだけを「強い」とする。**

## 合格の線(測る前に決める)

「立場を与えると読める」と言えるのは、次を**全部**満たすとき:

1. P の 3 回のうち 2 回以上で、書斎の縦横が両方とも ±5% に入る。
2. P の書斎の縦横の当たりの回数が、N1 と N2 のどちらよりも多い(N が 3 回とも当てたら、P が 3 回でも「立場の差は無い」と書く)。
3. 囮 D で、書斎の縦横を「分かる」と答えて ±5% に入った回が、P より少ない。**D が P と同じ回数入ったら、P の答えは疑う**(おーちゃんの指示。反転しても寸法の数字は裏返しになるので、数字を読めたなら反転に気づくはず)。

**不通過になる数字の例**: P が 3 回とも 1,925×1,485 に入っても、N1 も 3 回とも入れば線 2 を満たさない(立場の差ではなく、印字を読んだだけ)。

合否と別に、4 辺・壁の種類・3 回の一致・「描き方の癖」の答えをそのまま並べる。囮が反転に気づいたかも数える。

## 守ること

- 自動確定は 0 件のまま。読む役の答えは候補で、確定させない。
- 人の入力・合成の寸法は使わない。PDF の文字に残る事務所名・個人名は、どの出力にも書かせない。
