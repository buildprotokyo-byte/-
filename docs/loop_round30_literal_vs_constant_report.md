# 周30 の報告 — **予想が大きく外れた。「惜しい組」は 490 件出て、本物は 1 件だけだった**

**基準**: `docs/loop_round30_literal_vs_constant_criteria.md`(測る前にコミット、`4d45754`)
**道具**: `benchmarks/measure_literal_vs_constant.py`(**構文木で読む。正規表現ではない**)
**数えた範囲**: `app.py`・`axes/`・`estimating/`・`intake/`・`arbitration/`
**数えた量**: 定数 **179 個**、文字列 **3,779 個**(説明文は除く)

---

## 線3(対照)— 数え方は、既に分かっている 1 件を拾えるか

**拾えた。**`KIND_PERIMETER = "室の周長"` と、`app.py` 66 行目の `"周長"` の組が
「惜しい」として出た。**だから線1 の結果を読んでよい。**

---

## 線1 — 「惜しい」組は何件あるか

**予想は 1 件。実際は 490 件。大きく外れた。**

**基準にこう書いてある**——「**『惜しい』組は、そのまま欠陥とは限らない。
だから件数を出したあと、1 件ずつ中身を見て振り分ける。**」
**490 件は手で見るには多すぎたので、見る場所を狭める欄をあとに足した。**
**線1 の件数は動かしていない。**

### あとから足した診断(**判定には使っていない**)

**モジュールの直下に置かれた入れ物(辞書・組・一覧)の中にある文字列**だけに絞る。
**引き当ての表はそこに書かれる**(`app.py` の `FINISH_PART_QUANTITY` がその形)。

**52 件。**これは手で見られる数なので、**52 件を 1 件ずつ見た。**

| 振り分け | 件数 |
|---|---:|
| 長い注記・説明の文に、短い定数が偶然入っていた | 約 10 |
| 英語の識別子の断片(`scale` と `ocr_text_scale` など) | 約 15 |
| 日本語の部分語だが**別のものを指す**(`下地` と `下地欄が空欄の継続行`、`床面積` と `施工対象床面積`) | 約 26 |
| **同じ鍵空間の食い違い(本物)** | **1** |

**本物は 1 件。周28 で見つけた巾木の `周長` / `室の周長` である。**

**振り分けはこちらの判断である。**機械で分けたのは 490 → 52 までで、
**52 → 1 は人が読んで決めた。**

---

## **この周でいちばん持ち越す値打ち — この数え方は、そのままでは使えない**

**「惜しい」だけを機械で数えると 490 件出て、本物は 1 件。**
**外れが 99.8% である。**

**だからこの道具を「見張り」にはできない。**
毎回 490 件を人が見ることになり、**そのうち 489 件は何も起きていない。**
**見張りが鳴りっぱなしなら、見張りは無いのと同じである。**

**使えるのは、今回のように「1 件見つかったあとで、ほかにもあるかを一度だけ
確かめる」使い方**である。**それは今回やった。答えは「ほかに無い」。**

---

## 線2 — 「書き写し」は何件あるか(**判定は置いていない**)

**定数があるのに、別のファイルで同じ文字列を直接書いている場所: 115 件。**

| ファイル | 件数 |
|---|---:|
| `intake/drawing_intake.py` | 19 |
| `intake/start_kit.py` | 15 |
| `arbitration/units.py` | 13 |
| `app.py` | 12 |
| `axes/image_axis/pdf_room_outlines.py` | 8 |
| `estimating/from_room_dimensions.py` | 8 |

**これは欠陥ではない。**値が一致しているので、いま動いている。
**危ないのは、定数の側が変わったときに気づけないこと**である。

**「減らすべきだ」とは書かない。**減らす価値があるかは測っていない。

---

## 書かないこと(**基準に先に書いてある**)

1. **「食い違いは無い」とは書かない。**
   **定数に置かれていない名前どうしの食い違いは、この数え方では拾えない。**
2. **「書き写しを減らすべきだ」とは書かない。**数えただけである。
3. **直していない。**K-29 に従い、見つけたものは試験で固定して判断待ちに回す。

## 次の周に渡すもの

**周28 の巾木の 1 件を直すかどうか**は、おーちゃんの判断待ちのままである
(#186)。**周30 で「ほかに同じ種類の欠陥は無い」ことは確かめた。**

## 何に触っていないか

**コードを 1 行も直していない。**`app.py`・`intake/`・判定に手を入れていない。
**自動確定は 0 件のまま。**
