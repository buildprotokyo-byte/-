# K-41 10時間ループ 周8: 展開図の注記を機械が読む — **測る前の基準**

この文書は**測る前に**コミットする。

## 前

- 周 6 で、改装平面の**色つき**の注記が、機械に無い正解を 19 件当てた。
- 展開図(9〜16 ページ)の文字は**全部黒**で、色で工事を読み分ける周 6 のやり方は使えない(赤・青の行は 0 行、黒は 219 行。機械で数えた)。

## 変えるもの(1 つだけ)

**展開図(9〜16 ページ)の文字の行のうち、工事を表す語を含む行を、機械が「注記の行」にする。**

- 工事を表す語(測る前に固定する): 凡例 n の「新設・交換・脱着・移設・撤去」と、「取付・造作・貼・張・見切・下地・範囲・補修・塗装」。
- 「既存」で始まる行は除く(既存のままを表すため)。数字だけの行、`CH`・`FL` で始まる行、表題欄は除く。
- 場所はページ(展開図の室)。数量は付けない。本番の出力には入れない。

## 囮

同じ 9〜16 ページの黒い文字の行のうち、工事を表す語を**含まない**行(上と同じ除外のあと)。

## 採点

採点する役は、伏せた名前の 2 本と、機械の 67 行、周 6 の色つきの注記 66 行を受け取り、正解 93 件に結ぶ(周 1 と同じ決まり)。

## 合格の線

1. 展開図の注記の行が当てた正解のうち、**機械の 67 行も周 6 の注記も当てていないもの**が、囮より **3 件以上多い**。
   (周 6 の上乗せを測るので、周 6 の 5 件より低い線にする。測る前に決める。)
2. 展開図の注記の行のうち正解に当たった割合が、囮以上。

**不通過になる数字の例**: 展開図の注記が新しく 4 件、囮が 2 件なら差は 2 で 1 を満たさない。

## 守ること

自動確定は 0 件のまま。人の入力・合成の寸法は使わない。

## 結果

(測ったあとに足す)
