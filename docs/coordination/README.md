# コーディネーション記録(2026-09-22 おーちゃん8時間不在時の指示)

この文書は、おーちゃんが2026-09-22 03:03 JST頃にプロジェクトスレッドへ送った指示を、
要約・言い換えをせずそのまま書き起こしたものです。以降の作業はここに書かれたルール・
マージ可否・作業キュー・判断待ちの扱いに従います。

作業の進行状況は [status.md](./status.md) に記録します(status.md はこの記録係スレッドのみが更新します)。

## ルール

1. 記録を信じず、コードを読んでから着手する(「実装済み」と書かれていても、本番の経路から呼ばれているか確認する)
2. 修正は、再現する失敗テストを先に書いてから行う
3. 単一の指標だけを根拠に自動確定させる変更はしない
4. 実図面・見積明細・そこから作ったデータは、匿名化済みでもリポジトリにコミットしない。テストには合成データを使う
5. 設計に関わる作業は Opus 5、作業だけのスレッド(テスト実行、定型の修正)は Sonnet 5 を明示して起動する
6. 計測の待ち時間には、次の作業を進める。クラウドでの並列化は速くならないことがあるので、それを前提にしない
7. 各作業の報告は「スレッド名、使用したコミット、完了したこと、残っていること」の形で status.md に残す

## マージしてよいもの、してはいけないもの

マージしてよい(全件テストと CI が通り、その作業の完了条件を満たした場合)：

- 機能を追加するだけで、既存の判定のしかた(ファイアウォールの階層条件、許容差、群合計制約の判定、質問の選び方)を変えない変更

マージせず、PR のまま私の確認を待つもの：

- 既存の判定のしかたを変える変更(群合計制約の修正など)
- 判定の基準値(許容差、抜き取り率の既定値など)を変える変更
- 段階0.5の既定を A2 に戻すこと

絶対にしないこと：

- main への force push、ブランチの削除、テストを飛ばす・弱める変更、CI の無効化
- 実図面・見積明細のコミット

## 作業キュー(上から順に。止まったら次へ)

1. 本番の入口(最小構成)：すでに依頼済みの内容のとおり。PDF の入口、ページ種別判定、縮尺・面積・開き戸の接続、根拠付きの証拠として InferenceOrchestrator へ渡す、面積の選択は案件ごとに1回だけ質問して使い回す、合成ベクター PDF での通しテスト。完了条件：通しテストと全件テストと CI が通る → マージしてよい
2. 図面の文字を位置付きで読み、表として構造化する：建具表(建具番号、種別、幅、高さ、数量)と内装仕上表(部屋名、部位、仕上)。1の入口に証拠として接続する。テスト用の表入り PDF は合成で作る。完了条件：合成 PDF で表が正しく復元されるテスト、全件テスト、CI → マージしてよい
3. 抜き取り監査を階層1にも広げる：階層1と階層2の抜き取り率を別々に設定できるようにし、監査の記録に階層を残す。既定の抜き取り率の値は今の階層2の値を変えず、階層1の既定値は PR 本文で提案するだけにする。完了条件：既存と新規のテスト、CI → 既定値に触れない範囲ならマージしてよい
4. 群合計制約の修正(別スレッドで実装中)：実装が終わったら PR のまま待つ。効果の測定は私が戻ってから Codex 側で行う
5. 質問選定の高速化：設計案とテストまで(実装しない)。条件は、選ばれる質問の順番が今のエンジンと完全に同じであること、決定性を壊さないこと、暫定目標は1,000要素で1問あたり P95 5秒以内・全体10分以内。4と同じファイルを触る場合は、重なる箇所を明記する

キューが全部終わったら、新しい作業は始めず、status.md に「完了」と、次にやるべきことの提案を書いて待ってください。

## 判断待ちの扱い

私の判断が必要になったら、止まらずに次を行ってください。

- status.md の「判断待ち」の節に、何を決めてほしいか、選択肢、あなたの推奨、その理由を書く
- その作業は保留にして、キューの次の作業へ進む

## 追加ルール(2026-09-22 03:06 おーちゃんが追加)

追加の指示です。移動中に使用量の上限で作業が止まる可能性があるので、次を守ってください。

1. 同時に動かすスレッドは2本までにしてください。作業だけのスレッドは Sonnet 5 で動かしてください。
2. 大きな作業に入る前と、コミットした直後に、status.md を更新してください。「いま何をしていて、次に何をするか」が、status.md だけを読めば分かる状態を保ってください。
3. 止まった後に「status.md を読んで続きから再開してください」と送られたら、会話の履歴を読み返さず、status.md と README.md だけを読んで、キューの続きから再開してください。

## 作業キューの優先順位変更(2026-09-22 04:42 おーちゃんが追加)

作業キューの優先順位を変更します。

実図面(三井D)の分析で、すべての見積項目が最後に「数量化」と「見積の行への対応付け」を通ることが分かりました。
今ここが無いため、読み取りが進んでも見積の1行として出力できません。
一方、キラークエスチョンは、読み取りが候補を出すまで効き目が小さいことも分かりました。

### 新しい順番

1. 本番の入口(作業中のまま続ける)
2. 建具表・内装仕上表の構造化(変更なし)
3. 【新規】数量化と見積の行への対応付け
   - 表などから得た数量を、見積の行(取付費、材料費など)に当てはめる仕組み
   - 当てはめが一意に決まらない場合は候補として出し、確定しない
   - 当てはめの規則(会社の積算ルール)は、コードに埋め込まず、差し替えられる設定として持つ
   - 詳しい仕様は、Codex が一般的な形で作成中です。届いたら反映してください。それまでは入力と出力の形の設計を先に進めてください
   - テストは合成データで行う。機能の追加なので、全件テストと CI が通ればマージしてよい
4. 抜き取り監査を階層1へ(変更なし)
5. 群合計制約の修正(PR のまま待つ。変更なし)
6. 質問選定の高速化の設計案：いったん保留にしてください。途中まで進んでいれば、その時点の内容を status.md に残して止めてください

status.md の作業キューを、この順番に更新してください。

## 開発全体の最上位の原則(2026-09-22 05:45 おーちゃんが追加。今の作業より前に進める最優先)

開発全体の最上位の原則を定めます。今の作業より前に、次の文書を作ってください。

### 作るもの

docs/principles/start_kit.md を新しく作り、docs/coordination/README.md の冒頭から「すべての作業はこの原則に従う」としてリンクしてください。実図面や見積の中身は書かず、原則だけを書いてください。

### 1. 積算の絶対原則

- 工事内容とは、現況と計画の差分である。現況が分からなければ積算はできず、計画(何をしたいか)が分からなければ何を読むべきかが決まらない
- 見積書は提案書である

### 2. 意味を理解して動く

- 読み取った値には、必ず意味を付ける：それは何か、どこのものか、現況か計画か、目的にどう関係するか
- 意味が付けられない値は、数量に使わない

### 3. スタートキット(人が最初に与える手がかり)

3つの要素は、人が与えられる確かさがそれぞれ違う。人の入力が悪いと全体が悪くなる設計にしてはならない。

#### 3-1. 基準(人が正確に与えられる唯一の要素)

- 図面に書かれた縮尺の表記は当てにしない。図面そのものがスキャンや縮小で歪んでいる可能性がある
- その画像に対して物差しを当てる：人が画像を拡大し、区切りのいい寸法(1m、3m、10m など)の両端をクリックし、その実際の長さを入力する
- 縦方向と横方向を別々に持てるようにし、さらに画像を区画に分けて、区画ごとに基準を持てる形にする(局所的な歪みへの対応)
- 人のクリックや入力も間違う前提とする。図面に書かれた寸法の数字と突き合わせて検算し、合わなければ警告する

#### 3-2. 目的(方向性と資料の指し示しだけ。二段階で確立する)

- スタートキットの段階では、人も詳細は分からない。人に詳細を設定させると自動積算の意味を失うので、詳細は求めない
- 始めに人がするのは次の2つだけ
  - 自由記述で方向性を書く(例：戸建ての水回りリフォーム)
  - やりたいことが最も濃く書かれている資料を選ぶ(基本仕様書、イメージパース、要望書など)
- 方向性の記述は弱い手がかりとしてだけ扱う。図面の中身と食い違った場合は、そのこと自体を人に聞き返す
- 詳細な目的は、AI が図面を読んだ後に組み立て、AI の理解を要約して人が確認する

#### 3-3. 現況(正確さを求めず、把握の状態を申告する)

- 人に正確な現況を用意させるルールにしてはならない
- 人は、現況をどの程度把握しているかを選ぶ
  - 明確に分かる
  - おおむね分かるが確実ではない
  - 全く分からない、または図面で表現されていない
  - 部分によって混在する(この場合は範囲ごとに申告する)
- 分からない部分は、AI が図面から推論するか、仮説を置いて進む

### 4. 人が先に入力してよいものの条件

次の3つをすべて満たすものだけを、AI が読む前に人に入力させる。

1. 人がすぐに、確実に分かる
2. AI では確実に推定できない
3. 図面の読み方を縛らない

現時点でこれを満たすのは基準点だけである。それ以外は、AI が先に図面を読み、方向を見失いそうな大きな分かれ目(ゲート)を抽出し、そこだけを人に聞く。人は候補から選ぶ、または確認する形で答える。

### 5. 数量の出し方

- 前提が揃うまで止めるのではなく、何に基づくかを区別して出す：確定/推論に基づく/仮説に基づく/現地確認が必要
- 仮説に基づく行は、どの仮説に依存しているかを記録し、仮説が変わったら連動して見直す
- 人が与えた情報も、図面の中身と突き合わせて矛盾を探す。人の入力を無条件の正解にせず、単一の根拠では自動確定させない
- 人の負担は、質問の数ではなく、操作の回数と時間で測る

### 6. 未決事項と決め方

人が先か AI が先かの順番は、まだ決めていない。次の3方式を切り替えられる作りにし、同じ図面で比べて決める。

- A 方式：人が方向性と資料を先にセットする
- B 方式：AI が先に読み、ゲートを抽出してから人に聞く
- 混合方式(当面の仮の方式)：基準点だけ人が先、残りは AI が先

比べる指標：確定できた行と金額、誤って確定した行、人の操作回数と時間、わざと誤った方向性を与えたときにシステムが食い違いに気づけるか。この比較の計測は、読み取りの流れが動くようになってから Codex 側で行う。今は実装しない。

### あわせて行うこと

- 今のコードと、キュー3(数量化と見積の行への対応付け)の設計を、この原則と照らし合わせ、原則に反している所を一覧にして status.md に書いてください。例：意味の付いていない値を使っている、縮尺の表記に依存している、人の入力を無条件に正解としている、確定と仮説の区別がない、順番が切り替えられない
- キュー3の設計は、この原則に合わせて見直してください。特に、読み取った数量/案件の前提/会社のルールの3層に加えて、各値の「何に基づくか」(確定・推論・仮説・現地確認)を持てるようにしてください
- 判定のしかたは変えないでください。コードを直すのは、一覧をおーちゃんが確認してからです
- この文書はプルリクエストにして、マージせずに待機してください。おーちゃんが中身を確認してからマージします
