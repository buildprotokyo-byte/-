# キュー5「質問選定の高速化」作業メモ（中断時点）

2026-09-22。設計案はまだ書いていない。順番待ちで中断したため、再開時の出発点として
「コードを読んで分かったこと」と「実測した数字」だけを残す。

## 使ったコミット

main の `7d21245`（`71420fd` / `ad01a24` / `7d21245` を含む）。ブランチ
`claude/killer-question-speedup-design`。コードは一切変えていない。

## 実測（このリポジトリ、`benchmarks/profile_killer_question.py`）

鎖状につながった N 個の要素（各レンジ幅3）で、`next_question()` を1回呼ぶのにかかった時間と
z3 の呼び出し回数。

| N | solve() 1回 | next_question() 1回 | solver.check | Optimize.check | Optimize.add |
|---:|---:|---:|---:|---:|---:|
| 4 | 0.025s | 0.19s | 17 | 136 | 1,080 |
| 8 | 0.031s | 0.92s | 33 | 528 | 8,432 |
| 16 | 0.086s | 5.68s | 65 | 2,080 | 66,528 |
| 24 | 0.181s | 16.94s | 97 | 4,656 | 223,440 |
| 32 | 0.275s | 37.66s | 129 | 8,256 | 528,320 |

**N を2倍にすると時間は約6〜7倍**。`Optimize.add` の回数は N の3乗で伸びている。

## 時間の使い道（コードを読んで特定した構造）

1. `ConsistencySolver.solve()` は、変数1個ごとに `_tighten()` を呼び、その中で
   **下限用と上限用に `z3.Optimize` を2つ新規に組み立て直す**（`consistency_solver.py`
   の `_tighten` / `_optimize`）。組み立てのたびに全制約を `opt.add` する。
   → solve() 1回あたり `Optimize` を 2N 個、`opt.add` を 2N×M 回（M は制約数）。
2. `KillerQuestionEngine.score_candidate()` は、**候補値1つにつき
   `solver.clone().solve()` を1回**呼ぶ（最大 256 候補）。
3. `next_question()` は、**未確定の変数すべてについて** `score_candidate()` を呼ぶ。

掛け合わせると、質問1問あたり `Optimize` の組み立てが
**（未確定数）×（候補数）×2×（変数数）** 回。1,000要素では現実的な時間に収まらない。

なお `score_candidate()` は `graph.connected_component(variable)` で下流を取っているのに、
**仮確定して解くときは毎回グラフ全体を解き直している**。まとまり（連結成分）の外の変数は
仮確定の影響を受けないので、ここが最初に削れるところ。

## Codex 側の試作（おーちゃん経由、2026-09-22 に受領。リポジトリのコードは未変更）

- 候補の評価を `dependency_graph` の components に限定し、まとまりごとの solver を
  質問ごとに1回作って使い回す試作で、**7種類の構造（独立、橋渡し、合計、鎖、星、
  幅の広い定義域、混合）すべてで、今のエンジンと質問の列・スコア・最終状態が完全一致した。**
- 時間: 30要素 14秒→4.2秒、50要素 80秒→11秒、100要素 打ち切り→42秒で全解決。
  **300・1,000要素はまだ180秒で打ち切り**（1,000要素は最初の1問に127秒）。
- 残りの時間の **62〜74% は範囲の計算（`_tighten` / `_optimize`）**、Z3 コンテキストの
  作成は 7〜17%。
- **まとまり間で評価結果を質問間に使い回す案は未検証。**

この数字は上の実測と同じ向きを指している（時間の主因は範囲計算の組み直し）。

## 再開時にやること

1. 設計案を3段階（①まとまり限定 ②まとまり間の評価結果の使い回し ③範囲計算の軽量化）の順で、
   効果とリスクを書く。中心は「選ばれる質問の順番が完全に同じ」「決定性を壊さない」の担保。
2. 今のエンジンと候補実装で質問の列が完全一致することを突き合わせるテストを書く。
3. 目標（1,000要素、1問 P95 5秒、全体10分）に対する現在値と見込みの見積もり。
   実測できないものは「見積もり」と明記する。

## 注意（既知）

- 決定性は Z3 の共有コンテキストで一度壊れている（`03b8eac` で毎回 `z3.Context()` を
  作って解消）。まとまりごとに solver を使い回す案は、この「毎回新しいコンテキスト」と
  真正面からぶつかるので、設計案で必ず扱うこと。
- 別スレッドが `claude/group-total-masking`（main 未マージ、`c3d3053...`）で
  `arbitration/group_total.py` の新設と `ConsistencySolver.Variable.detection_range` の
  追加をしている。質問選定は同じあたりを触るので、重なる箇所を報告に明記すること。
