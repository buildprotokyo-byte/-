# 作業状況(おーちゃん不在時、8時間の自走)

ルール・マージ可否・作業キューの原文は [README.md](./README.md) を参照。
このファイルは記録係スレッドのみが更新します(2026-09-23 から、「いまの状態」の節は判断待ちの正本として K-07 以降を受けているスレッドが更新する。K-12 7 番)。

**再開時の読み方:** 会話の履歴を読み返さず、README.md とこのファイルだけを読んでください。
**いまの状態と判断待ちの正本は、すぐ下の「いまの状態」の節です**(2026-09-23 夜、K-11 6 番・K-12 7 番)。

## いまの状態(2026-09-24 未明。ここが最新)

> おーちゃんの K-11 6 番(2026-09-23 23:37)「status.md が9/22から止まっています。今の状態に更新してください」と、
> K-12 7 番(23:48)「共有フォルダ側の一覧を status.md に移して、以後はそちらを正本にしてください」を受けて、
> K-07 以降を受けているスレッドが書いた(記録係のスレッドは 9/22 10:11 から動いていない)。
>
> - **判断待ちの一覧の正本は、この節である。**共有フォルダの `reports/判断待ち一覧.md` は 2026-09-23 23:55 までの古い写しで、先頭に「正本は status.md に移した」と書いてある。以後は更新しない。
> - 判断待ちが増えた・片づいたときは、このファイルを PR で直す(文書だけなのでマージしてよい)。
> - **この節より下は 2026-09-22 10:11 までの記録で、書き換えていない。**下の「判断待ち」1〜39 のうち、どれがその後の回答(K-02〜K-12)で片づいたかは 1 件ずつ照合していない(辿れていない)。

### 本線とテスト

- この節を書いた時点の本線: `af9556e`(#121 まで)
- 全件テスト: **1,489 件通過**(本線 `8be812e` を手元で流した数)。そのあと入った #106 の枝では 1,497 件通過・1 件スキップ(PR の本文。本線 `af9556e` そのものは手元で流していない)。PR ごとに CI でも全件が走る。

### どこを読めばよいか

| 知りたいこと | 場所 |
|---|---|
| いま全体がどこに立っているか(作ったものと効いたものの区別、本番に繋がっていないもの) | [`docs/reports/2026-09-23_K-09_全体まとめ.md`](../reports/2026-09-23_K-09_全体まとめ.md)(K-09、#112) |
| **仮の判断**(1 案件でしか確かめていない決め方)の一覧 | [`docs/provisional_decisions.md`](../provisional_decisions.md) |
| おーちゃんの決定 | `docs/decision_k03.md`(K-03)、`docs/decision_area_basis.md` ほか `docs/decision_*.md`。K-04 以降の回答の全文は、プロジェクトの共有フォルダの `reports/K-0x_回答*.md`(実案件の話を含むのでリポジトリには入れていない) |
| 取り決め(ルール・マージしてよいもの) | [README.md](./README.md) |

### 判断待ち(正本)

**PR(マージせず待っているもの)**

| PR | 何か | 決め方 |
|---|---|---|
| #100 | ページの申告に頼らず、室の輪郭と記号を全ページで探す(判定に触る) | 実図面での前後の測定(測定担当 Codex-A)の結果を見て決める。手順書は共有フォルダの `reports/PR100_P011測定_手順書.md` |
| #118 | 「会社のルールの行」を「図面に現れない行」に改名(K-17 1 番、コード)。行に付く文言 2 つと説明の文だけが変わる | 判定の言葉に触るので、おーちゃんの返事を待つ。**おーちゃんが戻ってから決める**(2026-09-24 00:49「全部推奨で」の 6 番)。CI 通過。変える前と後で合成の見本 3 つの出力を全部の欄で突き合わせ、違いは文言 2 つだけ。全件テストは前後とも 1,489 件 |
| #139 | 図面枠を表と誤認していた件の直し(K-29)。罫線の表を探す部品が外枠と表題欄を 1 つの表として返していたため、寸法の読みが 34 ページ全部で 0 件だった | **判定に触る**(枠と表を分ける割合 60%/25% を入れた)のでこのまま待つ。CI 通過。仮の判断として `docs/provisional_decisions.md` 6 節に出してある |
| #141 | K-30 の測定 3 本(ページの構造・線の太さ・読む順番)と、その基準・報告 | 測っただけで実装は変えていないが、**先に決めた線に 2 本届かなかった**ので、どうするかをおーちゃんに聞いている。CI 通過 |
| #146 | 面が多いページの室名に「弱い」印を付ける(囮を混ぜて数えたら、線の多いページでは本物 28 件に対し囮 23 件が通った) | **下書き。**どの輪郭を室と見なすかの窓は変えていないので拾える件数は 0 件も変わらないが、**判定の材料が増える**のでこのまま待つ。全件テスト 1,706 件通過。300 個という線はデータ点が 2 つしかないので `docs/provisional_decisions.md` 7 節に仮の判断として出してある |
| #148 | 周6〜周10・周2・周3 の測定と報告。**冊子の中に、取り出せる文字が 0 件のページが 4 枚**あった(3 枚は文字が線で描かれた製品の図、1 枚はスキャン)。その 3 枚を 5 通りの読み方で読み、知識の数を数え、ページの種類を文字を読まずに見分けた | 測っただけで本番の経路には繋いでいない。**7 周のうち線を全部通ったものは 0 周。**3 枚の読み取りは **5 通り当てて最初のやり方(4/8)を超えられず頭打ちで止めた**。**囮の線の決め方が悪かった周が 2 つある**(偏りを差し引いていない・囮が最初から当たらない相手だった)ので、報告にそう書いて採っていない。全件テスト 1,752 件通過 |
| #150 | 周4。縮尺の基準を**誰が入れたか**を記録する。人が入れた値は `entered_by`、機械が出した比は `set_by` で**欄の名前を分けた**。誰が入れたかが空の基準点は断る | **下書き。**記録を足すだけで、**判定の出力は 1 か所も変わらないことを見本 3 つで示した**(全件テスト 1,756 件通過)。ただし**入力を断る向きの変更**で、いままで通っていた前提が通らなくなる(この案件ではスタートキットが空なので今は誰にも当たらない)。`calibrated` にも手法の一覧にも触っていないので自動確定は 0 件のまま |
| #152 | 周5(空間)。天井高の注記 71 個に**行き先があるか**を数えた。**断面図も矩計図も 0 枚**(おーちゃんの決まりにそのまま当たる)。ただし天井高は `CH=` として 5 ページ 71 か所に印字されているので「高さの情報が無い」とは書いていない | 測っただけで本番の経路には繋いでいない。**先に決めた線 3 本に 3 本とも届かなかった**(行き先が 1 つに決まったのは 71 個のうち **1 個**で、**でたらめな点を撒いた囮の 8 分の 1**)のでどうするかを聞いている。**5 ページとも注記は「2 番目に大きい面」に落ちており、取れている面は室ではなく間取り全体が 1 つになった面。**全件テスト 1,716 件通過 |
| #154 | 周11。**囲いが割れない原因は開口の幅か**を測った。つなぐ幅を 50mm から 3,000mm まで 60 倍に動かしても、天井高の行き先が決まる注記は **1 件から動かない**。`MAX_GAP_MM` というつまみは**この図面ではほとんど何も決めていなかった**(幅を 60 倍にして仮の辺は 3% しか増えない) | 測っただけで実装の既定値は変えていない。**先に決めた線 6 本に 6 本とも届かなかった**のと、**こちらの「嘘の壁」の数え方に欠陥が出た**(11,917 本と出したが 98.7% は 100mm 未満で、壁の中の細い途切れを繕った線。繕いは嘘ではない。嘘になりうるのは多くても 34 本)ので、どうするかを聞いている。全件テスト 1,722 件通過 |
| #156 | 周12。**面積の窓(0.5〜200㎡)が原因か**を測った。上限を 200㎡ から 15㎡ まで下げると、天井高の行き先は**増えるどころか 0 件**になる。**上限 15㎡ のとき 5 ページ中 3 ページで注記が 1 つも面に入らない**(面は 6〜11 個残っているが、そこは天井高が書かれている場所ではない)| **#152 の上に積んである**(周5 の採点を使うため)。測っただけで既定値は変えていない。**線2 が不通過**で、**室は窓の外にあるのではなく面として存在していない**と分かった。周11(閉じ方)と周12(選び方)の両方で出てこないので、**調整つまみの届かない所に原因がある。**全件テスト 1,723 件通過 |
| #157 | 周13。**まだ振っていなかった下限**(幅 400mm・面積 0.5㎡)を振った。幅の条件を外すと 5 ページで 130 個の面が現れるが、**それは室ではない**(全部 400mm 未満。実装自身が「室や廊下は 600mm 以上」と書いている)| **#152 の上に積んである。採らない。****一度「室が落とされていた」と書いたが誤りで、訂正済み**(報告の冒頭とPR本文)。幅は実寸ミリで合っていた(コードで確認)。**行き先が 1→19 に増えたのは室が見つかったからではなく、小さい面を足して注記の落ちる先ができただけ**——囮が先に言っていた(値そのもので 300mm は 9 対 10、200mm は 14 対 14)。**囮と比べるときは値そのもので比べる。**全件テスト 1,722 件通過 |
| #160 | 周14。**取れている面が間取りのどこか**を室名から数えた。**室の幅(600mm 以上)の面 43 個のうち、室名が 1 つ入っているのは 4 個だけ**で、同じ文字をばらばらに置き直した囮は 3 個。**差は 1 個。**天井高のほうは**室の幅の面を避けて落ちている**(本物 6 面に対し囮 14 面)| **#152 の上に積んである。**測っただけで判定には触っていない。**見立て(取れているのは非居室では)は確かめられなかった。**線3 の条件を**どんな図面でも必ず成り立つ言い換え**で書いてしまい判定できない(**周13 と同じ失敗を形を変えて繰り返した**。直しは次に測る前に入れる)。**「43 個は室ではない」とは書いていない**——室名は輪郭の外に置かれることがあるため。全件テスト 1,727 件通過 |
| #162 | 周15。**室名が面の外にもあるか**を数えた(周14 で内側に無いと出たが、室名は輪郭の外に置かれることがあるため)。**いちばん狭い帯 500mm で本物 14 個・囮 14 個とぴたり同じ。**帯を広げると囮のほうが多くなる(1,000mm で −2、2,000mm で −8)| **#160 の上に積んである(#160 は #152 の上)。**測っただけで判定には触っていない。**3 本とも不通過。**500mm の帯で室名が当たった 14 個のうち**10 個は帯の中に室名が 2 つ以上**——**面は 1 つの室の輪郭になっていない。****ここで頭打ちと見て止めた**(決まり④)。室という 1 つの対象に技術を 5 通り当てたが、**5 通りとも出典はこの PDF の線の図形データ 1 つ。変えていなかったのは出典のほう。**次は出典を変える案(仕上表から室を名前で取る)を書いたが**測っていないので根拠にしない**。全件テスト 1,736 件通過 |
| #164 | 周16。**出典を変えた**(線ではなく内装仕上表の文字から室を取る)。**この 10 周ではじめて、先に決めた線が 3 本とも通った。**仕上表は 2 ページ、室名の行 59 行、並んでいる室は重複なしで **10 個**。うち 9 個が語に当たり(囮 1 個)、7 個は平面図にも印字されている(囮 0 個)| **#162 の上に積んである(#162→#160→#152)。**測っただけで判定には触っていない。**取り違えないこと: 取れたのは「どの室があるか」の一覧で、面積ではない。**仕上表の室は 10 個、平面図から取れている面は 43 個、**そのどれがどれかはまだ 1 件も分かっていない。足りないのは室の一覧ではなく「室と面の対応」。****同じ 1 つの PDF なので独立した 2 つ目の根拠にもならない**(K-08)。全件テスト 1,746 件通過 |
| #166 | 周17。**室と面の対応**を数えた(探す語を、こちらの語の表から**図面自身が仕上表に並べた 10 個**に変えた)。**3 本とも不通過で、3 本とも囮が上**(21 対 23、**0 対 9**、12 対 18)| **#164 の上に積んである(#164→#162→#160→#152)。**測っただけで判定には触っていない。**いちばんお伝えしたいこと: 線3 は本物 12 個で先に決めた合格の数(3 個以上)を超えていた。囮が 18 個だったので不通過になった。囮が無ければ「12 か所で面が 1 つに決まった。対応が付き始めた」と報告していた。**予想も外れた——**のべ 41 か所のうち 20 か所はどの面にも届かず、届くときはほぼ 1 つの面に届く**(いくつもに届いているのだろうという予想と逆)。1 つに決まる面が 0 個なのは**別々の室の名前が同じ面に集まっている**から。全件テスト 1,755 件通過 |
| #168 | 周18。**室名が集まっている面はどれか**を数えた。**中身は守りに置いた線3 のほうにあった**——その面は**辺が 164〜2,093 本**あり、**24〜28% はこちらが仮に閉じた辺**。室の輪郭は普通 4〜12 本で足りる | **#166 の上に積んである(#166→#164→#162→#160→#152)。**測っただけで判定には触っていない。**線1・線2 は標本が足りず答えになっていない**(5 ページ中 2 ページは面が同数で 1 つに決まらず、使えたのは 3 ページ。本物 1 対囮 1、本物 2 対囮 2)。**「詰まりは 2 か所ある」とは書いていない。****周11 の結論に但し書きを付けた**——周11 が数えたのは本数と行き先で、**既定の 1,200mm がいま現に室と室を繋いでいないかは測っていない。**ただし「繕いをやめれば室になる」とも書いていない。全件テスト 1,764 件通過 |
| #170 | 周19。**辺の数(形)で室かどうかを分けられるか**を数えた。**分けられない。**ただし**面が二極化している**と初めて分かった——43 個のうち**辺 4〜12 本が 17 個、13〜20 本が 0 個、51 本以上が 24 個**。そして**その 17 個に天井高は 1 件も入らない**(囮は 4 件)| **#168 の上に積んである。**測っただけで判定には触っていない。**`find_room_outlines` に辺の数の条件は入れていない。****書いていないこと**: 「室らしい面が 17 個見つかった」(図面枠も凡例も辺は少ない)、「絞ったら室名が決まるようになった」(線3 は本物 4・囮 4 で**差 0**。周13 で踏んだ形そのもの)。**大きさ・形の目盛りはこれで 4 つとも外れた。**全件テスト 1,771 件通過 |
| #172 | 周20。**きれいな 17 個が何なのか**を読んだ。**同じ大きさの繰り返しだった**——面積は 1.81〜2.88 ㎡に詰まり、**値の種類は 7 つだけ。2.56 ㎡が 5 ページすべてに 1 個ずつ、1.81 ㎡が 1 ページに 5 個。**室の大きさが偶然そろうことはない | **#170 の上に積んである。**測っただけで判定には触っていない。**線2 で直す先が変わりうる発見**: 17 個のうち**12 個が辺 51 本以上の面の内側にまるごと入っている**。化け物の面は「室の取り違え」ではなく**「中に図形を抱えた袋」**。ただし「袋をほどけば室になる」とは書いていない。線3 は不通過で天井高はむしろ**遠い**(5,760mm 対 囮 4,879mm)。**線1 は「見立てどおり」と出たが、ばらつきを見たら見立てが的外れだった——囮の無い線が 1 本で結論にできない実例。****面の絞り込みはここで止める。次は出典を変える(寸法線と通り芯)。**全件テスト 1,779 件通過 |
| #174 | 周21。出典を変えて**寸法線と結び付いた数字**から芯々の大きさを組めるかを測った。**報告に訂正が入っている(周22)**——「平面図の 87〜90% が表とみなされ数字 205 個が落ちている」という欠陥は、**2026-09-24 の PR #139 が既に直していた。周21 は直っていない枝の上で測っていた。****周21 の「寸法 0 件」は図面の性質ではなく、測った枝の性質である** | **#172 の上に積んである。**測っただけで判定には触っていない。**持ち越す落とし穴**: 「実装済み」の記録を信じないだけでは足りず、**自分が測っている枝にその直しが入っているかを確かめる必要がある**(周21 は `pdf_dimensions.py` を読んだが、読んだのは本線の版だった)。**着手前にコードを読んで、この問いには 2026-09-23 の答えが既にあると分かった**(`wall_network.py` 冒頭)。**周11〜周20 はそれを別の道から確かめ直していたことになる。記憶に無く、残っていたのはコードの説明文だった。**全件テスト 1,787 件通過 |
| #176 | 周22。**#139 の上で周21 を測り直した(数え方は 1 文字も変えていない)。平面図 5 ページの寸法は 0 件 → 79 件**、連なりも 0/0 → 縦 6・横 5。落とした数字のうち「表の升目の中」は 205 個 → 9 個。**新しい問い(寸法の連なりで紙を格子に割ると室が 1 つずつ入るか)は不通過**——本物 2 個・囮 4 個 | **#174 の上に積んである。この枝は #139 の変更を含む**(測るために合流させただけで、**#139 を採るかどうかの判断とは別**)。測っただけで判定には触っていない。**不通過の中身**: 格子が室の境目に来ていないのではなく、**格子が紙の 5.5% しか割っていない**(5 ページとも縦の格子線が 2 本だけ)。**印字された室名 41 件のうち 33 件が格子の外。**線2 で通った「横の連なり」も、中身は**5 ページとも同じ x 区間に重なって置かれた寸法 2 本**で、建物の横方向に進む連なりではなかった(**先に決めた線は動かさず、読み方のほうを直した**)。**囮は測る前に差し替えた**——最初の囮は格子線が 1 本もできず、どんな図面でも 0 を返す囮だった。**79 件は周22 の成果ではなく #139 の成果。**全件テスト 1,886 件通過 |
| #177 | 周23。**周22 が書き残した予想を測ったら、こちらの予想が外れた。**横の寸法線にも**区間は複数ある**(横の読み 29 件のうち 22 件が、同じ寸法線に相手を持つ)。**連ならない理由は、同じ線に乗った読みどうしが隣り合っていないこと** | **#176 の上に積んである。**`pdf_dimensions` の定数は 1 つも動かしていない。**いちばん効いた数字**: 同じ寸法線に乗る組のうち、**縦は 79 組すべてが隣り合う(間が空いた組は 0)のに対し、横は 31 組のうち 26 組で間が空いている。****切れているのは寸法線ではなく、読めなかった数字のほう。****ただし「読めなかった数字を読めば連なる」とは書いていない**——空いた区間に落とし物が実際にあるかは数えていない(次の周)。全件テスト 1,897 件通過 |
| #179 | 周24。**周23 が残した問い(「間が空いているのは、単に何も書かれていない区間かもしれない」)に答えを出した。答えはいいえ。****空き 26 個のどれにも、読めなかった数字が入っている(囮は 0 個)** | **#177 の上に積んである。**`pdf_dimensions` の定数は 1 つも動かしていない。**ただし「読めなかった数字が 1 つあって切れている」という形ではない**——ちょうど 1 個だけ入った空きは 26 個中 11 個で、空きは広く区間が複数並んでいる。**先に書いた予想はまた外れた**: 最多の理由は「対応する寸法線が見つからない」ではなく**「寸法線の候補が複数あり、どれを指しているか決まらない」(重複を除いて 25 件)**。これは `pdf_dimensions` の `_pick_span()` が「長さの違う候補が残ったらどちらも選ばない」作りにしている所。**ただし 25 件のうち正しい候補が選べるものがいくつあるかは数えていないので、直す根拠にはしていない。**全件テスト 1,908 件通過 |
| #181 | 周25。**先に決めた対照が落ちたので、本題の数字は結論にしなかった。****そして落ちたこと自体がいちばん重い数字**——**読めた 79 件のうち 28 件が、そのページの縮尺と 1% 超で食い違う(合うのは 51 件・64.6%)** | **#179 の上に積んである。**`pdf_dimensions` の定数も `_pick_span` も動かしていない。**#139 の「±0.1% で一致」は正しい**が、それは**互いに一致した読みの組の中の話**で、実装は一致しない読みを外れとして残す作り。**「79 件は読めたであって正しいではない」に具体的な数が付いた。****こちらの落ち度も記録した**: 対照の 9 割という線は実装の作りに対して高すぎた(周13・周14 に続く「線の立て方の失敗」3 度目。ただし前 2 回は不通過になりようがない線で、今回は厳しすぎた線)。**結果を見てから線は下げていない。**全件テスト 1,919 件通過 |
| #184 | 周27。**周26 の札の推しの案(面積が要る行を先に洗う)を進めた。**一本通した **67 行のうち数量が入っているのは 5 行だけ**(全部「箇所」)。**㎡ の 46 行と m の 16 行は 0 件。****待っているのは「人が 10 室の縦・横を入れること」だった** | **#181 の上に積んである。`app.py`・`intake/` に手を入れず、走らせて数えただけ。****#139 は入口までは届いている**(入口の数量 2,726→2,887、+161)**が、行の側は 1 つも変わらない。**コードで分けた: `app.py`→`drawing_intake`→`read_dimensions` は繋がっているが、㎡ の行に数量を入れる `_finish_lines()` が見るのは**人が入れた室の寸法だけ**で、**図面から読んだ寸法が室の面積になる道は 1 本も無い。****人の入力待ち 62 行が指す場所は 10 か所**(仕上表の室名 10 個と同じ)。**入れても自動確定にはならない**(`from_room_dimensions` は `calibrated=False`/上限 `weak`、説明文に裏返しの危険が書いてある)。全件テスト 1,928 件通過 |
| #186 | 周28。**合成の寸法を 10 室ぶん渡して一本通した。数量が入った行は 5 → 51。自動確定は 0 件のまま(裏返しの危険は実物で起きなかった)。****ただし単位 m の 16 行(巾木)は 1 件も入らず、原因は人の入力と関係なかった** | **#184 の上に積んである。`app.py` に手を入れず、走らせて数えただけ。****原因は名前の食い違い**: `app.py` は巾木の数量を **`周長`** で探すが、`estimating/from_room_dimensions.py` が作るのは **`室の周長`**。**引き当ては永久に外れ、人が何を入れても入らない。**床・壁・天井の名前は合っていて、巾木だけ。**作る側は 3 種類とも作れているので `gaps` は空**——**「作れなかった理由を残す」仕掛けでは原理的に拾えない種類の欠陥。****K-29 に従って直さず、`tests/test_finish_part_quantity_names.py` の 4 件で固定した**(直したら落ちる試験。落ちたら消してよいと書いてある)。**直すかどうかは判断待ち。**線1 は不通過(46/62、合格 50 以上)で、**結果を見てから線は下げていない。**全件テスト 1,932 件通過 |
| #188 | 周29。**名前で引き当てている場所を数えた。数量の種類の食い違いは巾木の 1 件だけ**(予想どおり)。部位の取りこぼしも 0 行(予想どおり)。**ただし線3 で予想が外れた——室名の突き合わせは空白も改行も全部捨てている** | **#186 の上に積んである。`app.py` に触っていない。**`_nfkc()` は `"".join(NFKC(text).split())` なので、**中の空白も改行も落ちる。****この図面では衝突 0 件で実害は出ていない**(10 室を数えた)。**効くのは人が打つ側**: **改行を含む室名が 1 つあり、区切り文字を入れて打つと引き当たらない。**→ **人に室の寸法を入れてもらう形にするなら、室名は打たせず一覧から選ばせる必要がある**(周26 の札の②に効く)。試験 7 件で固定(実案件の室名は書いていない)。**数えたのは `app.py` の 3 か所だけ**で、`estimating/`・`arbitration/` の中どうしは数えていない。全件テスト 1,939 件通過 |

| #190 | 周30。**周28 の巾木の 1 件と同じ種類の欠陥がほかにあるかを数えた。****答えは「ほかに無い」。****ただし予想が大きく外れ、そこがこの周の値打ち**——機械が出した「惜しい組」は **490 件**で(予想 1 件)、**本物は 1 件だけ**だった | **#188 の上に積んである。コードは 1 行も直していない。**構文木で読んだ(正規表現ではないので説明文は入らない)。`app.py`・`axes/`・`estimating/`・`intake/`・`arbitration/` から**定数 179 個・文字列 3,779 個**を棚卸し。**外れが 99.8% なので、この数え方は「見張り」には使えない**——毎回 490 件を人が見て、489 件では何も起きていない。**鳴りっぱなしの見張りは無いのと同じ。****使えるのは「1 件見つかったあとで、ほかにもあるかを一度だけ確かめる」使い方で、それは今回やった。**490 件は手で見るには多すぎたので、あとから「モジュール直下の入れ物の中」に絞る診断の欄を足して **52 件**まで狭め、**52 件は 1 件ずつ手で見た。線1 の件数(490)は動かしていない。**52 → 1 の振り分けはこちらの判断。対照(線3)は通過——既に分かっている巾木の 1 件を拾った。**書き写し(定数があるのに別ファイルで同じ文字列を直接書いている場所)は 115 件**だが、**「減らすべきだ」とは書かない**(減らす価値は測っていない)。**「食い違いは無い」とも書かない**——定数に置かれていない名前どうしは、この数え方では拾えない。数値の表は共有フォルダ `reports/周30_定数と文字列の突き合わせ.json`。**周28 の巾木を直すかどうかは判断待ちのまま(#186)。**全件テスト 1,947 件通過 |
| #192 | 周31。**測る前のコード読みでいちばん重いことが出た: 周27 の「67 行」は規則ファイル**無し**で出た行だった。**`app.py` 547 行目は、規則ファイルが無いと**入口の数量を 1 件も行に当てはめず丸ごと捨てる**(内部の記録に 1 行書くだけ)。**捨てられている 2,887 件の対象は 6 種類しかない**(予想 50 以下、桁を 1 つ読み違えた) | **#190 の上に積んである。コードは 1 行も直していない。****周27〜30 が追っていた「面積待ちの 62 行」と、この 2,887 件は別の話だった。**「#139 は入口まで届くのに行が 1 つも変わらない」の理由もここで変わる——**繋がっていないのではなく、規則が無いので当てはめの段が丸ごと飛んでいる。**規則は対象の種類に当てるので、**6 は書かねばならない規則の本数の下限**(上位 3 種類で 99.8%)。**ただし「6 本で足りる」とは書かない**——あとから足した参考の欄のほうが重い: **2,887 件のうち現況か計画かが決まっているものは 1 件も無い**(不明 164 件、意味そのものが無いもの 2,723 件)。**「規則が書けない」とは書かない**(条件にしなければ書けるし当たる)。正しくは**「いま規則を書くと、撤去する物と新しく付ける物を区別しない規則しか書けない」**。**線3 は不通過で、線の立て方がこちらの誤り**——種類 6 に対して「上位 10 を選ぶ」と書いたので本物も囮も全部を選び、差が出ようがなかった。**線は下げていない。****線の立て方の誤りはこれで 4 種類目**(周13・14 落ちようが無い線／周25 厳しすぎる線／周22 効きようが無い囮／周31 母数より大きい選び方)。線4(安全確認)通過——**架空の規則を渡しても自動確定は 0 件のまま。**数値の表は共有フォルダ `reports/周31_規則を何本書けば行になるか.json`。全件テスト 1,956 件通過 |
**PR 以外**

| 何か | 状態 |
|---|---|
| マスタープロンプト一式の 00(統合版)と 02(理解AI)が未着(K-32) | おーちゃんが貼るのを待っている。届いたら `docs/prompts/` に置き、K-31 の直し(`docs/prompts/K31_適用待ち.md`)を当てて PR にする。残りの 6 つと索引は #144 で本線に入れた。**一式の直しが終わるまで、ほかの作業は止めている** |
| 知識の候補 28 件の採否 | 採否を決める順番の表は K-19 3 番の回答で出た(先頭は A-2 壁の高さは天井高を使う、C-2 耐火間仕切りか片面だけか)。**おーちゃんが採否を付ける。**K-20 の 10 時間は触らない |
| P011 の凡例 2 枚(6 枚目・22 枚目)が照合に使えていなかった件の調べ(K-12 1 番) | 検証ループのスレッドで進行中。調べるだけで、新しい実装はしない |
| **おーちゃんが戻ってからの宿題(「全体のまとめ(K-09)」のスレッドから)**: ①「組」「台」の単位を受け取る変更を入れるか(判定に触る。一言でよい)、あわせて「枚」の 3 行も入れるか ②B-2 を知識の表から外す(K-18 4 番で決まった、1 行の変更) ③K-17 の改名を知識の側の 3 か所(`docs/knowledge/coverage_matrix.md` の「会社の諸経費」の行、`candidates.md` の A 節の見出し、`knowledge_candidates_encoded.json` の説明文)にも入れる | K-20 を受けてそのスレッドは止まっている。3 つとも**未着手**で、案と数字は 00:45 の回答にある |
| 「形から出す」「記号を数える」を、やり方ではなく数え方の単位の名前(「面積・長さで数える行」「箇所・個数で数える行」)に付け直すか(K-17 3 番) | 札で聞いている。答えが来るまで今の名前のまま |
| 「図面に現れない行」8 行が公開の積算基準に載っているかの調べ(K-17 2 番) | 調査担当(Codex-B)が探す。渡す表は共有フォルダの `reports/図面に現れない行_8行_公開基準の探し先.md` |
| 仮設水道・仮設電気の行と「貸与かどうか」の問い(K-19 2 番) | いまの仕組みでは、行は 1 式で立つが金額と貸与は書けず、問いも立たない。入口を作るのは単価の側の新しい実装なので、作る前におーちゃんに聞く |

**決まって、始める時機を待っているもの**

| 何か | いつ |
|---|---|
| 試験C(段階の順番の入れ替え)。指示文 4 本は並べ替えたもので固定(K-12 2 番) | 測定担当(Codex-A)の手が空いてから。手順は `docs/experiments/stage_order/codex_a_runbook.md`、指示文は共有フォルダの `reports/試験C_指示文_全文.md` |
| #44(精密/概算モードの設計)の実装 | 設計はマージしてよい(K-12 5 番)。**実装に入る前に、もう一度おーちゃんに聞く** |

**決める時機が決まっているもの**

| 何か | いつ決めるか |
|---|---|
| a1・a2 の報告の数字を、直した印(「印字をそのまま読む」→「波及」の 9 行)で測り直すか | K-11 の 1 番・2 番が終わってから(K-11 5 番) |
| 群合計: 止まった要素が 2 つ以上ある群の素通り。候補の直し方でも残る死角(許容差の中に収まる誤り) | 許容誤差の本決めと一緒に(K-10 6 番。候補の直し方はいま入れない。測定 `docs/group_total_multi_stop_report.md` は残す) |

### おーちゃん側の宿題

- **会社の標準記号表を用意する**(K-11 4 番)。記号を読むには、その案件の凡例と会社の標準記号表の**両方**が要る。
  - P011 匿名化v2 の図面リスト(1 ページ目)には凡例の図面が 2 枚ある: 6 枚目「凡-1 平面図凡例」と、22 枚目「凡ー2 電気位置図 凡例・標準仕様」(凡例と標準仕様が 1 枚に同居)。
    K-11 では「独立した凡例ページが無い」とあったが、K-12 1 番でおーちゃんが訂正した。
  - 凡例の記号と平面図の記号を照らし合わせられるかは別の話で、A-1 の 11 周目では 1 つの名前に最大 474 通りの形が付いた
    (12 周目で 1 通りに減ったが判定は保留。`docs/a1_legend_one_per_row_report.md`)。標準記号表を入れたときに照合できるかは、まだ測っていない。

- **図面に現れない行と駐車場代の決め方・値を決める**(K-15、K-19 1 番で 5 行に確定。2026-09-24)。
  - 小運搬費(決め方)、荷上費(決め方)、墨出し(㎡あたりの値)、竣工時清掃(㎡あたりの値)、駐車場代(日数と日額)。
  - 図面を読む技術は要らず、おーちゃんが答えれば埋まる行。K-03 37 番で「後回し。知識の質問リスト(Codex-B)に合流させる」と決めていたもの。
  - 墨出しの「どの面積に掛けるか」は公開の基準に式があり、小運搬・荷上は調査担当が探している。見つかれば、「決め方」は「公開の基準の式を使うか、自社の決め方にするか」を選ぶ問いになる。㎡あたりの値と日額は、どちらにしても金額の側。
  - 駐車場代の入れ方は「要るかどうか・日数・日額と台数を別々に入れる」を提案している(K-13 3 番)。**いまはこれを受け取る入口が無い。**
- **金額はリポジトリに書かない**(取り決め 4 番。K-19 4 番で「共有フォルダのままでよい」と確認)。金額つきの表は共有フォルダの `reports/おーちゃん側の宿題.md` にある。

### 知識はまだ入れていない(K-17、2026-09-24。「入れても効かなかった」ではない)

**知識を入れて試した結果、効かなかったのではない。知識はまだ入っていない。**自動確定が 0 件のままなのは、知識が効かない証拠にはならない。
2026-09-24 に、次の 4 点をコードと記録で確かめた。

1. **知識の表の候補は全部「候補」で、1 件も採用されていない**(`benchmarks/fixtures/knowledge_candidates_encoded.json`。確かめた 2026-09-24 未明は 26 件で採用 0・不採用 0)。そのあと #119 で確認日が埋まり、#121 で墨出し・養生清掃の 2 件が足されて 28 件になったが、どちらも採否は「候補」のまま。
2. **#116 で繋いだのは配管だけで、知識の中身は流れていない。**
   - 足したのは、「どの知識で数えたか」の番号を運ぶ欄と、番号が実在するかの突き合わせ(`knowledge/linkage.py`)だけ。
   - 突き合わせを呼んでいるのは計測のスクリプトだけで、本番のコードからは 0 か所。
   - 知識の中身(式・しきい値)を読んで数量を変えるコードも 0 か所。判定の側は知識の表を読み込まない(テストで固定)。
3. **記号の意味を表す知識は 1 件も入っていない。**
   - 26 件の内訳は、数え方 15・問い 6・波及 5。記号に触れているのは「記号ごとに個数で数える」という数え方の 1 件だけ。
   - 記号の名前だけの一覧(61 語、`estimating/examples/knowledge_symbol_vocabulary.json`)は計測用の見本で、形も意味も持たない。
   - 建築の標準記号表(建築工事設計図書作成基準 別表 1〜3)は読んだが(`docs/knowledge/candidates.md` G 節)、表には入っていない。電気の記号の基準は書誌だけ。
4. **読んだ文書の中身の大半が、まだ表に入っていない。**
   - おーちゃんの確認は「82 文書は地図を作っただけで、本文を読んだのは 4 文書」。**この 82 と 4 の数字は、リポジトリと共有フォルダでは辿れなかった。**
   - リポジトリの資料一覧(`docs/knowledge/sources.md`)は 37 文書で、本文を読んだ印は 7 文書。表に入ったのはそのうち 3 文書だけ。
   - 例: 公共建築数量積算基準 第 7 編-1-2-2 の墨出しと養生清掃の式は、本文を読んでいたのに表に入っていなかった。
   - 公共建築工事共通費積算基準・標準単価積算基準・標準歩掛は、本文を読んでいない。

### 区分の名前(K-17 1 番、2026-09-24)

- 正解 99 行の区分「会社のルール」を**「図面に現れない行」**に変えた。図面を読んでも出てこないが、工事には必ず発生する行、という意味。
- 「会社のルール」と呼んだせいで、公開の積算基準を探す先から外れていた(おーちゃんの指摘)。
- 正解ファイルの中の元の名前は `standard_rule` で、「会社」という言葉は入っていなかった。
- **8 行のうち 3 行は、行が立つ条件が図面の 2 ページ目(計画概要)の「工事規制」の欄に印字されている**(共用部養生、エレベーターと搬出入規制)。図面に現れないのは数量と率のほう。
- コードの中の名前は #118(判断待ち)。本当に会社が決めたもの(面積の芯々など)は「会社のルール」のまま残している。

### 2026-09-23 に決まって片づいたもの(判断待ちから外したもの)

- 本線に入った PR: #44・#96・#101・#102・#103・#104・#105・#107・#108・#109・#110・#111・#112・#113・#114・#116・#117・#120・#122、2026-09-24 未明に #106(現況と計画を分けて読む。札「1 件戻して入れる」)・#119(確認日を埋める)・#121(墨出し・養生清掃の候補、候補は 28 件に)。
- おーちゃん不在の 10 時間(K-20、記号と線種を読む)の進め方 6 項目は「全部推奨で」(2026-09-24 00:49)。
- 正解 99 行の数量の出どころは 6 区分のまま(形から出す/記号を数える/印字をそのまま読む/波及/図面に現れない行/人の入力。「図面に現れない行」は K-17 で「会社のルール」から改名)。印字の 15 行を 5 区分へ寄せない(2026-09-24 00:07 の札)。
- 正解ファイルの「印字をそのまま読む」の印: 9 行を「波及」に直した(K-07 2 番)。直す前の版は封印フォルダに残し、読み替え用の正誤表を共有フォルダに置いた(札「正誤表を横に置く」「直した版のまま」)。印字の場所が辿れない 1 行は、今の印のまま注記を残す(K-12 3 番)。
- 採点の当て方は変えない。当て方が原因で落ちた件数は、言い当ての数とは別枠に記録した(K-07 1 番)。
- 知識の表の拘束力に `社内見立て` を足した(K-10 5 番)。確認日が空欄の知識は採用の判断に進めない(K-07 5 番)。
- 仕上表の「同上」は一覧から外して問いに回す、下地欄の語を 7 つ足す、94 行になったことを仮の判断に書く(K-10 1〜3 番、#111)。

---

## 2026-09-22 10:11 時点の状況(当時の記録。書き換えていない)

**【2026-09-22 09:37 訂正: 「P011(三井D)は34ページ全部スキャンで文字データが無い」は誤り】**
- 実際には**P011には2つのファイルがある**。この訂正より前の全ての記録(「実案件P011ではこの実装は1件も効かない」「実図面にいま最も効く追加はOCR」等)は**古いほうのファイルだけの話**だった。
  - **古いほう(18MB・A0相当)**: 34ページ全部が画像、文字データなし。**これまでの記録どおり。**
  - **新しいほう(匿名化v2、27MB・A3)**: **34ページ中30ページに文字があり、合計29,003文字。罫線の表を取り出す仕組みを34ページに掛けて107個の表が取れた**(建具表・内装仕上表・仕様書・凡例が行と列の対応を保ったまま読めている)。**この案件にOCRは要らない。** 18〜21ページの4枚だけ文字なし(画像だけ)。1ページ目が図面リストで、要約資料がある案件の条件を満たす(既存平面図/改装平面図/既存撤去図/展開図8枚/建具表/建具詳細図/内部仕上表2枚/仕様書/平面図凡例/電気位置図/照明器具リスト)。
- **⚠️ 今後P011の話をするときは、どちらのファイルかを必ず区別すること。** 「実図面にいま最も効く追加はOCR」という従来の結論は、**少なくともP011 v2には当てはまらない。**
- 匿名化v2に設計事務所名・個人名・建築士登録番号が文字として残っている件(18ページ分)は従来どおり。実験の入力パッケージからは機械で検査して取り除く方針。
- **下記の各キューにある「P011は全ページスキャン」等の記述は、この訂正が出る前の記録であり、古いほうのファイルについての記述として残す(書き換えない)。** 新しい報告はこの訂正を踏まえて行うこと。

**【2026-09-22 06:17 おーちゃんより作業停止の理由確認があり、コーディネータが報告済み(経緯として記録)】**
- 最後に完了した作業: 原則の文書化(キュー0、06:03)。その前は数量を見積の行に当てはめる仕組みのマージ(キュー3、05:07)。
- 止まった時点: 06:10。動いている作業がゼロになった。
- **止まった理由: 判断待ち。** 使用量の上限・エラー・セッション終了のいずれにも当たっていない。おーちゃん自身の「コードを直すのは一覧の確認後」「この文書はマージせず待機」という指示に従って止めたもの。
- 判断待ちは22件(下記「判断待ち」の一覧のとおり)。
- 判断の要らない次の作業として着手: CIワークフローの古い書き方の非推奨警告を直す。thread `cmsg_01XUHdxe1bsBZCx8utd2aYfMFbhpWFe1v22FWvsgSkTy81`、session `cse_01PtmUWPNZLFatLHwCMSfEJr`、Sonnet 5。**→ 完了・マージ済み(下記参照)。**
- OCR(スキャン図面から文字を読む)は新しい作業のため、当時はおーちゃんの返事待ちで着手していなかった。**→ 下記06:20の回答により着手済み。**

**【2026-09-22 06:20〜06:24 おーちゃんより判断待ち22件すべてに回答。以下が最新】**

**すぐ始めてよいものとして承認(2件):**
- スキャン図面から文字を読む(OCR)作業 → 着手済み。thread `cmsg_01XUHdxe1bsBZCx8utd2aYfMAb8z5j6wxpy5XvzsPCo8Hm`、session `cse_01SY67hMoS93mFjxcAQFixXB`、Opus 5。判定を変えない機能追加のため、テストが通ればマージしてよい。
- PR #24(開発全体の最上位の原則)→ マージしてよいとの回答あり。原則スレッド自身がマージを実行し、その後「原則に反している所の修正」に着手する(既存の判定のしかたを変える修正は別PRで待機、意味欄・「何に基づくか」欄の追加だけはマージしてよい)。

**22件の個別回答は下記「判断待ち」の各項目に転記済み。** 要点:
- 3(抜き取り監査の本番組み込み)→ まず設計を書く。既存のキュー4スレッドに設計を依頼済み(着手、下記参照)。
- 4(群合計制約PR #6)→ Codexの固定ベンチマークでの測定待ちのまま(Codexが止まっているため保留継続)。
- 16(面積未回答時)→ 両方の値で「仮説に基づく」行を2本出す方針に確定。
- 20〜22(質問選定の高速化)→ 保留継続。
- その他の詳細回答は各項目を参照。

**【2026-09-22 06:24〜06:32 コーディネータより追加報告】**
- **CIの非推奨警告(旧・次の一手5)→ 完了・main マージ済み**(`de395e9`)。`actions/checkout@v4→v7`、`actions/setup-python@v5→v7`。CIグリーン、Node.js 20の警告2件が消えたことを確認済み。`ubuntu-latest`のUbuntu 26移行(2026-10-19)は案A(何もしない)のままおーちゃんの最終確認待ちとして判断待ち23に残す。
- **抜き取り監査の本番組み込み(判断待ち3)→ 設計完了、実装着手。** `docs/audit_production_integration_design.md`(`claude/audit-production-design`、`8807dd4`)。**組み込んでも当面は抽出0件のまま**(判定側の問題であり監査側の問題ではない)。詳細はキュー4を参照。PR #10の上に積む形で実装中。
- **原則スレッド → PR #24(原則の文書)を main にマージ済み**(`c7db5d7`)。続けて「原則に反している所の修正」の1つ目のまとまり(PR #29、意味欄・Basis4値の追加のみ、判定は変えない)がCI待ち。全件テスト708→728件パス。詳細はキュー0を参照。
- **いま動いているスレッド(最大2本ルールの範囲内): OCR(スキャン文字読み)、原則違反の修正(案件前提の層 `CasePremise` に着手予定)、抜き取り監査の本番組み込み実装。**

**【2026-09-22 06:40 おーちゃんより原則の位置づけを再定義。これまでの原則関連の指示はこの内容で置き換え】**
- **用語:** 「積算の絶対原則」(工事内容=現況と計画の差分/見積書は提案書)と「スタートキットの3要素」(目的・基準・現況)は別のもの。正式な文書は `docs/principles/start_kit.md`。
- **「最上位」= 判断に迷ったとき先に照らす基準のことで、実行時の関門ではない。既存コードは違反一覧に載せて順に直す対象であり、壊れているとみなさない。**
- **優先順位: ①運用のルール ②start_kit.md ③個別の設計文書 ④既存のコード・テスト。食い違いはテストを書き換えず報告する。**
- **数量は止めずに出す方針を再確認・強化(以前「前提が揃うまで止める」と書いた箇所はすべて取り消し)。** 詳細・違反一覧5・11・15の再解釈は下記「キュー0」を参照。
- 原則スレッドは理解の報告済み、返事を待たずOCR・原則修正・抜き取り監査の3スレッドを継続中。

**【2026-09-22 06:44〜06:45 おーちゃんより「まだ残るもの」の進め方(理解の報告は「ずれなし」と確認済み)】**
- 順序: ①目的/現況の申告の受け皿(画面は作らずデータの受け皿だけ、判定を変えない追加でテスト通過後マージ可) ②意味の4欄(新規は必須・既存は修正のついでに) ③現況と計画の差分(工事内容)を作る場所は**別スレッドへ切り出し**(積算の中心のため、まず設計をdocsに書き、PRのまま確認待ち)。→ 詳細はキュー0・キュー8(新規)参照。
- **いま動いているスレッド(4本。おーちゃんの「同時に動かすスレッドは2本まで」ルールを超えているが、コーディネータの判断で並行進行中。記録として残す): OCR(スキャン文字読み)、原則違反の修正(PR#29マージ済み→目的/現況の受け皿→前提の層)、抜き取り監査の本番組み込み(設計済み・実装中)、現況と計画の差分の設計(新規・キュー8)。**
  - **【2026-09-22 06:48 コーディネータより理由の説明】** 「2本まで」ルール(2026-09-22 03:06)は、おーちゃんが移動中に使用量の上限で作業が止まるのを防ぐためのもの。いまはおーちゃんが戻っていて本人が同時に4つの作業を指示している状態のため、コーディネータの判断で4本動かしている。**ルールを忘れたのではなく、置かれた目的が今は当てはまらないという判断。** おーちゃんにもその旨を伝達済み。使用量が厳しくなれば2本に戻す。
- 原則スレッドの進捗2: PR #24マージ済み(`c7db5d7`)、PR #29マージ済み(`2b46fb5`)、PR #30(CasePremise、752件パス)はCI待ちで開いたまま、PR #32(一覧訂正・文書のみ)新規。詳細はキュー0参照。

**【2026-09-22 06:51 おーちゃんより優先順位の再編、「2本まで」ルールを復元。理解確認は「ずれなし」】** そのまま記録:
> 確認ありがとうございます。2本に戻してください。優先順位は次のとおりです。1. 寸法の数字を読む作業 2. スキャンの図面の文字読み取り 3. ページの種類の宣言の修正 4. 目的と現況の申告の受け皿 5. 差分(工事内容)の設計。1と2を先に動かし、どちらかが終わったら次を始めてください。今動いている3〜5は、区切りのよい所で止めてstatus.mdに続きの位置を書いてください。今回のように、おーちゃんが決めたルールを状況に合わせて変えたほうがよいと判断した場合は、変える前に確認してください。

- **新しい優先順位: ①寸法の数字を読む(新規・🟢着手、下記「キュー9」) ②スキャン図面の文字読み取り(継続中、変更なし、キュー7) ③ページの種類の宣言の修正(⏸停止) ④目的/現況の申告の受け皿(⏸停止) ⑤差分(工事内容)の設計(⏸停止、設計は完成・PR #33で待機)。①②を先に動かし、どちらか終わったら次へ。**
- **同時2本ルールに復元。** ルール変更前に確認する新規則をREADME.mdに追記済み(下記参照)。
- ③④(原則違反の修正スレッド)・抜き取り監査の本番組み込みは、区切りのよい所で止めて続きの位置を送るよう指示済み。**抜き取り監査は新しい一覧に入っていないため停止するが、設計そのものは承認済みで評価が下がったわけではない**(実図面で何も動いていない原因を先に開けるための中断)。
- **差分(工事内容)の設計は完成、PR #33で待機。** 続きの位置の報告も到着済み。詳細は下記「キュー8(差分の設計)」を参照。

**現在の優先順位:**
1. **【07:39完了・PR #39】寸法の数字を読む** — 判定のしかた自体は変えないが縮尺の結論が変わるためマージ判断待ち(判断待ち33)。下記「キュー9」を参照。判断待ち30(目的受け皿への乗り換え)も未回答。
2. OCR(スキャン図面から文字を読む) — **【07:13完了・main マージ済み `b093f1c`】870件パス。** 下記「キュー7」を参照。
3. ページの種類の宣言の修正(原則違反11番の残り) — **【07:43案Aで確定・880件パス】判定を変える変更のためマージせず待機(判断待ち32はマージ可否のみに縮小)。** キュー0の一部。
4. 目的/現況の申告の受け皿 — ⏸停止。キュー0の一部。
5. 差分(工事内容)の設計 — **設計完了、PR #33で待機。実装は①②が空くまで着手しない。** 下記「キュー8(新規)」を参照。
6. 開発全体の最上位の原則 — 一覧・見直し完了。**PR #24・#29・#30・#32・#35マージ済み。** 判定が変わる修正5件は未着手(設計のみ)。**【07:52完了・main マージ済み】おーちゃんが原則に3層構造(絶対原則/解釈の原則/作業の手順)を追加指示、PR #41(文書のみ、`a89e481`)マージ済み。指示メッセージが途中で切れており続きを確認中(判断待ち35〜37も追加)、続き到着まで停止中。** 詳細はキュー0参照。
7. 本番の入口 — **完了・main マージ済み**(旧キュー1)。
8. 建具表・内装仕上表の構造化 — **完了・main マージ済み**(旧キュー2)。
9. 数量化と見積の行への対応付け — **完了・main マージ済み**(PR #21)。
10. 抜き取り監査を階層1へ拡大 — **完了・main マージ済み(PR #10 → `90fae24`)。本番の処理から走るようになった。検査対象1件以上出たら報告する約束あり。**
11. 群合計制約の修正 — PR #6、Codexの効果測定待ちのままPRで保持。**07:12 main(90fae24まで)取り込み直し済み、842件全件パス、コード衝突なし。**
12. 質問選定の高速化の設計案 — 保留継続。
13. CIの非推奨警告の解消 — 完了・マージ済み。
14. **【新規・2026-09-22 10:10】P011での比較実験(A/B読み取り)の準備完了。** 採点基準PR#50/#51、OCR部品のCI組み込みPR#52、いずれもマージ済み。実行はA/B交互に1回ずつから開始。下記「キュー10(新規)」を参照。

現在おーちゃんは在席中で優先順位を直接指示しているため、この節は「戻ったとき」ではなく現在の状況として読む。参照すべきは下記「次にやるべきことの提案」と「判断待ち」(22件+差分設計の6件、すべて回答済みまたは受付済み)、
および未マージのまま残っているPR(#6〈群合計、Codex測定待ち〉, #15〈質問選定高速化、保留〉, #33〈差分の設計、確認待ち〉, #34〈OCR、CI待ち〉)です。
- **次の一手:** 寸法数字読み取り(キュー9)・OCR(キュー7)の2スレッドが進行中(2本ルール)。③④⑤(ページ種類宣言・目的現況受け皿・差分設計の実装)は区切りのよい所で停止済み、続きの位置はキュー0・キュー8に記録。それぞれの続報が届いたら記録係が転記する。

## 作業ごとの状況

### 記録係(このスレッド)

- スレッド名: 記録係(コーディネーション記録)
- 使用したコミット: PR #5・#8・#9(マージ済み)。以降の更新はこのブランチ(`claude/coordination-log-4`)で行う。
- 完了したこと: docs/coordination/README.md と status.md の作成・追加ルールの反映、キュー1・2・3・4・5 の報告の反映
- 残っていること: コーディネータから届く各作業の進捗を、その都度このファイルに反映する。このスレッドは自分から動かず、更新が届いたときだけ動く。

### キュー0(新規・2026-09-22 05:45おーちゃん追加・最優先): 開発全体の最上位の原則(スタートキットの原則)

- スレッド名: Opus 5 スレッド。thread `cmsg_01XUHdxe1bsBZCx8utd2aYfMLXpYhVLQQqwNSHHjzjf5bv`、session `cse_01UeSBRtdnErgdLtw67iF1GU`
- 状態: **完了(一覧・設計見直しとも)。【2026-09-22 06:20 おーちゃんより回答】PR #24はマージしてよい。原則スレッド自身がマージを実行し、その後「原則に反している所の修正」に着手する(回答1: 既存の判定のしかたを変える修正は別PRで待機、意味欄・「何に基づくか」欄の追加だけはマージしてよい)。**

- **【2026-09-22 06:40 おーちゃんより原則の位置づけを再定義。「これまでの原則に関する指示はこの内容で置き換えてください」との明示あり。以下、原文の要点をそのまま記録(下記「原則の中身」より優先)】**
  1. **用語をはっきり分ける。** 「積算の絶対原則」(工事内容=現況と計画の差分/見積書は提案書)と「スタートキットの3要素」(目的・基準・現況)は**別のもの**。「3要素」と言えばスタートキットの3要素を指す。正式な文書は `docs/principles/start_kit.md`。
  2. **「最上位」の意味 = 「設計の判断に迷ったとき、何より先にこの原則に照らす」ということ。** 次の意味ではない: 実行時にすべての処理の前に置く関門ではない/3要素が揃わないと何も動かない、ではない/**原則にまだ合っていない既存のコードを壊れているとみなす意味ではない**(違反の一覧に載せて順に直す対象)。
  3. **優先順位(ぶつかったら上が勝つ):** ①運用のルール(実図面・見積をコミットしない/判定のしかたを変える修正はマージしない/単一の根拠で自動確定しない)②`start_kit.md` ③個別の設計文書 ④既存のコードやテストの慣行。**原則と既存のテストが食い違う場合、テストを勝手に書き換えず、食い違いとして報告する。**
  4. **3要素それぞれが何に効くか:**
     - **基準**: 効くのは**図面上の長さから数量を計算する処理だけ。** 基準点が無い場合、図面に書かれた寸法の数字から計算できるものは計算してよい。**縮尺の表記だけに頼って長さを出した値は「仮説に基づく」にする。止めるものではない。**
     - **目的**: 効くのは何を読むか・どの見積の行が対象かの推定。人が与えるのは方向性の自由記述と資料の指定だけで、どちらも弱い手がかり。図面と食い違ったら**人への確認事項として出す**。**与えられていなくても処理は止めない。** AIが推定した目的には推定であることを示す。
     - **現況**: 効くのは現況/計画の区別と差分(工事内容)の計算。人は把握の状態(明確/おおむね/不明・表現なし/混在)を申告するだけ。**未申告は「不明」として扱う。** 不明の部分は推論または仮説で進め、その旨を示す。
  5. **数量は止めずに出す。** 「前提が揃うまで数量を出さない」と以前書いた箇所があれば**取り消し**。数量は出し、各値に「何に基づくか」(確定/推論に基づく/仮説に基づく/現地確認が必要)を付ける。**自動確定できるのは「確定」だけで、確定には単一でない根拠が必要。**
  6. **今進めている作業との関係:** スキャン読み取り(3要素とは独立に進めてよい。読んだ文字にはページ・場所を記録)/原則に反する所の修正(Basis欄・意味欄の追加は進めてよい、判定が変わる修正はPRで待機)/抜き取り検査の設計(「確定」とした行が本当に正しいかを検査する仕組みとして設計)/キュー3の3層(「何に基づくか」を加える)。
  7. **原則どおりにできない場合、作業全体を止めず、その部分だけ質問としてstatus.mdに書き、他の作業を進める。**
  - **コーディネータによる違反一覧の書き換え指示(下記一覧に反映済み): 5番は違反ではなくなり「仮説に基づくと表示する」話になる。15番は「止める作り」そのものが取り消し(両方の値を仮説に基づく行として出す方向、判定が変わるためPRで待機)。11番も目的・現況を「未入力でも止まらない弱い手がかり」として扱えば筋が通る。残る違反は19・20・21と、意味の4欄が既存コードに未反映であること。**
  - **原則スレッドはこの再定義の理解を「最上位の原則が効く所/効かない所」「3要素が揃っていないときの動き」「解消する警告と残る警告」の形でおーちゃんへ報告済み。返事を待たずに6の3スレッド(スキャン読み取り・原則の修正・抜き取り検査の実装)を継続中。**
- 使用したコミット: 土台main `a60c831` → 実装 `97dc54f` → 最新main `56bd4bc` 取り込み `6516bb2`(ブランチ `claude/development-principles`、PR #24)。
- 成果物3つ(依頼内容そのまま):
  1. `docs/principles/start_kit.md` を新規作成し、`docs/coordination/README.md` の冒頭から「すべての作業はこの原則に従う」としてリンク(実図面や見積の中身は書かず原則だけ)。
  2. 今のコードとキュー3の設計を原則と照らし合わせ、**原則に反している所の一覧**をstatus.mdに書く(例: 意味の付いていない値を使っている/縮尺の表記に依存している/人の入力を無条件に正解としている/確定と仮説の区別がない/順番が切り替えられない)。
  3. キュー3(数量化と見積の行への対応付け)の設計の見直し(文書のみ)。読み取った数量/案件の前提/会社のルールの3層に加えて、各値の「何に基づくか」(確定・推論に基づく・仮説に基づく・現地確認が必要)を持てるようにする。
- **判定のしかたは変えない。コードを直すのは、一覧をおーちゃんが確認してから。**
- **この文書はPRにしてマージせず待機。** おーちゃんが中身を確認してからマージする(自走ルールの「マージせず確認を待つもの」に明確に該当する指示のため、記録係としてもマージしない)。
- 原則の中身(そのまま記録。本文の置き場所は `docs/principles/start_kit.md` の1箇所のみとし、README.mdには全文を書かない。README.mdの冒頭リンクはこのスレッドが追加する):
  1. 積算の絶対原則: **工事内容とは現況と計画の差分である。** 現況が分からなければ積算はできず、計画が分からなければ何を読むべきかが決まらない。**見積書は提案書である。**
  2. 意味を理解して動く: 読み取った値には必ず意味を付ける(それは何か、どこのものか、現況か計画か、目的にどう関係するか)。**意味が付けられない値は数量に使わない。**
  3. スタートキットの3要素は人が与えられる確かさがそれぞれ違う。**人の入力が悪いと全体が悪くなる設計にしてはならない。**
     - 基準(人が正確に与えられる唯一の要素): **図面に書かれた縮尺の表記は当てにしない**(スキャンや縮小で歪んでいる可能性)。人が画像を拡大し、区切りのいい寸法の両端をクリックして実際の長さを入力する。縦横別々に持ち、さらに区画ごとに基準を持てる形にする。**人のクリックや入力も間違う前提とし、図面の寸法の数字と突き合わせて検算し、合わなければ警告する。**
     - 目的(方向性と資料の指し示しだけ。二段階で確立): **人に詳細を設定させると自動積算の意味を失う。** 人がするのは自由記述で方向性を書くことと、やりたいことが最も濃く書かれている資料を選ぶことの2つだけ。方向性は弱い手がかりとしてだけ扱い、図面と食い違ったらそのこと自体を人に聞き返す。詳細な目的はAIが図面を読んだ後に組み立て、要約を人が確認する。
     - 現況(正確さを求めず、把握の状態を申告する): **人に正確な現況を用意させるルールにしてはならない。** 明確に分かる/おおむね分かるが確実ではない/全く分からない・図面で表現されていない/部分によって混在する(範囲ごとに申告)の4つから選ぶ。分からない部分はAIが推論するか仮説を置いて進む。
  4. 人が先に入力してよいものの条件(3つすべてを満たすもの): ①人がすぐに確実に分かる ②AIでは確実に推定できない ③図面の読み方を縛らない。**現時点でこれを満たすのは基準点だけ。** それ以外はAIが先に読み、方向を見失いそうな大きな分かれ目(ゲート)だけを人に聞く。人は候補から選ぶか確認する形で答える。
  5. 数量の出し方: **前提が揃うまで止めるのではなく、何に基づくかを区別して出す**(確定/推論に基づく/仮説に基づく/現地確認が必要)。仮説に基づく行はどの仮説に依存しているかを記録し、仮説が変わったら連動して見直す。**人が与えた情報も図面と突き合わせて矛盾を探す。人の入力を無条件の正解にせず、単一の根拠では自動確定させない。** 人の負担は質問の数ではなく操作の回数と時間で測る。
  6. 未決: 人が先かAIが先かの順番は未決。**A方式(人が方向性と資料を先にセット)/B方式(AIが先に読みゲートを抽出してから人に聞く)/混合方式(当面の仮の方式。基準点だけ人が先、残りはAIが先)を切り替えられる作りにし、同じ図面で比べて決める。** 比べる指標は、確定できた行と金額/誤って確定した行/人の操作回数と時間/わざと誤った方向性を与えたときに食い違いに気づけるか。**この比較の計測は、読み取りの流れが動くようになってからCodex側で行う。今は実装しない。**
- **完了したこと(成果物3つ、そのまま記録):**
  1. `docs/principles/start_kit.md`(原文1〜6節をそのまま起こした)+ `docs/coordination/README.md` の冒頭にリンク3行。
  2. `docs/principles/principle_conformance_review.md`(原則に反している所の一覧、下記に転記)。
  3. `docs/principles/queue3_redesign.md`(キュー3の設計見直し、文書のみ、下記に転記)。
  - **Pythonは1行も触っていない。判定のしかたは変えていない。**
  - 読んだ範囲(記録ではなく現物): `intake/drawing_intake.py` 全1,172行、`intake/start_kit.py`、`intake/case_answers.py`、`estimating/` 4ファイル、`arbitration/method_policies.py`、`axes/image_axis/pdf_vector_symbols.py` の面積・縮尺部分、`axes/reading/protocol.py` の由来の扱い。**読んでいない**: `arbitration/` の残り、`benchmarks/`、`killer_question/`、`mobile/`。

- **原則に反している所の一覧(そのまま転記):**

  **A. 意味の付いていない値を使っている(原則2)**
  1. 面積の記載に現況/計画もどこの面積かも付いていない。`drawing_intake.py:943`、拾うのは `pdf_vector_symbols.py:302` の正規表現で「専有延床面積/施工床面積」の2語だけ。
  2. 開き戸の件数に「どの部屋か」が無い。`drawing_intake.py:894`、対象名は `開き戸::ページN`。現況/計画は人が宣言したときだけ入る(`:906-912`)。
  3. 建具表の数量が、現況と計画を1つのレンジに混ぜている。`drawing_intake.py:677`。足さないのは正しいが、想定しているのは値の幅ではなく意味の違い。
  4. 見積の行を決める規則が現況/計画を条件にできない。`quantities.py:110` の `kind` は最初の `::` の前しか見ないので `開き戸::現況::ページ1` の「現況」は規則から見えない。規則が使える属性は `from_intake.py:27` の3つ(種別・幅・高さ)だけ。→ 原則1(工事内容=現況と計画の差分)を行の側から表現できない。

  **B. 縮尺の表記に依存している(原則3-1)**
  5. **【2026-09-22 06:40 再定義により違反ではなくなった】** 基準点が無いページでは、表題欄の印字した縮尺だけで実寸を出している。`drawing_intake.py:725`(読みが1つなら `_all_agree` は常に真)→ その縮尺で `:894` が建具の幅をmmに直す。**基準点が無い場合に寸法の数字から計算するのは許される動きであり、縮尺の表記だけに頼った値は「仮説に基づく」と表示すればよい(止める理由にはならない)。** → Basis欄の追加で対応(判定は変えない)。
  6. **【2026-09-22 06:45 重みが増した】** 「図面に書かれた寸法の数字と突き合わせて検算」が無い。`:830` が突き合わせるのは同じ2点から計算した印字由来の長さだけ。寸法線の数字を読む経路はリポジトリに1つも無い。**人のクリックの取り違えは今の作りでは捕まらない。** **06:40の再定義で「基準点が無くても寸法の数字から計算してよい」と決まったが、その数字を読む手段がリポジトリに1つも無いため、基準点が無いページで計算できるものは実質ゼロのまま。スキャンの文字読み取り(キュー7)とは別の話で、ベクターのページでも寸法線の数字を読めていない。原則スレッドの見立てでは、いま最も効く追加はここ。**
  7. 区画ごとの基準が持てない。`start_kit.py:185`、縦横それぞれページに1組まで。局所的な歪みを表現できない。

  **C. 人の入力を無条件に正解としている(原則5・原則4)**
  8. 面積の基準の指定をそのまま採る。`drawing_intake.py:1023-1024`。保存済み回答との食い違いは見るが**図面の中身とは突き合わせない**。
  9. 人が宣言したページの種類が抽出の範囲を無条件に決める。`drawing_intake.py:605`。種類を図面から自動判定する経路が無いので宣言の誤りは検出できず、そのページの建具は黙って0件になる。**原則4の条件3「図面の読み方を縛らない」に正面から反する。**
  10. 人が宣言した現況/計画/解体が突き合わせなしに対象名へ入る(`:906-912`)。
  11. **【2026-09-22 06:45 原則スレッドが再整理・こちらが正】** 原則4「先に入力してよいのは基準点だけ」に対し、いま先に受け取っているのは4つ(`start_kit.py:173-181`)。**3つ解消、1つだけ残る。** 現況と計画の対応・面積の選択は「未入力でも止まらない弱い手がかり」として扱えば筋が通る。**残るのはページの種類**で、人が「建具表」と宣言するとそのページで建具を探すのをやめるため、弱い手がかりではなく**図面の読み方を縛っている**(原則4の条件3に反する)。(→ 判断待ち18)
  ※ 人の入力を自動確定の根拠にしない点は守れている(`method_policies.py:113` 未校正)。破れているのは「突き合わせる」ほうと「先に入力させる範囲」。

  **D. 確定と仮説の区別がない(原則5)**
  12. 4つの区別がどこにも無い。あるのは2値だけ(`quantities.py:127`、`mapping.py:59`)。「現地確認が必要」はgrepで0件。
  13. 仲裁層には由来(read/derived/assumed)があるのに、`from_intake.py:92-106` が `derivation` を写していない。**仮説から来た数量と読んだ数量が行の上で見分けられない。**
  14. 仮説の識別子も依存関係も無く、仮説が変わったときに見直す経路が無い。
  15. **【2026-09-22 06:40 再定義により「止める作り」そのものが取り消し】** `drawing_intake.py:978`、面積の基準が未回答だと `施工対象床面積` を出さない。**原則5「数量は止めずに出す」に反する作りであり、以前「前提が揃うまで止める」と書いた箇所はすべて取り消し。** 両方の値を「仮説に基づく」行として出す方向に確定(判断待ち16の回答、判定が変わるためPRで待機)。

  **E. 順番が切り替えられない(原則6)**
  16. A方式固定。`read_drawing`(`:406`)は前提を先に受け取る形しかなく、方式を選ぶ引数が無い。
  17. B方式に要る「ゲートの抽出」が無い。人への質問は面積の基準1問の決め打ちだけ。
  18. 「人の操作回数と時間」を記録する場所が無い。「金額」は `estimating/` が意図的に持たないので、原則6の比較にはもう1層要る。

  **F. 実装が1つも無い原則**
  19. **目的(3-2)がまるごと無い。** 方向性の自由記述も資料の指し示しも受け取る場所が無く、二段階目も、食い違いを聞き返す経路も無い。原則2の「目的にどう関係するか」が値に付けられないのはこのため。
  20. **現況の把握の申告(3-3)が無い。** `PageDeclaration.phase`(現況/計画/解体)は**ページが何を描いているかの区別**で、**人がどの程度把握しているかとは別物**。範囲ごとの申告も無い。
  21. **現況と計画の差分(原則1)が無い。** `PagePairing` は対応を記録するだけ。`estimating/` にも撤去・新設が無い。**原則1が「工事内容」と呼ぶものがまだどこにも存在しない。**
  22. 「見積書は提案書である」を表す場所が無い(未着手)。

  **G. 守れているところ(壊さない)**: 人の入力を単独の確定根拠にしない/食い違ったら黙って片方を選ばない/読めなかった属性を既定値で埋めない/規則を外部ファイルに置く/次元の違う換算をしない/ページをまたいで足さない。

- **キュー3の設計見直しの要点(`docs/principles/queue3_redesign.md`、そのまま転記):**
  - **3層のうち「案件の前提」が層として存在しない**のが根本。人の前提は `intake/` の中で値に溶け、見積の行から遡れない → `CasePremise`(premise_id/kind/statement/source=human・inferred・hypothesis/scope/alternatives)を独立した層に。**仮説も前提の一種。**
  - 各値に4値の `Basis`。**「確定」は仲裁層だけが作れ、この層は絶対に格上げしない。弱いほうが勝つ。** 既存の `is_confirmed` が真なら確定、偽なら依存で決まる=**判定を変えない。**
  - 依存の記録(`premise_ids`、`affected_by(premise_id)`)。目的は速さではなく「どの行がどの前提のせいで変わったか」を人に見せること。
  - `phase` を文字列からではなく属性として持たせ、規則が条件にできるようにする(規則ファイルの版を上げる)。差分そのものを作るのは1つ手前の層。
  - 意味の4欄(what/where/phase/purpose_link)。**ただし今これを必須にすると出る数量が0件になる** → 目的が入ってから。
  - 変えないもの: 確定の3条件、規則を外部ファイル、次元の違う換算をしない、ページをまたいで足さない、単価・金額を持たない。
  - 入れる順番: ①`derivation` の写し忘れ修正 ②`Basis` の4値(①②は判定を変えないのですぐ入れられる)③前提の層 ④`phase` を属性に ⑤止めずに出す(判断待ち16の後)⑥意味の4欄(目的の後)。

- **【2026-09-22 06:04 補足】使用したコミット `6516bb2`(実装 `97dc54f` + main `56bd4bc` の取り込み)。**
  - **全件テスト708件パス**(ローカル、246.48秒、終了コード0)。`__pycache__` を消して `PYTHONDONTWRITEBYTECODE=1` で実行。件数はmainと同じ(Pythonを1行も触っていないので増減なし)。PRは `mergeable_state: clean`、衝突なし。
  - **落とし穴として記録: 1回目のテスト実行は結果が取れなかった**(`pytest | tail` のパイプで終了コードがtailのものになり、出力も空だった)。ファイルに落として流し直したのが上の708件。
  - **【2026-09-22 06:05 訂正】PR #24(head `6516bb2`)の `test` チェックは成功**(`completed`/`success`、05:54:56〜05:58:01、185秒)。**赤くも未実行でもない。** 前の「失敗・実行中のsuiteなし」という報告は言い方が雑だっただけで、`check_suite.completed` イベントはthird-partyのsuiteについて述べていただけだった。
    - 確認の手段(記録として残す価値あり): この環境に `gh` が無く、当該セッションのツールに `get_job_logs` も入っていなかったため、リポジトリがpublicであることを使ってGitHub REST APIの `/commits/{sha}/check-runs` をcurlで直接読んだ。
    - 注釈2件は失敗ではなくこのPRとも無関係(①`actions/checkout@v4`と`actions/setup-python@v5`がNode.js 20を指しているという非推奨の警告 ②`ubuntu-latest`が2026-10-19からUbuntu 26に移るという告知)。どちらもワークフロー自体の話でmainでも同じrunで出る。文書だけのPRでは触っていない(直すなら別PR。→次にやるべきことの提案に追加)。
  - メモリに `jidou-sekisan-top-level-principles` を追加済み(コーディネータより)。
- **まとめ: CI緑、ローカル全件708件パス、`mergeable_state: clean`。PR #24はマージせずおーちゃんの確認待ち。**
- 残っていること: 無し(記録係としてはこの反映で完了)。**PR #24はマージ承認済み(原則スレッドが実行)。コードの修正(原則に反している所)はマージ後に着手済み(下記参照)。**
- **【2026-09-22 06:24 追記】原則に反している所の修正に着手。** おーちゃんの回答1により、既存の判定のしかたを変える修正はPRのまま待機、意味欄・「何に基づくか」欄を追加するだけの修正はマージしてよい。

- **【2026-09-22 06:32 進捗】PR #24(原則の文書)を main にマージ済み(マージコミット `c7db5d7`)。おーちゃんの「原則の文書はマージしてよいです」に従ったもの。**
- **1つ目のまとまり(PR #29、ブランチ `claude/principles-basis-fields`、コミット `79016a7`、土台 `c7db5d7`)。回答1の「欄を追加するだけの修正」に当たるためCI通過後マージ予定(コーディネータも止めていない)。→ 【2026-09-22 06:45 マージ済み `2b46fb5`、CI緑】**
  - 新規 `estimating/basis.py`。4値(確定/推論に基づく/仮説に基づく/現地確認が必要)と**「弱いほうが勝つ」規則**。空の並び・知らない値にも既定を返さない。
  - **由来の写し忘れの修正(一覧D-13)。** 仲裁層の `read`/`derived`/`assumed` を `from_intake.py` が数量へ運んでいなかった不具合を修正。
  - `MappedLine` が基づき(Basis)と依存する前提を持ち、`lines_depending_on()` で**前提を差し替えたとき作り直す行だけ**を引ける(回答10に対応)。
  - 「現地確認が必要」は**根拠が無いと名乗れない**(回答8に対応)。
  - 全件テスト **708→728件パス(260.87秒)**。壊し試験5通りで検出を確認。**判定のしかたは変えていない**(確定の3条件はそのまま、「基づきを足しても確定が増えない」ことを `test_adding_the_basis_does_not_settle_anything_new` で固定)。
- **設計判断2つ(コーディネータの判断、おーちゃんが見て違うと思えば直す前提で記録):**
  1. `queue3_redesign.md` では `assumed`(一般則で補った)を「推論に基づく」に寄せる予定だったが、**「仮説に基づく」に変更。** 原則3-3の「仮説を置いて進む」そのものであり、既存の「`assumed` は階層1に入らない」守りと向きがそろうため。
  2. **図面から読めただけで仲裁層が確かめていない値は「確定」ではなく「推論に基づく」**にした。読めたことと正しいと確かめられたことは別だから。**4区分に「読めたが未確認」に当たる箱が無いため、いちばん近い箱に寄せたという性質の判断。** おーちゃんが別の置き方を望む可能性あり。
- **訂正: 回答12「同じ次元の単位換算(mmとm)は許可」は、調べたところ既にそうなっていた**(`arbitration/units.py` でmもmmも同じ正規形に落ちる)。新しく許可したのではなく、テストで固定しただけ。次元が変わる換算(長さ→面積)は引き続き行わず、前提の層(次のまとまり)で扱う。
- **次の段取り: ③案件の前提の層(`CasePremise`)。そのあと、判定が変わる修正(②面積未回答時に仮説行2本出す、④`phase`を属性に、16単位無し寸法をmm仮定、17建具表と平面図の食い違い表示、12次元が変わる計算)は別PRにまとめてマージせず待機。5(混合方式を既定に)と6(区画は食い違ったときだけ細かく)は設計文書に入れる。**
- **【2026-09-22 06:45 進捗2】PRの状況:**
  - **#24 マージ済み**(原則の文書)`c7db5d7`。
  - **#29 マージ済み**(「何に基づくか」の4値+由来の写し忘れ)`2b46fb5`、CI緑。
  - **#30 開いたまま**(案件の前提の層`CasePremise`)`cedb724`+main取り込み。**全件752件パス。** 判定は変えていないのでCIが緑ならマージ予定。
  - **#32 新規**(位置づけの追記と一覧の訂正)`42e852c`。文書だけ。
- **一覧の訂正(PR #32の中身、そのまま記録):**
  - 5(基準点が無いページが印字の縮尺に乗っている)→ **違反ではなくなった。**「仮説に基づく」と表示する話(上記のとおり反映済み)。
  - 11(人が先に入力するものが4つ)→ **3つ解消、1つだけ残る。** 現況と計画の対応・面積の選択は「未入力でも止まらない弱い手がかり」で筋が通る。**残るのはページの種類**で、人が「建具表」と宣言するとそのページで建具を探すのをやめるため、弱い手がかりではなく**図面の読み方を縛っている**(原則4の条件3)。**コーディネータが先に整理して送った内容より1件多く残る。原則スレッドの整理が正。**
  - 15(面積の基準が未回答だと出さない)→ **止める作りそのものが取り消し**(上記のとおり反映済み)。
  - **逆に重みが増したもの: 6(図面に書かれた寸法の数字を読む経路が無い)。** 「基準点が無くても寸法の数字から計算してよい」と決まったが、**その数字を読む手段がリポジトリに1つもない。** 基準点が無いページで計算できるものは実質ゼロのまま。**スキャンの文字読み取りとは別の話で、ベクターのページでも寸法線の数字を読めていない。** 原則スレッドの見立てでは**いま最も効く追加はここ**。→ 「次にやるべきことの提案」に追加。
  - `start_kit.md` には原文1〜7節をそのまま追記。**元の1〜6節は1文字も変えていない。**
  - `queue3_redesign.md` に、この形で確定をもらったことと、`assumed` を「推論に基づく」から**「仮説に基づく」**に変えたことを追記。
- **食い違いの報告(優先順位3節に従い、テストは書き換えていない):** 「面積の基準が未回答なら`施工対象床面積`を出さない」を固定しているテストが**2件**ある(`tests/test_drawing_intake.py:430-431`、`tests/test_intake_start_kit.py:525`)。**「数量は止めずに出す」と正面から食い違う。** 判定が変わる修正のPRの中で、新しい動き(両方の値で「仮説に基づく」行を2本、どちらも自動確定しない)を固定するテストに**置き換える**方針で、おーちゃんに確認を出してある。
- **【2026-09-22 06:44 おーちゃんより「まだ残るもの」の進め方の指示。理解の報告は「そのとおりで、ずれはありません」と確認済み】** 次の順で進める:
  1. **目的と現況の申告を受け取る場所(違反一覧19・20番)。** 画面は作らず、データの受け皿(入力の形式と、未入力のときの扱い)だけ作る。**判定を変えない追加なので、テストが通ればマージしてよい。** 目的は方向性の自由記述と資料の指定だけ(どちらも弱い手がかり、未入力でも止めない)。現況は把握の状態(明確/おおむね/不明・表現なし/混在)を申告するだけで、**未申告は「不明」として扱う。**
  2. **意味の4欄。** これまでどおり、新しいコードでは必須、既存のコードは修正のついでに入れる。
  3. **現況と計画の差分(工事内容)を作る場所(違反一覧21番)は別スレッドへ切り出し。** 下記「キュー8(新規)」を参照。
  - 1・2はこの原則スレッドが続けて担当(PR #29 → 目的/現況の受け皿 → 前提の層の順)。
  - **申し送り(コーディネータが仲介): 3番の設計は1番の「現況の申告の受け皿」と噛み合う必要がある。受け皿の形が決まればコーディネータ経由でキュー8のスレッドへ渡す(逆方向も同じ)。目的・現況の申告はどちらも「案件の前提」の性質を持つため、このスレッドが進めている `CasePremise`(案件の前提の層)と別々の仕組みを2つ作らないこと、とコーディネータから伝達済み。**
- **【2026-09-22 06:57 停止指示による続きの位置(そのまま記録)】** コミット・プッシュ完了、作業ツリーは空、未完成のまま残っているものは無い。
  - **PRの扱い**: #30(案件の前提の層)**マージ済み**(`55f3475`、CI緑を確認のうえスレッドがマージ)。#32(位置づけの追記と一覧の訂正)**マージ済み**(`d6aeda0`、checkrun`test`が`42e852c`でsuccess、mergeable clean、文書だけで判定は変えていない)。**#35(目的と現況の申告の受け皿)を新しく提出。** ローカル全件**780件パス**、判定を変えない追加。おーちゃんの承認済みなので**CIが緑になった時点でスレッドがマージ**(それ以外の作業はしない)。
  - **main に入っているもの**: `docs/principles/start_kit.md`(原則1〜6+位置づけの追記、#24`c7db5d7`/#32`d6aeda0`)/`principle_conformance_review.md`(一覧22件、#32で訂正済み)/`queue3_redesign.md`/`estimating/basis.py`+`quantities.py`・`mapping.py`への欄の追加(#29`2b46fb5`)/`estimating/case_premises.py`(仮説には解消条件が必須、#30`55f3475`)。
  - **【2026-09-22 06:59 訂正】#35はmainにマージ済み**(`e4eaa65`。checkrun`test`が`5f71f7a`でsuccess、mergeable cleanを確認のうえマージ)。**このスレッドにPRのまま待っているものは1本もない。このスレッドは停止した。**
  - **まったく手を付けていないもの(再開の起点)**: **判定が変わる修正をまとめるPRは1行も書いておらず、ブランチも無い。設計だけが上の2文書にある。** 対象5件: ①面積の選択が未回答のとき2つの面積を両方「仮説に基づく」の行として出す(どちらの仮説に依るかを各行に記録) ②縮尺の表記だけに頼って出した長さを「仮説に基づく」にする ③`phase`(現況・計画・解体)をルールが照合できる属性にする(ルールファイルの`format_version`を上げる) ④単位の書かれていない寸法をmmの仮説として扱い基準点と突き合わせる ⑤建具表の数量と平面図の開き戸検出数の食い違いを、合算も自動選択もせず矛盾として出す。
  - **意味の4欄**: 「新しいコードでは必須、既存コードは修正のついでに」という方針だけ決定。既存コードへの後追いは未着手。
  - **【ページの種類の宣言(優先順位3番)、実装完了・PR #37(判定を変えるためマージせず待機)】** 別スレッド(thread `cmsg_01XUHdxe1bsBZCx8utd2aYfM1JSceKneNV3XVGbw1bWuX9`)が実装。コミット`3bb4126`(main `b956474`から分岐)。
    - 問題(見立てのとおり確認): `intake/drawing_intake.py:647`。人がそのページを「平面図」以外(建具表など)と宣言すると、**そのページでは`find_door_arcs`をそもそも呼ばない。** ページの種類を図面から判定する手段はどこにも無いため、**宣言が間違っていても気づけず黙って0件になる。** 原則4「図面の読み方を縛らない」に正面から反する。**着手前の現物確認で、この1箇所が唯一の関門だった**(建具表・仕上表・面積・縮尺の読み取りは宣言では止まらない)。
    - **直し方(実装済み)**: 宣言の有無に関わらず必ず円弧を探す。食い違い(「建具表」宣言のページで円弧が出た)のときは**どちらも捨てず**、数量は出して「仮説に基づく」印で自動確定から外し、食い違い自体を人の確認事項にする。同じページで建具表が表として読めていればその件数も一緒に残す(**人の宣言が正しい場合もあるので材料を両方渡す**)。円弧0件なら確認事項を作らない。宣言が「平面図」のときと宣言無しのときは従来どおり。
    - 確認方法: 新テスト11件を**先に書いて11件とも失敗することを確認**してから修正(原則②どおり)。壊し試験8通りすべて検出。全件**880件パス・1件失敗**。
    - **【2026-09-22 07:43 案Aで修正済み】** 押し出したコミット`81c009e`。**全件880件パス・失敗0**(削除1件・追加11件でmainの870件から10件増)。**マージはまだ**(判定のしかたを変えるためルールどおり待機、おーちゃんに確認中)。
    - 削除した`tests/test_intake_start_kit.py:440`(`test_door_arcs_are_not_hunted_on_a_page_declared_as_a_table`)が心配していたこと(建具表と宣言されたページで円弧を探すと罫線や記号を拾いうる)は**実際に起きる**。0件にするのではなく自動確定させず人に回す形へ移した。担当の対応は4件の表として新テスト冒頭・PR #37本文・`docs/page_kind_weak_hint.md`4節に記録。削除箇所にも理由と代わりの在りかをコメントで残した。**書き換えではなく削除にしたのは、名前と中身を書き換えると何を取り消したのかが履歴から読めなくなるため。**
    - 残り: 現況/計画/解体の宣言(`phase`)は未着手(突き合わせる相手が図面側に無く別作業)。実図面では未確認(合成ベクターPDFのみ。P011は全ページスキャンでこの経路は動かない)。**食い違いが実案件で何件出るかは未知で、出すぎると人の負担になる。件数の実測はCodex側が筋。**
    - **所見(実装はしていない): ページの種類の宣言に「確かさ」を付けられるか。** 入る(置き場所は宣言のデータに欄を1つ足すだけで既存の呼び出しは壊れない、前例は現況の把握の状態と同じ考え〈未申告は「分からない」に倒す〉、使う先は宣言を使う場所が食い違いの判定1か所に寄っている)。**ただし「何に効かせるか」を決めないと入らない**: ①食い違いを人に聞くかどうかを変える→**向きが逆**(確かでない宣言ほど食い違いは大事なので「たぶんだから聞かない」は危ない) ②食い違ったときどちらを疑うかを変える(「確か」なら円弧を疑い、「たぶん」なら宣言も疑う)→筋は通るが判定のしかたを変えるので別PR。**⚠ 危険: 確かさを付けられるようにすると「確か」と言われた宣言を単一の根拠で自動確定させる入口になりえる。**「確かさが付いても、人の入力を単一の根拠で自動確定させないことは変わらない」を先に書き留めてから実装するのが安全(原則の第2層に直接関わる)。→ **判断待ち34に追加。**
  - **【現況の申告の受け皿の形(キュー8の差分設計スレッドへ渡す予定)】**
    - 現況: `ConditionSurvey(overall=<4択>, ranges=(ConditionRange(description, awareness), ...))`。4択は原則3-3の原文どおり。「部分によって混在する」のときだけ`ranges`が必須、それ以外では持てない。**申告が無いとき`StartKit.effective_condition_awareness()`は「全く分からない…」を返す。黙って「分かる」に倒さない。**
    - `estimating/from_intake.py`の`premises_from_intake()`が`CasePremise`に翻訳。未申告→`premise_id="condition::unstated"`、`source="hypothesis"`、`resolved_by="人が現況の把握の状態を申告する"`。申告あり→`premise_id="condition::survey"`、`source="human"`、`entered_by`必須。
    - **差分を作る側への接続**: その`CasePremise`のidを数量の`hypothesis_premise_ids`に載せれば、その数量は自動的に「仮説に基づく」になり自動確定しない。前提が後で埋まったときに作り直す対象もidで引ける(`MappingResult.lines_depending_on(premise_id)`)。**差分の側で独自の仕組みを作る必要はない。**
    - 目的: `Purpose(direction=<自由記述>, source_documents=(SourceDocument(label, page_number), ...), strength="weak")`。両方空は拒否。`strength`は`"weak"`固定(原則3-2をコードで固定)。**目的が無いときは前提を作らない**(「目的が不明」という前提をでっち上げない)。
- 次の一手: **このスレッドは停止した。** 判定が変わる修正5件・意味4欄の後追いは⏸停止中、①②(寸法読み取り・OCR)が空くまで再開しない。

- **【2026-09-22 07:49 新規指示: 原則の3層構造を追加】** おーちゃんが原則に3層構造を追加指示。**⚠ おーちゃんのメッセージは途中で切れている**(第3層の3例目「水回りリフォーム…それ」で終わり、「1.」に続く2以降が未達)。**推測で埋めず「ここから先は未受領」の印を文書に置いた。続きをおーちゃんに確認中。**
  - **第1層=絶対原則**: 工事内容は現況と計画の差分/値には意味を付け意味の付かない値は使わない/**人の入力も含め単一の根拠では自動確定しない。**
  - **第2層=解釈の原則**: **人の入力は「読み方の軸」を足すものであり「読む範囲」を狭めるものではない。** 軸から出てくるのは「あるはずのもの」=**確かめるべき問いであって事実ではない。** 人の入力に確かさ(確か/たぶん/分からない)を付けられるようにし、**未入力は「分からない」と同じに扱う。** 目的・現況・ページの種類など**すべての人の入力**にこの層を当てる。
  - **第3層=作業の手順**: 軸ごとに「確かめるべき問い」の一覧を持つ。(3例目まで受領、4例目以降は上記の通り未受領)
  - **実装: PR #41(文書だけ)、【2026-09-22 07:52 mainマージ済み】**。main `e5a2a66`を土台に`bb2b973`。全件**870件パス**。**原則の文書はマージしてよいという既定に従いスレッド自身がマージ(取り込みコミット`a89e481`)。** 既存1〜6節は消さず対応表を付けた。第3層に当たる既存の節は無く今回が初めて。「確かさ」の注意は**「注(原文ではない)」として原文と分けて**記載。3層構造と食い違いの一覧10件は本体に反映済み。
  - **新たに見つかった食い違い10件**を`principle_conformance_review.md`に追記(直していない)。特に効くのは2件:
    - **人の入力に確かさを書く欄が、5つの入力のどれにも無い**(唯一あるのは現況の把握の状態)。**基準点はいま既定で「強い根拠」として扱われるので、精度の確認が済んだ瞬間に「たぶん」で指した2点が強い根拠になる。**
    - **未入力を「分からない」として扱う仕組みは現況の分だけあるが、本番の処理からは一度も呼ばれていない**(テストからしか呼ばれていない)。
  - **判断待ちに3件追加(下記35〜37参照)**: ①目的が未入力のとき「目的は分からない」という前提を作るか ②平面図と建具表の突き合わせをいつ入れるか ③確かさの欄をどの入力から足すか。
- 次の一手: **PR #41はマージ済み。このスレッドは、切れた続き(第3層4例目以降)が届くまで停止中。** 判断待ち35〜37への回答も待つ。

- **【2026-09-22 09:17 原則の追加(設計者の要約資料・目的と読むことの対)、mainマージ済み】**
  - **原則の文書に追加**: 要約資料(仕様書・仕上表・工事概要書)の扱い/「読むことと目的は常に対で働く」/「要確認」と「キラークエスチョン」の区別。3-2節の「二段階で確立する」は置き換わり、その場に印あり。
  - **取り込みコミット**: `aa6480b`(PR #43、設計者の要約資料)、`5de40bd`(PR #45、目的と読むことの対)。
  - **マージ待ちのPR**: #44(精密/概算モードの設計、実装なし)、#46(軸に沿った読み方の設計、対の形とキラークエスチョンの強化)。
  - **【おーちゃんの09:05指示による一覧: 目的の情報を受け取れる読み取りの部品は0件】** どれも図面のパスとページ番号しか受け取らない。
    - 縮尺の印字/開き戸の円弧/面積の記載
    - ページの種類分け(ベクター・ラスター・空の区別だけ)
    - 表の升目/建具表/内装仕上表
    - スキャンの文字(OCR)/線の読み取り
    - **意味を返すのはOCRの面積と縮尺の2つだけで、そこも目的の欄は「目的未確立」で固定。どの経路を通っても意味が完成しない。**
  - **土台から無いもの**: 部屋の認識の実装(面積25項目中22項目がここで止まる)。図面の凡例と線の種類を読む実装(これが無いと、要約資料に書かれていない工事の区分は現況図と計画図の見比べしか決め手が無い)。
  - **用語の食い違い**: 工事上の区分の言葉が3通り(今回の指示は「区分不明」、差分の設計は「区分未定」、コードは現況/計画/解体/不明)。**3つ目は別物**(図面が何を描いているか 対 工事で何をするか)なので揃えず両方残すことを設計で勧めている。どれに揃えるかは判断待ち。
  - **直すのは設計の確認後。今回は記録のみ。**
  - **判断待ちに2件追加(下記38・39参照)**: ①精密/概算の呼び名が既存の質問モードと衝突している ②要約資料の中身を読む実装を先に入れるか。

### キュー8(新規・2026-09-22 06:44おーちゃん指示): 現況と計画の差分(工事内容)を作る場所の設計

- 何のための作業か: 違反一覧21番「現況と計画の差分(原則1)が無い」の解消。**積算の中心になる部分なので、まず設計をdocsに書く。**
- おーちゃんの指示(そのまま記録): 「現況と計画の差分(工事内容)を作る場所：積算の中心になる部分なので、まず設計をdocsに書いてください。現況と計画の図面の対応づけ、差分の種類(撤去/新設/既存のまま/改修)、現況が不明なときの仮説の置き方を含めてください。設計はプルリクエストで待機し、おーちゃんの確認後に実装します」
- 含めるもの: ①現況と計画の図面の対応づけ ②差分の種類(**撤去・新設・既存のまま・改修**) ③現況が不明なときの仮説の置き方。
- スレッド名: Opus 5 スレッド。thread `cmsg_01XUHdxe1bsBZCx8utd2aYfMHbnmdQjvnbRMaUvZXBD9ZK`、session `cse_01CdC6ir3Q8A9fVvyoqX7o3w`
- 状態: 🟢着手(新規)。
- **設計はPRで待機し、おーちゃんの確認後に実装。**(自走ルールの「マージせず確認を待つもの」に該当)
- 申し送り: キュー0(原則修正スレッド)が進める「目的と現況の申告の受け皿」と噛み合わせる必要あり。受け皿の形はコーディネータ経由でこのスレッドへ渡される。目的・現況の申告と同じ「案件の前提」の性質を持つため、キュー0が進める`CasePremise`と別々の仕組みを2つ作らないこと。
- **【2026-09-22 06:53 設計完了・PR #33】** `docs/principles/scope_of_work_diff.md`(新規1ファイル、**Python 0行**)。ブランチ`claude/scope-of-work-diff-design`、コミット`357a3b9`(main `55f3475`取り込み済み)。**マージせず確認待ち。**
  - **設計の要点(そのまま記録):**
    1. **差分の層は`estimating/scope_diff.py`に置き、入力も出力も`QuantityItem`。** `mapping.py`/`rules.py`は1行も変えない(規則は新属性`工事区分`を条件にするだけ)。
    2. **この層は確定(階層1)を作らない。** 現況と計画は同じPDFから読んだ2ページなので独立ではない(裏返しの危険と同じ理由)。
    3. **差分を引き算で作らない。** 現況3件・計画2件から「撤去1件」を自動で出さない(1件だけ撤去したのか3件撤去して2件新設したのかは数の差では決まらない)。→ **どちらも捨てず、`exclusive_group`(排他の組)で両方出す**(回答16と同じ形。同じ組の行は合計しない)。
    4. **対応づけ**は3段(範囲/対象/個体)。1段目(`PagePairing`)のみ実装済み。**対象の対応の鍵は`(what, where)`で、`where`は室の輪郭が無いため当面`PagePairing`のページの組で代用**(粗い。1枚に台所と浴室があると区別できない→行の注記に必ず残す)。個体の対応(3段目)は作らない。
    5. **対応の状態は4つ**: `matched`(差分を作る)/`existing_only`(撤去の候補)/`planned_only`(新設の候補)/`unpaired`(対応が無い、**差分を作らない**)。**`planned_only`と`unpaired`を絶対に混同しない**(混同すると現況図の無い案件で計画側が全部「新設」になる)。
    6. **差分の種類は5つ(日本語で持つ)**: 撤去・新設・既存のまま・改修・**区分未定**。「既存のまま」は工事なしと見落としの区別のため出す。「区分未定」は原則5(止めずに出す)のため出す。属性が比べられない場合や数が違う場合も区分未定または排他の組。
    7. **解体図の扱い**: 差分の入力にせず突き合わせの相手にする(一致→根拠を足すが階層1には上げない/食い違い→どちらも採らず人に示す/解体図しか無い→撤去として出すが基づきは「推論に基づく」)。
    8. **現況が不明なときの既定の仮説は「現況は不明のまま」(3案中の案3、推奨)。** 案1(現況には何も無い=全部新設)・案2(現況は計画と同じ=全部既存のまま)は**間違いが静かに通る**ため不採用。案3は計画側の数量を「区分未定」で出し、**撤去は作らず「現況の確認が必要」を1件だけ残す**(根拠付きで提案、回答8と同じ形)。未申告の現況把握は「全く分からない」扱い。
    9. 仮説を置く条件3つ: ①`resolved_by`(何が分かれば要らなくなるか)を必ず書く ②仮説は現況についてだけ置く(計画について仮説は置かない、原則3-2の裏返し) ③答えが変わる仮説は`alternatives`を書き排他の組にする。
    10. **届くために直す必要がある2か所(いずれも判定を変える変更、PRのまま待つ側)**: a) `drawing_intake.py:677`(建具表の数量、現況/計画を1レンジに混ぜている)→ まとめる鍵を「建具番号」から「建具番号と`phase`」に変える。b) `quantities.py:110`(`phase`を対象名でなく属性にする)→ 規則ファイルの`format_version`を上げる。
    11. **原則と既存テストの食い違い2件(書き換えていない)**: `tests/test_drawing_intake_schedules.py:278`(現況2件・計画5件が1レンジ`(2.0,5.0)`に混ざる状態を固定)、`tests/test_intake_start_kit.py:420`(`開き戸::現況::ページ1`という対象名を固定)。**上記10のa・bを実装すると両方落ちる。** 意図(足さない・選ばない/人の宣言が下流に残る)は残し確かめ方を変える提案あり。**先に報告済みの「面積の基準が未回答なら出さない」テスト2件と、後述のページ種類宣言のテスト1件と合わせて合計5件の食い違い(下記キュー0の06:57続きの位置を参照)。**
    12. 入れる順番: ①器(`WorkScopeItem`/`ScopeDiffResult`、誰も呼ばない、判定は変えないので通ればマージ可)②`phase`を属性に(**判定が変わる、PRのまま待つ**)③建具表を`phase`ごとに分ける(**判定が変わる、PRのまま待つ**)④対応づけ・差分本体(②③が入ってから)⑤現況申告との噛み合わせ(申告が入ってから)⑥排他の組を下流で足さないことの固定⑦対応づけのAI推論(別作業)。
  - **① どこまで確定・どこが判断待ちか**: 確定事項は上記1〜6・8。判断待ち6件(下記「判断待ち」24〜29)はいずれも**実装の最初の一歩を止めない。**
  - **② 実装に入るときの最初の一歩**: 「器だけ作る」(順番①)。**ただし器だけでは差分は作れない。本体(順番④)の前に②③が必ず要り、②③はどちらも判定を変える変更なのでおーちゃんの承認が出るまで器から先へ進めない。** 再開する人はまずここを確認する。
  - **③ 実装の途中で困ると見ている箇所**: 1) `where`が取れない(室の輪郭が無い、代用は粗い、設計で消せない限界) 2) 対応づけをAIが推論する経路がまだ無い(推論が入るまで人の申告が無いページは全部`unpaired`、黙ってページ順で対応させない設計なので壊れずに差分が出ないだけ) 3) 既存テスト2件との食い違い(上記11、勝手に書き換えない)。
  - **申し送り(優先順位1「寸法の数字を読む」スレッドへ)**: 対応づけの推論と、原則3-1の「寸法の数字と突き合わせて検算」は同じ「図面上の文字を位置つきで読む」経路に乗る。寸法の数字が読めれば対応づけの推論(同じ縮尺・同じ輪郭のページを結ぶ)も現実味が出る。
  - 他スレッドとの重なり: `docs/principles/`に新規1ファイルを足しただけ。既存ファイルは編集していない。`estimating/case_premises.py`(PR #30)・`intake/start_kit.py`(現況申告作成中)・`estimating/quantities.py`/`mapping.py`/`basis.py`(PR #29)は参照のみで変更なし。
  - **TODO(申し送り): `start_kit.md`冒頭の関連文書リスト(「以下の2つ」)へのリンク追加が未了。** 再開時の宿題として残す。
- **【2026-09-22 06:55 CI成功】PR #33のCI成功(06:51:33〜06:54:43、約3分)、main との衝突なし。** テストの件数はこの環境から読めなかったが、変更は文書1ファイルのみなのでmainと同じ(座標変更なし)。**おーちゃんに見てほしいのは文書11節の6件で、番号と「これで」だけで答えられる形。**
- 次の一手: おーちゃんの確認・判断待ち6件(判断待ち24〜29)への回答を待つ。実装は②③の承認が出るまで着手しない(優先順位再編によりいまは⏸停止中)。

### キュー9(新規・2026-09-22 06:51おーちゃん承認・最優先): 図面に書かれた寸法の数字を読む

- 何のための作業か: 06:40の原則再定義で「基準点が無くても図面に書かれた寸法の数字から計算してよい」と決まったが、その数字を読む経路がリポジトリに1つも無い(違反一覧6番、重みが増したもの)。基準点が無いページで計算できるものが実質ゼロのままで、キュー8(差分設計)の対応づけ推論にも要る。優先順位1番。
- スレッド名: Opus 5 スレッド。thread `cmsg_01XUHdxe1bsBZCx8utd2aYfMQYo34Wg34eS2nnDSTemN2M`、session `cse_01Uq5e5LX6p6nNpKbRAJLBYX`
- 状態: 🟢着手中(中間報告あり、下記)。
- 優先順位再編(06:51)により、キュー7(OCR)と合わせて2本ルールの枠内で最優先で進める。どちらか終わったら次(ページの種類の宣言修正)へ。
- **【2026-09-22 07:09 中間報告】** main `55f3475` から分岐、ブランチ `claude/read-dimension-text`、コミット `0026d7f`。全件テストの基準は752件パス(このコミットでの正確な件数は次の報告待ち、テスト14件追加)。
  - **着手前の事実確認(記録と一致): 寸法の数字を読む経路はリポジトリに1つも無かった。** `axes/image_axis/`にあったのは表・面積ラベル・表題欄の縮尺表記のみ。
  - 新規: `axes/image_axis/pdf_dimensions.py` — 数字を位置・向きつきで拾い、両端に目印(寸法補助線・矢印)のある線の区間と対応させる。**対応が取れない数字は落とす。** 単位が書かれていない数字は「建築図面として成り立つ縮尺になる側」だけで mm と m を見分ける(1000倍違いで、成り立つ幅は1000倍より狭いため両方成り立つことは起こりえない)。**縮尺はページ内の寸法どうしの一致から出す(1件だけでは主張しない、平均も取らない)。**
  - 新規: `axes/reading/meaning.py` に意味の4欄。**`purpose_link`は目的の受け皿がまだ無かった時点の実装のため「目的未受領」の明示の印を置き`is_complete`をFalseにした。これは原則2「意味が付けられない値は数量に使わない」を緩めている。→ 下記「判断待ち30」に追加。** 目的の受け皿はその後、原則スレッドがPR #35で main に入れた(`e4eaa65`)ため、いまは乗り換えられる状態。コーディネータからこのスレッドへその旨を伝達予定。
  - `arbitration/method_policies.py` に `pdf_dimension_text` / `pdf_dimension_scale` を `calibrated=False` で登録(登録簿に2行追加)。
  - 次の一手: 入口(`intake/drawing_intake.py`)に接続。①基準点が無いページでも寸法から縮尺を出す ②人が入れた基準点を寸法の数字で検算し、合わなければ警告(原則3-1末尾) ③表題欄の印字だけに頼った長さは「仮説に基づく」に落とす。
  - 他スレッドとの重なり: `axes/image_axis/`をOCRスレッド(キュー7)も触っているが新規ファイル2つのため今のところ衝突なし。`method_policies.py`の登録簿は追記のみ。プッシュ前にmainを取り込み直す予定。
- **【2026-09-22 07:39 完了・PR #39(マージせず待機)】優先順位1番、完了。** main `b956474`から分岐・取り込み直し済み、ブランチ`claude/read-dimension-text`。全件**830→895件パス(1件スキップ、OCRの実機テスト)**。
  1. `axes/image_axis/pdf_dimensions.py`(新規)。数字を位置・書き方向つきで拾い、両端に目印(寸法補助線・矢印)のある線の区間と対応させる。**対応が取れない数字は落とす。** 単位無しの数字は成り立つ縮尺になる側だけでmm/mを見分ける。**ページの縮尺は一致が2件以上ないと出さず、平均も取らない**(一致した読みのうち平均に最も近い実在の読みを採る)。許容差の既定値はこの層に置かず入口が渡す。
  2. 入口に接続。①基準点も表題欄の縮尺も無いページで縮尺が出て開き戸の抽出まで進む。②**人が入れた基準点を寸法で検算し、合わなければ`human_input_vs_dimension_disagreement`として警告しどちらも採らない**(原則3-1末尾が初めて実装された)。③記入された寸法そのものを意味の4欄と根拠つきの数量として出す。
  3. **不具合を1件直した**: 最初の版は建具表のページで幅・高さの数字を**升目の罫線**と対応づけ、升目の長さをその数字の長さとして読み、**そのページの縮尺を丸ごと誤って開き戸の抽出を止めていた。** 既存の通しテストが落ちて発覚。表の位置は`pdf_tables.find_tables()`に聞く形に直し回帰テストで固定。
  4. 壊し試験8通りすべて検出。**うち2通りは最初の壊し方では落ちず、テストが判別できていなかったのでテストを直した**(報告書5節)。
  5. 報告書`docs/dimension_text_reading_report.md`、design_v8 **18章**(OCRが17章を使ったので繰り下げ)。
  - **判断待ちに追加: PR #39のマージ可否。** 判定のしかた(階層条件・許容差・群合計・質問の選び方)は変えていないが、**縮尺の決め方に3つめの読みが増えるのでページの結論は変わる**(縮尺が新しく出るページ/新しく食い違いが出るページが生じる)。どちらも安全側だが判断に近いためPRのまま、おーちゃんに確認中。→ 下記「判断待ち33」参照。
  - 「表題欄の印字だけに頼った長さを仮説に基づくにする」は**この PR に入っていない**(既存の由来の付け方を変えるので別PRが筋)。入れる場所は`_door_arc_findings`と`_reference_dimension_findings`の`pdf_text_scale`の2箇所。
  - 既存の判断待ち(`purpose_link`が埋められない=原則2緩め、`phase`不明時の扱い)は**推奨は今のまま**。
  - Codex側で測る5件: 対応の適合率・再現率/単位の取り違え/印字との一致率とどちらが正しかったか/人が指す2点の誤差/寸法が多いページの実行時間。
  - 申し送り: `intake/drawing_intake.py`・`arbitration/method_policies.py`・`docs/design_v8.md`・`axes/reading/meaning.py`で衝突4件を解決。**意味の4欄はOCRスレッドとこちらで同じものを2つ書いてしまった**(mainに先に入ったOCR側を採用、こちらの版は破棄)。`_resolve_scale`はOCRの`printed_origin`と寸法の両方を受ける形。
  - **⚠ 実案件P011ではこの実装は1件も効かない**(34ページ全部がスキャン画像で文字データのあるページが無い)。合成PDFでしか確かめておらず実図面での誤り率は未知。
- 次の一手: おーちゃんのPR#39マージ可否の判断待ち。優先順位1・2番とも完了したため、次の作業(優先順位4番以降、または新規)の着手判断を待つ。

### キュー10(新規・2026-09-22): P011での比較実験(A/B読み取り)の準備

- 何のための作業か: P011(実案件、v2ファイル。上記09:37の訂正参照)を使い、読み方の違いによる差を比較実験として計測する準備。**採点基準のコミットハッシュをstatus.mdに残すのがおーちゃんの指示。**
- **【2026-09-22 10:10 準備完了】**
  - 採点基準の最初の版: マージ`97bf73d`(実装`864a3b3`、PR #50)。`docs/experiments/ab_reading/output_format.md`と`scoring_rules.md`。
  - **改訂**: マージ`ddf3296`(実装`bed9c1b`、PR #51)。**改訂の理由**: 図面から数量が決まらない行でも、工事があること自体は見つけられる。そこは読み方の差が出るところなので、**工事の言い当ての分母からは外さない(数量の一致の分母からは外したまま)。** 工事の言い当ては3つの数字を必ず出す(全行/図面から決まる行/決まらない行)。**実行を1回も始める前の改訂で、結果を見てからの変更ではない。**
  - **OCRの部品をCIに入れる変更**: マージ`3b5a9f1`(実装`4017833`、PR #52)。今までスキップだった1件が毎回走る。CIのジョブ時間は3分12秒→2分48秒。追加ダウンロード約40MB、置き場所約83MB増。**報告書にあった「415MB」は数え方の誤りで、画像処理ライブラリの重複を含んだ数字だった。**
  - **但し書き**: CIで走るのは「エンジンが口に収まっている」「位置がページの中に収まっている」の2つだけで、当たり外れの証拠ではない。日本語モデルは人が取ってくる必要があるので、2モデルを突き合わせる経路はCIでは動かない。また「871件・スキップ0」はCIの画面から直接読んだ数字ではなく、同じ部品構成を手元で回した実測とジョブが緑だったことが根拠。
  - 入力パッケージは`/mnt/project-files/ab_reading_input_P011/`(リポジトリ外なのでコミットされない)、実行の出力先は`/mnt/project-files/ab_reading_runs/`。実行はAとBを交互に1回ずつから始めている。
- 次の一手: A/B実行の結果報告を待つ。

### キュー1: 本番の入口(最小構成)

- スレッド名: Opus 5 スレッド(実図面を読む本番の入口)
- 状態: **完了。本体・追加依頼(スタートキット)とも main にマージ済み。**
- 使用したコミット: 実装 `d8414e8` → main 取り込み `b02f204` → マージ `c2ad7dc`(PR #7、main の基点は `a8682c0`)
- 完了したこと(本体): 依頼の7項目すべて。全件テスト 575→601件、全件パス(マージ前に main を取り込み直して再度601件で確認済み)。壊し試験5通りで検出を確認。群合計スレッド(キュー4)とのファイル単位の重なりは無し(`arbitration/` で触ったのは `inference_orchestrator.py`〈証拠に任意の根拠フィールドを追加のみ〉と `method_policies.py`〈新しい手法2件の登録のみ〉)。マージ判断の根拠: 階層条件・許容差・群合計・質問の選び方には触れていない。
- **所見(本体実装時点、通しテストで固定済み): この入口を通しても確定する数量は0件。**
  - 不具合ではない。階層1は独立した強いデータ源2つ以上を求め、独立性はファイル内容の指紋で判定されるため、1つの図面PDFから何手法で読んでも独立数は1にしかならない。つないだ2手法(縮尺読み・開き戸検出)はどちらも未校正でハード制約に入らない。
  - どちらかの手法を「校正済み」に変えるとこの通しテストが落ちるよう固定されている。
  - **【訂正・下記の追加依頼で判明】「図面1枚だけでは階層1に到達しない」という当初の結論は誤り。人の入力(スタートキット)は図面とは別のデータ源であり、これで2つ目の独立したデータ源ができる。** 確定が起きない本当の理由は「データ源が1つしかない」ことではなく「今ある手法がどれも未校正である」こと。→ 判断待ち3を参照(内容も更新)。
- 拾えなかったもの(そのまま記録): 引戸・折戸は円弧を描かないため検出0件(合成図面に引戸2つを描き、期待値0で固定)。室の輪郭からの面積計算は未実装(面積25項目中22項目がここで止まる)。スキャンページの中身は未読で「未対応」として記録に残る。
- Codex側で測るべき項目として `docs/production_intake_report.md` に記載済み: `pdf_text_area` の校正、開き戸手法の適合率・再現率(独立データで)、34ページの実行時間とメモリ。
- **追加依頼(2026-09-22 03:31、おーちゃんより): スレッドを再開し、入口の関数が「スタートキット」を任意の入力として受け取れるようにする。実装完了、main にマージ済み(PR #14)。**
  - 内容: ①基準点(ページ番号、図面上の2点の座標、その間の実際の長さmm、縦横それぞれ1組まで) ②ページの種類(平面図/展開図/建具表/仕上表/設備図など、および現況/計画/解体の区別) ③現況と計画の対応(どのページ同士が同じ範囲を表すか)。
  - 扱いのルール: 前提は必須にしない(無ければ従来どおり自動処理)。基準点から求めた縮尺は縮尺表記や寸法の数値とは独立した読みとして証拠にし突き合わせ、食い違えば判断待ちとして記録。人の入力もそれだけを根拠に自動確定させない。画面は作らず関数の入力として受け取るところまで。
  - 使用したコミット: 実装 `eebcef7`(ブランチ `claude/intake-start-kit`、main `6d25a75` から分岐、最新main `f522664` 取り込み済み)。PR #14。
  - 完了したこと: 基準点・ページの種類(現況/計画/解体を含む)・現況と計画の対応・面積の選択の4つを任意入力として受け取る。画面は作っていない。前提を渡さなければ従来どおり動く(既存の通しテスト23件を1行も変えずに通過)。基準点から求めた縮尺と印字の突き合わせ、食い違いの判断待ち記録、建具表と宣言されたページでの円弧探索の抑止、現況/計画を対象名に残す処理を実装。テスト **601→625件** 全件パス、壊し試験5通りで検出を確認。
  - **独立性の絵が変わった(前述の訂正の根拠): 人の入力は図面とは別のデータ源なので、2つ目の独立したデータ源ができた。いま確定が起きないのは、2つの手法(基準点由来の縮尺・印字由来の縮尺)がどちらも未校正だからというだけ。**
  - **裏返しの危険(そのまま記録): `human_reference_point` と `pdf_text_scale` を校正済みに変えた瞬間、人が1回入れた値と図面の印字が一致しただけで階層1(自動確定)に到達する。** おーちゃんの「人の入力もそれだけを根拠に自動確定させない」という指示に反するため、両方を未校正で登録してテストで固定。登録簿と入口の両方を校正済みに変えるとそのテストが落ちることを壊し試験で確認済み。
  - 但し書き(そのまま記録): 基準点から求めた縮尺と表題欄の印字は、2点の座標を共有している。独立なのは縮尺の部分だけで、座標の入れ間違いは両方に同じように効くのでこの突き合わせでは捕まらない。証拠に載せてテストで固定済み。
  - 許容差は新しい数値を作らず、ファイアウォールが長さに使っている暫定±5%を借用。
- **マージ済み(2026-09-22 03:51): PR #14 → main `f05bf20`(main の基点は `f522664`)。CI・全件テスト625件とも通過。マージ判断の根拠: 機能の追加だけで、階層条件・許容差・群合計・質問の選び方には触れていない。既存の通しテスト23件が1行も変わらず通っており、前提が任意であることの裏づけになっている。**
- **`intake/` の形が変わった(キュー2へ申し送り済み): `to_orchestrator_request()` の引数が「1件のDrawingFinding」から「同じ対象の読みの列(Sequence)」に変わり `sources={"drawing": (id, 指紋), "start_kit": (id, 指紋)}` を渡す形に。`DrawingFinding` に `source_kind`/`axis_id`/`strength`/`derivation`/`derivation_basis` を追加。**
- 残っていること: 画面は未着手で、人がどうやって前提を入力するかは未定(→判断待ち8)。現況と計画の対応は記録するだけで、差分(撤去・新設)の数量は作っていない。
- Codex側で測るべき項目を追加: 「人が指す2点の位置の誤差」(±5%の許容差が妥当かがここで決まる。→判断待ち9)。
- 次の一手: 完了。追加作業は無し。

### キュー2: 建具表・内装仕上表の構造化

- スレッド名: Opus 5 スレッド(ブランチ `claude/schedule-table-reading`、main `c2ad7dc` から分岐)
- 状態: **完了。main にマージ済み(マージコミット `bee3aaa`、PR #13)。**
- 使用したコミット: 実装 `89b3608` → PR #14取り込み `eb15178` → 記述修正 `d7bfca7` → マージ `bee3aaa`
- **PR #14との衝突3ファイルの解決: `intake/drawing_intake.py` はmainの版を採って表の読み取りを上に載せ直し、`to_orchestrator_request()` の新しい形(同じ対象の読みの列+`sources`)と`DrawingFinding`の新フィールドに合わせた。建具表の数量は `source_kind="drawing"` のまま(図面から読んだ値なので人の入力とは別のデータ源にしない)。main側の「人が『建具表』と宣言したページでは開き戸を探さない」はそのまま活きる。`method_policies.py` は両側の2行ずつを残し4つとも `calibrated=False`。`docs/design_v8.md` は14-2節を先に、15章を後に。建具表の2手法も未校正のまま登録。**
- 完了したこと:
  - 着手前の事実確認: **文字を位置つきで読む経路はリポジトリに1つも無かった。** `pdf_pages` も `find_area_labels` も平文(`get_text("text")`)で、行と列の対応が最初から存在しない。面積2ラベル固定で流用不可。
  - 新規2モジュール: `axes/image_axis/pdf_tables.py`(罫線の升目をセル座標つきで返す。縦結合セルと空セルを区別)、`axes/image_axis/schedule_tables.py`(列の役割を見出しの文字から決める。内装仕上表は「室名/部位/仕上」と「部位そのものが列」の2通りに対応)。
  - 入口に接続。建具番号ごとの数量を `建具数量::<番号>` として根拠つきで仲裁層へ。**開き戸の円弧の対象とは別にし、突き合わせはしない**(→ 判断待ち7)。
  - 全件テスト **601→625(キュー1追加依頼取り込み後)→680件**、全件パス。CI(test)成功。壊し試験8通りすべてで検出を実測(最初は2つ見逃していたのでテストを足した)。報告書 `docs/schedule_table_reading_report.md`、v8 15章。
  - 意味: 引戸・折戸は円弧を描かないので図形からは原理的に拾えないが、建具表の種別の文字が読めれば拾える。P011の新設建具は全部これだった。`IntakeResult.arc_blind_doors()` が該当する建具番号を返す。
- **独立性の記述を追補後の事実に訂正済み: 「図面PDFは1ファイル=1データ源だから確定しない」はもう正確でないため、報告書4節とv8 15章を「確定が起きないのは手法がどれも未校正だから」に書き換え済み。**
- そのまま記録する限界: スキャンページからは何も読めない。**P011は34ページ全部スキャンなので、この案件の実図面では建具表も内装仕上表も1件も読めない。** 罫線の無い表、ページをまたぐ続き表、建具表に載っていない建具も拾えない。**合成の表でしか確かめていないので実図面での誤り率は未知。**
- Codex側で測るべき項目(報告書6節)として記載済み: ①`pdf_table_door_schedule` の校正(升目の取り違え率/建具表に載っていない建具の割合) ②見出し候補の網羅度 ③キャプション24pt閾値 ④wide/longの割合 ⑤34ページの実行時間(`find_tables()` がページごとに罫線を探すので入口全体の時間が変わる)。
- 判定のしかたは変えていない(`arbitration/` で触ったのは `method_policies.py` の登録簿2行だけ。群合計スレッド・抜き取り監査スレッドとも重なり無し)ので、自走ルールの「マージしてよいもの」に該当。
- メモリに `jidou-sekisan-schedule-tables` を追加済み(コーディネータより)。
- 次の一手: 完了。追加作業は無し。

### キュー3(新規・2026-09-22 04:42おーちゃん追加): 数量化と見積の行への対応付け

- スレッド名: Opus 5 スレッド。thread `cmsg_01XUHdxe1bsBZCx8utd2aYfM53NXAfp3hW1eJpaw4BvqT5`、session `cse_01FXnJLB5hdfXZ1fkna8jArv`
- 状態: **🟢着手(優先順位変更により最優先)。着手前調査完了、失敗テストを書く段階へ。**
- 使用したブランチ: `claude/estimate-line-mapping`(main `f005394` から分岐)
- 依頼内容:
  - 表などから得た数量を、見積の行(取付費、材料費など)に当てはめる仕組み。
  - 当てはめが一意に決まらない場合は候補として出し、確定しない。
  - 当てはめの規則(会社の積算ルール)はコードに埋め込まず、差し替えられる設定として持つ。
  - **詳しい仕様はCodexが一般的な形で作成中。届いたら反映する。それまでは入力と出力の形の設計を先に進める。**
  - テストは合成データ。機能の追加なので、全件テストとCIが通ればマージしてよい(おーちゃんの取り決めどおり)。
- 変更の理由(おーちゃんの言葉、そのまま記録): 実図面(三井D)の分析で、すべての見積項目が最後に「数量化」と「見積の行への対応付け」を通ることが分かった。今ここが無いため、読み取りが進んでも見積の1行として出力できない。一方、キラークエスチョンは、読み取りが候補を出すまで効き目が小さいことも分かった。
- **着手前調査で確かめた事実(コードを実走した結果、記録ではない):**
  - 合成PDFで `read_drawing()` を実走。出てくる数量は4種類のみ — `開き戸::ページN`(箇所)、`建具数量::<番号>`(箇所)、`専有延床面積`/`施工床面積`(㎡)、回答があれば `施工対象床面積`。
  - **判定は全件 tier=3 / requires_review / confirmed_range=None で自動確定は0件**(理由コード `empirical_calibration_missing_or_failed` と `tier1_requirements_not_met`)。内装仕上表の行は数量ではない(室の輪郭を取る実装が無いため)。建具表の種別は `DoorScheduleRow.kind` にある。
  - **見積の行の語彙は `benchmarks/run_golden_eval.py` にすでにある**(code / work_item / major_category / unit / expected_source_type)ので、新語彙を作らずこれに合わせる方針。
- **設計方針(Codex仕様が来たときの作り直しを小さくする順):**
  1. 新パッケージ `estimating/`。**`intake/` は1行も触らない**(他スレッドとの衝突回避)。アダプタは `estimating/from_intake.py` 側に置く。
  2. 中立の `QuantityItem`(kind/key を `::` で分割、単位、属性、tier/action、根拠)→ 規則 → `MappingResult`。仕様変更が効きやすい「規則の書式」だけを `estimating/rules.py` のローダに閉じ込める。
  3. 規則はJSONの外部ファイル。**リポジトリには合成の見本だけ置き、実際の会社の積算ルールは外のパスを設定で渡す。**
- **安全側の約束(失敗テストを先に書く前提、そのまま記録):**
  - 見積の行が確定するのは「規則が一意」**かつ**「数量自体が仲裁層で確定している」の両方が成立したときだけ(今日は後者が常に偽なので**確定は0件**)。
  - 複数の規則が当たったら束(取付費+材料費のセット)ごとの候補として出し確定しない。
  - 同じ見積の行に複数の数量が当たっても**足さず**衝突として人へ回す(入口の「ページをまたいで足さない」を打ち消さないため)。
  - 単位の次元が合わない規則は行を作らず理由を残し、勝手に換算しない。
  - 規則が当たらなかった数量は黙って捨てず `unmapped` に理由付きで残す。
- **【2026-09-22 05:03 コミット報告】main `1a53096` を取り込み、実装 `1fe51c8`(ブランチ `claude/estimate-line-mapping`)。PR #21(CI待ち)。**
- 完了したこと:
  - 新規パッケージ `estimating/`(quantities / rules / mapping / from_intake + 合成の見本規則JSON)。**`intake/` は1行も触っていないので他スレッドとの重なりはゼロ。既存の判定のしかたも変えていない(機能追加のみ)。**
  - **確定の条件を3つに割った:** ①規則が一意 ②数量そのものが仲裁層で確定 ③その行に他の数量がぶつかっていない。①だけで確定させると「規則がきれいに書けている」という単一の指標だけで自動確定することになるため避けた。③は入口の「ページをまたいで足さない」を当てはめ側から破らないため(既存と新設の開き戸が同じ行に当たったら足さずに人へ回す)。
  - **この経路から出る確定は0件**(②が常に偽)。`test_nothing_is_settled_through_the_whole_path` で固定、入口側の同名テストと対。
  - 規則はJSONの外部ファイル。`format_version` が違うファイルと知らない項目は**断る**(Codex仕様が来たとき半分だけ効くのを防ぐ)。
  - テスト新規28件、全件 **680→708件、全件パス**(3分30秒)。壊し試験8通りすべてで落ちることを実測。報告 `docs/estimate_line_mapping_report.md`、設計v8 16章。
- **【2026-09-22 05:07 完了報告】PR #21 を main にマージ済み(マージコミット `9a9d05f`、土台は main `1a53096`)。状態: 完了・マージ済み。**
  - マージ根拠: おーちゃんの元指示に「機能の追加なので、全件テストとCIが通ればマージしてよい」と明記あり。差分は**1,926行の追加のみ・削除0行**で、既存の判定のしかた(階層条件・許容差・群合計の判定・質問の選び方)は1行も変えていない。既存ファイルへの変更は `pyproject.toml` にパッケージ名を1行、`docs/design_v8.md` に16章を足しただけ。CIの`test`成功、mergeable clean。
  - 全件テスト 680→**708件、全件パス。** 壊し試験8通りすべてで検出を実測。
- 拾えていないこと(そのまま記録): 実案件では1件も動いていない(P011は全ページスキャンで入口が数量を出さない)。内装仕上表から行は作れない。単価・金額は出さない。**見積の行の側から見た抜け漏れは検出しない。**
- 残っていること: Codexの詳しい仕様が届いたら反映(変わりやすいのは規則の書式だけで `estimating/rules.py` の読み込みに閉じ込めてある)。未実装: 数値条件(幅900以上など)、歩掛・係数、規則の優先順位、1つの行に複数の数量をまとめる書き方。判断待ち13〜15、Codex側で測るべき項目は上記のとおり。詳細は `docs/estimate_line_mapping_report.md` 10・11節。
- 次の一手: 完了。次に動くのはCodexの仕様が届いたとき(このスレッドへ渡す)、またはおーちゃんの判断が返ってきたとき。

### キュー4(旧キュー3): 抜き取り監査を階層1へ拡大

- スレッド名: Opus 5 スレッド
- 状態: **【2026-09-22 07:04 完了・main マージ済み】**
- 使用したコミット: テスト `98c4759` → 実装 `7b88054`(ブランチ `claude/tier1-audit-sampling`、main `c2ad7dc` から分岐、PR #10)
- 完了したこと:
  - 階層ごとに抜き取りの割合を別々に決められるようにした。監査は階層ごとに1回ずつ走り、対象件数・抜き取り件数・的中率・見逃す確率を階層ごとに分けて出す。記録にも階層が残る。
  - **階層をまたいだ的中率は出せない作り**(まとめて出す入口を作っていない)。「階層を混ぜると的中率が薄まる」という前提は、的中率を1つの数字にまとめる場合だけ成り立つため、分けて数えるかぎり問題は起きない。
  - 既定値は変更なし。階層2は30%・最小5件のまま、階層2だけを渡したときに選ばれる対象が1件も変わらないことをテストで固定。階層1の割合はコードに入れず、PR本文で10%・最小3件を提案のみ(実測の裏づけは無く、「人が一度も見ない階層を完全に無検査にはしない」という方針だけが根拠)。→ 判断待ち4。
  - 先に落ちるテストを書いてから実装(最初のコミットはテストのみで正しく落ちる状態)。壊し試験7通りで検出を確認。全件テスト623件パス(最新のmain取り込み後、キャッシュを消して実行)。
  - 他作業との重なり: 群合計制約のブランチと重なるのは設計書1ファイルのみで、編集箇所は離れている(あちらは10章25項あたり、こちらは3-4節と10章末尾)。コードの重なりは無し。
- **うまくいっていない点(そのまま記録): この監査はいまも本番の処理から一度も呼ばれていない。** 部品として階層1にも使える形になっただけで、実際に監査される件数は階層1も階層2も0件のまま。運用上は何も増えていない。→ 判断待ち5。
- **【2026-09-22 06:24 追記・本番組み込みに着手(判断待ち3の回答)】** おーちゃんの回答「抜き取り検査を本番の処理に組み込んでください。まず設計を status.md に書いてください」を受け、このスレッドに設計を依頼。thread `cmsg_01XUHdxe1bsBZCx8utd2aYfMSirKiVcupcCfYpuL4J9xDJ`、session `cse_01MfihnCjY9CqGh3SrbGg91e`。**設計が先、実装は設計を送ってから**の順にしている。階層1の抜き取り率は10%・最小3件を初期値として設計に書き、実測の裏づけが無いことをそのまま明記する方針(判断待ち4・14参照)。正解の引き元(判断待ち15)は保留のため、「引けない間どう振る舞うか」を設計に含める。

- **【2026-09-22 06:31 設計届く】`docs/audit_production_integration_design.md`、ブランチ `claude/audit-production-design`、コミット `8807dd4`(main `de395e9` から分岐)。要点(そのまま転記):**
  - **0. この設計で数字は良くならない。** 組み込んでも当面は抜き取られる対象は0件のまま。原因は監査側ではなく判定側(図面PDFは1ファイル=1データ源で階層1条件を満たさず、いまの入口を通した対象は全部が階層3)。組み込みで今すぐ得られるのは、①母集団0件が「監査していない」と区別できる形で記録に残ること、②階層1・2が出るようになった瞬間に追加実装なしで検査が動き出すことの2つだけ。**それまでこの検査は0件を報告し続けるのが正しい出力。**
  - **1. どこで走らせるか**: `read_drawing()` の末尾(`decisions` が出そろってから `IntakeResult` を作るまでの間)。`InferenceOrchestrator.process()`/`AxisQualityFirewall.assess()`/`_decide()` は「1回の呼び出し=1対象」で母集団が常に1になるため落選。`estimating/` は判定の根拠(手法ID)を持たないため落選。**1案件ぶんの判定が全部そろう場所はここしかない。** 案件をまたぐ監査は今回実装せず、記録の形を決めるところまで。
  - **2. 母集団**: `auto_confirm`(階層1)と `provisional_audit`(階層2)、階層3は入れない。PR #10 の `run_tiered_audit()` を階層ごとに使う。抽出率: 階層1=**10%・最小3件**(実測の裏づけ無し、根拠は「人が一度も見ない階層を無検査にはしない」の1点のみ)、階層2=30%・最小5件(既存のまま)。カテゴリ(拡大単位)は手法ID。**監査で広げるのは対象であって、自動で階層を下げることはしない。**
  - **3. 正解の引き元(記入者が決まるまで保留)**: 抽出(`plan_tiered_audit()`、正解に触らない)と照合(`score_audit_plan()`)を関数として分離。既存 `run_tiered_audit()` はこの2つの合成として残す。理由: `read_drawing()`の「正解データは一切読まない」という約束を壊さないため、および `renovation-golden-001.json` の「抽出と採点の経路を分ける」方針(保留中の申し送り事項)と同じ形にするため。正解の引き口は `GroundTruthSource` プロトコル(`lookup(case_id, target) -> int | None`)、**既定は常に None を返す実装。** 正解が引けない間: `hit_rate` は `None`(1.0を返さない)、`status` は `awaiting_human`(母集団0件・全部一致とも区別)。**出力は「的中率」ではなく「人が確かめるべき対象の一覧」**(対象名・採用範囲・ページ番号・座標・元の文字列つき)= 原則5の「現地確認が必要」そのもの、回答8(システムが根拠を付けて提案し人が確認)に当たる。**正解データは別途作るのではなく、この一覧に人が答えることで溜まる。** 人が「一致」と答えても階層1に上げない・「不一致」でも1件で手法を止めない(**監査は階層を変えない**)。同じカテゴリの全件を監査対象として挙げるだけで、止めるかは人が決める。
  - **4. 結果の保存**: `AnswerStore` と同じ形で案件情報はリポジトリに置かない。`IntakeConfig.audit_log_path: Path | None = None`(既定None、Noneなら保存しない)。JSON追記。残す内容: 案件ID・時刻・**図面内容のsha256**・**人の前提の指紋**・乱数シード・階層ごとの(母集団・抽出件数・率・最小件数・状態・抽出対象)・1件ごとの対象名・採用範囲・単位・手法ID・**根拠(ページ番号・座標・元の文字列)**。**乱数シードは案件IDと図面の指紋から決め、時刻や実行回数を混ぜない**(同じ図面を読み直したとき一覧が入れ替わって確認済みの分が捨てられるのを防ぐ)。母集団0件のときは「監査対象なし」と書き、「0件監査、全部一致」とは書かない。`IntakeResult.audit_plan` と `summary()` に1行。
  - **5. 誰がいつ見るか**: 「質問」(答えないと数量が出ない)と「確かめてほしい一覧」(数量は出ているが確認してほしい、答えなくても進む)を混ぜない。原則5末尾の「人の負担は操作回数と時間で測る」から、止まる質問は少なく確認は後回しにできる形。原則5の4区分そのものの実装は本設計の範囲外(監査結果がどこに入るかだけ決める: 未確認→現地確認が必要/一致→区分は変わらない/不一致→同じカテゴリ全件を寄せる提案を出し、寄せるかは人が決める)。一覧が変わらない限り再通知しない。
  - **6. 実装の段取り**: ①`plan_tiered_audit()`/`score_audit_plan()` を分けて追加(`run_tiered_audit()` は合成として残す、**PR #10の上に積む**) ②`GroundTruthSource`と`NoGroundTruth` ③`IntakeConfig.audit_log_path`と記録の書き出し ④`read_drawing()`末尾に抽出を差し込み`IntakeResult.audit_plan`に載せる ⑤`summary()`に1行。各段階とも再現する失敗テストを先に。壊し試験3点: 母集団0件のとき「0件監査、全部一致」と報告しないこと/正解が無いとき`hit_rate`が`None`であること/同じ図面・同じ前提で2回読んだとき抽出対象が変わらないこと。
  - **7. 階層1の10%を後で測る項目(Claude Codeでは測らない、Codex側)**: ①階層1の誤り率(階層2と比較) ②検出力(実際の母集団・抽出件数で誤り1件を50%以上捕まえられるか) ③人の負担(抽出件数×1件の確認時間)。採否の判定基準は測る前に宣言してコミット。基準値は母集団が0でなくなってから決める。
  - **8. この設計で解決しないこと**: ①母集団0件のため検出力は当面0 ②1案件だけでは検出力不足(案件横断の積み上げは今回対象外) ③正解の記入者が未定 ④階層1の10%に実測の裏づけ無し ⑤原則5の4区分は未実装 ⑥**監査は階層を変えない**(不一致でも自動で階層を下げない。人1人の確認も単一の根拠であるため)。
  - **コーディネータの指示: 実装に進ませた。** 階層1の10%・最小3件はおーちゃんの回答14で承認済みのため基準値の無断変更に当たらず、全件テストとCIが通ればPR #10とあわせてマージしてよい。既存の判定のしかたが変わる修正が混ざったらそこだけPRのまま残す、と伝達済み。
- **【2026-09-22 06:54 実装完了・停止指示により保持(続きの位置、そのまま記録)】** 中断前に設計の段取り①〜⑤が全部実装済みになっていた。ブランチ`claude/tier1-audit-sampling`、HEAD `d452907`(最新main `55f3475`取り込み済み)、PR #10、**マージせず保持。** 設計のブランチ`claude/audit-production-design`(`8807dd4`)は中身がPR #10に取り込み済みで実質不要(消していない)。**全件テスト802件パス**(`rm -rf __pycache__`+`PYTHONDONTWRITEBYTECODE=1`)。CIは`d452907`の回がこれから走る。
  - **① 実装の到達点=段取り①〜⑤は全部完了:**
    1. 抽出と照合を関数として分割。`plan_tiered_audit()`(正解に触らない)と`score_audit_plan()`(照合だけ)。1階層ぶんは`plan_provisional_audit()`/`score_tier_plan()`。`run_tiered_audit()`は合成として残り、**結果も呼び出し口も変わっていない**(回帰テストで固定)。
    2. `GroundTruthSource`プロトコルと`NoGroundTruth`(常にNoneを返す既定)。
    3. `IntakeConfig.audit_log_path`と`append_audit_record()`(`AnswerStore`と同じ形。**リポジトリの外、既定値なし、Noneなら書かない**)。
    4. `read_drawing()`の末尾に`_audit_candidates()`→`plan_tiered_audit()`を差し込み、`IntakeResult.audit_plan`に載せた。**シードは`audit_seed(case_id, 図面の指紋)`で時刻を混ぜないので、読み直しても一覧が入れ替わらない。**
    5. `IntakeResult.audit_line()`を`summary()`に追加。**母集団0件のときは「監査対象なし」と書き、「全件一致」とは書かない。**
    - 設計に無かったが実装して分かって足したもの: `provenance["page_numbers"]`(根拠の形が読みの種類ごとに違うので、どのページを見ればよいかを1欄にまとめた)。
    - テストは`tests/test_audit_plan_split.py`(14件)と`tests/test_intake_audit.py`(14件)。**失敗テストを先に書き、壊し試験10通りですべて落ちることを実測。** 階層1・階層2が出たときの振る舞いは、仲裁層の代役を差し込んだテストで固定(いまの経路では全対象が階層3になるので実物では試せないため)。
  - **② 次の一歩=実装ではなく「マージするかどうか」の判断。** コードとしてやることは残っていない。判定のしかたには触れておらず、階層1の10%・最小3件は回答14で承認済み。**中断指示で止めてあるだけ。**
  - **③ 申し送り:**
    - **いまは検査される対象が0件のまま。不具合ではなく、そう報告するのが正しい出力。** この状態は、優先順位1「寸法の数字を読む」と2「スキャンの文字読み取り」が進んで独立した第2の情報源が生まれるまで変わらない。**逆に、その2つが動いた直後に、この検査は追加実装なしで走り始める。**
    - **PR #29/#30との接続点が1つ未配線。** `estimating/basis.py`の「現地確認が必要」は理由が空だと名乗れない作りなので、**抜き取った一覧を`QuantityItem.site_survey_reason`に渡せば、検査の出力がそのまま見積の行に立つ。** 配線には`estimating/from_intake.py`が`IntakeResult.audit_plan`を読む必要があり、そこは原則スレッドが触っているファイルなので別作業として切り出してある。**これが再開時のいちばん自然な続き。**
    - 階層1の10%・最小3件は実測の裏づけが無い。測るのはCodex側(①階層1の誤り率②検出力③人の負担)。採否の判定基準は測る前に宣言してコミットする約束で、基準値は母集団が0でなくなってから決める。
    - **案件をまたぐ検査は未実装。** 1案件の階層1・階層2は10〜20件なので、それだけでは誤り1件を半分以上見逃す。記録は積み上げられる形(図面の指紋と前提の指紋入り)にしてあるが、いつ走らせるか・誰が受け取るかは未定。
    - 正解データの記入者は保留のまま。決まるまで`hit_rate`は`None`、`status`は`awaiting_human`。
    - **他スレッドとの衝突は無し。** 触ったのは`arbitration/provisional_audit.py`と`intake/drawing_intake.py`と自分のテスト。面積の基準が未回答のときに数量を出さない件は一切触っていない。
- **【2026-09-22 07:04 マージ完了】main `d6aeda0`を取り込んだ`6126512`→**マージコミット`90fae24`(PR #10)。**
  - **抜き取り検査が本番の処理から走るようになった**(図面を読み終えて判定が出そろった時点で抜き取る)。抽出と照合を分けたので、**入口は正解データに一切触らない。** 正解の引き先は保留のままで既定は「常に引けない」実装。検査の状態は「人の確認待ち」になり**的中率は出ない。出るのは人が確かめてほしい対象の一覧**で、どのページを見ればよいかまで添える。
  - 記録は案件の情報として**リポジトリの外にだけ書く。パスを渡さなければ書かない。対象が0件だった回も記録に残す**(残さないと「検査していない」のか「検査して対象が無かった」のか区別がつかないため)。**同じ図面を読み直しても抜き取られる対象は入れ替わらない。**
  - 階層1の割合は承認どおり**10%・最小3件**。階層2の30%・最小5件は変更なし。**既存の判定のしかたには触れていない。**
  - **全件テスト802件が通りCIも緑。** PR本文の「758件」は書いた時点の件数で、最新main取り込み後は**802件**(マージコミットにその旨を記載)。
  - **いまは検査される対象が0件**(図面PDFは1つの情報源なので自動確定が出ないため)。不具合ではなく、そう報告するのが正しい出力。
- **⚠️【おーちゃんとの約束、目立つ形で記録】検査が実際に動き始めたら(最初に1件以上検査したら)、その結果をstatus.mdに書いて報告する。** 寸法の数字とスキャンの文字が読めるようになって自動確定が出はじめた瞬間に最初の1件が出る(キュー9・キュー7が鍵)。**検査の正解をどこから引くかは引き続き保留。**
- 次の一手: **完了。** 次に動くのは、キュー9(寸法読み取り)・キュー7(OCR)が進んで自動確定が出はじめ、監査対象が1件以上出たとき(上記の約束どおり報告する)。

### キュー5(旧キュー4): 群合計制約の修正

- スレッド名: 別スレッド(group-total-masking)
- 状態: **完了(実装・CIとも)。おーちゃんの判断待ちで未マージのまま保持。**
- 使用したコミット: `c3d053192ed9d560e40ee0272b326b50a7f3dfb5`(ブランチ `claude/group-total-masking`、PR #6)
- 完了したこと:
  - 実装済み。PR #6 の CI(`test`チェック)成功、**587件全件パス(143.74秒)**。手元と別環境で同じ件数が出たことを独立に確認。
  - PR は `mergeable: clean`(main と衝突なし)。件数書き出しステップも動作し、要約に passed/failed/error/skipped/合計の表が出る。
  - PR説明文にあった「このリポジトリにはPRに対して走るCIがない」の一文は、別スレッドでCIがmainに入ったため事実でなくなり削除済み。
  - **中心値検査の凍結数値が変わった件の切り分け完了。原因は「両方」だった:**
    - ①計測スクリプトの数え方のずれ: 「人が確かめる要素」を「変数登録されていない要素」で数えていたため、停止要素を変数登録すると恒久的に0になる不具合。19要素・シード1で 0.8947→0.3158、数え方の修正だけでは 0.6842 までしか戻らない。
    - ②実装側の本物の欠陥: 停止した要素が読み取り由来の狭い定義域を持ったままで、未校正の読み取りがクラスタ等価を通って他の要素を誤った値に確定させていた(台所群の例: 停止要素の誤読(3,3)が kitchen_2(正解4)を(3,3)に潰していた)。`detection_range` は矛盾検出の向きしか直していなかった。
  - 対処: 検出の向きで分けた。誤りが人へ回る向きでは読み取りの狭い範囲を使い、黙って確定させる向きでは何も主張しない。
  - 3シード×3条件で9点すべてが `71420fd` 以上。**ただしこれは9点の標本であり効果の測定ではない。** 効果の測定(`71420fd` 対 修正後、同一環境・同一評価データ)は設計メモ9章に「Codex側で測るべき項目」として記載済み。
- 残っていること: おーちゃんの帰還後、Codex側で効果測定を実施。測定が終わるまでマージしない。
- **申し送りの件は解決済み(2026-09-22 04:10): 「`claude/group-total-masking` がCI設定より前のmainから枝分かれしていて、そのまま取り込むと設定が消える」という懸念は事実ではなかった。** 片側だけにあってもう片側が触っていないファイルは、取り込みで消える動きにはならない。最新main(`bee3aaa`)を取り込み直し(ブランチ先頭 `2718162`)、衝突ゼロ、`.github/workflows/pytest.yml` は残っている。全件テスト**692件すべてパス**(手元、約3分57秒)。プッシュ済み・**PR #6は未マージのまま**。
- **取り込み後のCI確認済み(2026-09-22 04:13): PR #6 の `test` チェック成功、`mergeable: clean`、土台は最新main `bee3aaa`。** PR説明文の「587件」は取り込み前(枝分かれ時点)の数字のまま残っていたため、「取り込み後は692件すべてパス」と補足するコメントをPRに追加済み。数値の食い違いは説明文の記載漏れであり、テスト結果そのものに矛盾は無い。
- 次の一手: 記録係はこのキューについて追加の作業はしない。おーちゃんの判断(マージするか)を待つのみ。
- **【2026-09-22 07:12 main再取り込み(90fae24まで)】** 抜き取り監査の階層1拡大(PR #10)がmainにマージされた後の状態を取り込み直し。衝突は`docs/design_v8.md`1か所のみ(両スレッドが10章28項に別内容を書き足していたため)。**両方残し、PR #6側を30項に振り直した。**main側の28項・29項はそのままのため`intake/drawing_intake.py`の参照はずれていない。**コードの衝突なし。** 全件テスト**842件すべてパス(約3分59秒)**。ブランチ先頭 `80558ede75c06d5758f365c43e1627d2a6dfc0`。**マージはしていない**(Codexの固定ベンチマーク測定待ちの扱いは変わらず)。CI結果はこれから。

### キュー6(旧キュー5): 質問選定の高速化(設計案とテストまで)

**【2026-09-22 04:42 おーちゃんの指示により保留】** キラークエスチョンは読み取りが候補を出すまで効き目が小さいと分かったため、優先順位を下げて保留。以下はその時点(自己訂正・追加実測まで反映済み)の内容で止めたもの。追加作業は行わない。

- スレッド名: Opus 5 スレッド(ブランチ `claude/killer-question-speedup-design`、main `f05bf20` を取り込み、コミット `c735186`、PR #15)
- 状態: **完了。設計案・テストとも完了。高速化そのものは実装していない。PR #15 は未マージのまま保持。**
- **設計案の柱: 「決定性とどうぶつかるか」を先頭に配置。規則: 「使い回してよいのは、答えが1つしかない問いだけ。矛盾の中身を読むところは、これまでどおり毎回まっさらな状態から解く。」** 根拠は、候補を評価する経路が矛盾の中身を一度も読んでいないことと、読んでいる値(矛盾の有無、各要素の最小値・最大値)が数学的に一意であること。
- テスト19件(9.4秒)。質問の順番を突き合わせる仕組みと、設計案が寄りかかっている性質(つながりを越える制約は無い/絞り込んだ範囲は何度解いても同じ/片方のまとまりへの回答は他方を変えない)を1つずつ固定。将来の候補実装ができたら1行差し替えで同じ突き合わせが走る。**全件テスト644件パス。**
- **自己訂正2件(そのまま記録):**
  1. 「定数だけの制約がまとまり限定で落ちると矛盾が見逃される」は測ったら誤り。今の作りではそういう制約はそもそも作れず必ずエラーになる。要求は「落とすな」ではなく「黙って飛ばさず、今と同じようにエラーにしろ」に修正。
  2. 幅0のテストで条件の作り方を間違えてスコアが動いたので作り直した。
- **目標に届くとは書いていない。** 1,000要素は1問目に127秒(Codex実測)で目標の5秒から2桁離れている。
- **【2026-09-22 04:19 追加実測・自己訂正2件】** 質問を1問選ぶ時間の実測(今→試作)。
  - 全体が1つのかたまり: 16要素 5.4→4.3秒(1.3倍)、24要素 16.1→13.3秒(1.2倍)、32要素 37.7→30.1秒(1.3倍)。
  - かたまりが分かれている: 16要素 4.6→0.6秒(7.2倍)、24要素 14.6→1.1秒(12.9倍)、32要素 32.6→1.6秒(20.8倍)。
  - **訂正1: 「範囲の計算を軽くするのが最大の削りどころ」は誤りだった。** 時間の6〜7割がそこに使われているのは本当だが、その大半は式を組み立てる時間ではなく解く時間で、組み立てを減らしても解く回数は変わらない。3〜4倍を見込んだのは、組み立ての回数が3乗で伸びているのに引きずられた読み違い。
  - **訂正2(重要): 全体が1つのかたまりになる形では、いまの3案では目標に届かない。** 127秒を1.3倍で割っても約98秒で、目標5秒に20倍足りない。**群合計の制約が1本入るだけで全要素が1つのかたまりになる。群合計はこのプロジェクトの中心にある仕組みなので、実案件はこちらの形に近いと考えられる。** 前回「分からない」と記載していた箇所は、実測により「この形では届かない」という結果に更新。**逆に、かたまりが分かれている形では20倍前後の効果が出ている。**
- 残っていること: 無し(記録係としてはこの追加実測の反映で完了)。重い計測はCodex側で測るべき5項目として書き出し済み。採否の判定基準の案も先に書いてコミット済み。
- 決定性との衝突を避けられた根拠(試作で確認済み): 候補を評価する経路は矛盾の中身を一度も読んでおらず、読んでいるのは「矛盾するかしないか」だけ。読んでいる値(矛盾の有無、各要素の最小値・最大値)は数学的に一意。まとまりごとに区切って評価する試作で、今のエンジンと質問の順番もスコアも一致(独立・鎖・星・まとまり複数の4構造×2サイズ、9通りまで一致)。
- 連鎖構造の実測(Z3コンテキストの再構築コスト): 4要素0.19秒、8要素0.92秒、16要素5.7秒、24要素16.9秒、32要素37.7秒(倍増ごとに約6〜7倍)。32要素でsolverの再構築が約52万回。
- 参考情報(おーちゃんより。Codexがリポジトリ外で試作・測定、コミット`71420fd`、コードは未変更): まとまり限定の試作で7種類の構造すべて完全一致。30要素14秒→4.2秒、50要素80秒→11秒、100要素打ち切り→42秒。残り時間の62〜74%は範囲の計算(`_tighten`/`_optimize`)、Z3コンテキストの作成は7〜17%。
- 次の一手: 完了。追加作業は無し。おーちゃんの判断待ち(PR #15をマージするか、下記判断待ち10〜12)。

### キュー(追加・2026-09-22 06:17着手): CI非推奨警告の解消

- スレッド名: Sonnet 5スレッド。thread `cmsg_01XUHdxe1bsBZCx8utd2aYfMFbhpWFe1v22FWvsgSkTy81`、session `cse_01PtmUWPNZLFatLHwCMSfEJr`
- 状態: **完了・main マージ済み(マージコミット `de395e9`)。**
- 内容: `actions/checkout@v4→v7`、`actions/setup-python@v5→v7`。Node.js 20非推奨警告2件への対応。`ubuntu-latest`の移行告知(2026-10-19からUbuntu 26)は案A(何もしない)のまま対応せず、最終確認だけ判断待ち23に残す。**CIの中身(走るテスト、件数の書き出し)は変えていない。**
- 完了したこと: 修正コミット `aa7dfe9` → マージ `de395e9`。CIグリーン(testジョブ成功、3分9秒)を確認したうえで、check runの注釈でNode.js 20の警告2件が実際に消えたことを確認してからマージ。
- 残っていること: 無し。`ubuntu-latest`移行の最終確認のみおーちゃんの判断待ち(判断待ち23)。
- 次の一手: 完了。追加作業は無し。

### キュー7(新規・2026-09-22 06:20おーちゃん承認): スキャン図面から文字を読む(OCR)

- 何のための作業か: 「次にやるべきことの提案」1番目。P011は34ページ全部スキャンで、キュー1(表題欄)・キュー2(建具表・仕上表)の実装が実案件では1件も効いていない状態を解消するための追加。
- おーちゃんの回答(そのまま記録): 「スキャンの図面から文字を読めるようにする作業を始めてください。判定を変えない機能追加なので、テストが通ればマージしてよいです」
- スレッド名: Opus 5 スレッド。thread `cmsg_01XUHdxe1bsBZCx8utd2aYfMAb8z5j6wxpy5XvzsPCo8Hm`、session `cse_01SY67hMoS93mFjxcAQFixXB`
- 状態: 🟢着手(コーディネータより着手済みと報告)。
- マージ判断の根拠(あらかじめ記録): 判定のしかた(階層条件・許容差・群合計の判定・質問の選び方)を変えない機能追加であれば、全件テストとCIが通ればマージしてよい。既存の判定のしかたを変える提案が混ざる場合はPRのまま待機。
- **状態: 【2026-09-22 06:55 実装完了】完了・PR #34(CI実行中)。優先順位2番として継続担当。**
- 使用したコミット: main `de395e9` から分岐、実装`2ee2e50`(ブランチ`claude/ocr-scanned-pages`)。
- 完了したこと:
  - 新規: スキャンページをOCRで読む層(`axes/image_axis/ocr_text.py`)、既存と同じ正規表現で縮尺・面積を拾う層(`ocr_readings.py`)、RapidOCRの差し込み口(`ocr_backends.py`)、意味の4欄(`axes/reading/meaning.py`)、入口への接続(`IntakeConfig.ocr`)。
  - 手法`ocr_text_area`/`ocr_text_scale`を**未校正・上限weak**で登録。**OCRだけでは自動確定しない**(テストで固定)。
  - 全件テスト**708→748件パス**(+1 skip、OCRエンジン未インストールのため)。壊し試験8通りで全部落ちることを確認。実図面は使わず、テストのスキャンは合成(PyMuPDF内蔵の日本語フォントなので、日本語フォントの無いCIでも同じ絵になる)。
  - **実測の要点(重要): 中国語・英語のモデルは数字を正しく読んで日本語の語を化けさせ(種別→种别が確信度0.99)、日本語モデルは語を読んで数字を壊した(1650→16.0)。確信度は文字化けを止めない。** 2つのモデルを突き合わせると**通った語の誤りは0件、ただし29語中7〜8語しか通らない。** 詳細は`docs/ocr_scanned_pages_report.md`。
  - 重なる箇所(申し送り): `intake/drawing_intake.py`(`_area_findings`の鍵を`(ラベル, 手法ID)`に変更、`DrawingFinding`に`meaning`を追加、`_extract`の戻り値が6要素に)/`arbitration/method_policies.py`(2行追加)/`axes/image_axis/pdf_vector_symbols.py`(`parse_scale_text`/`parse_area_text`を公開関数として切り出し、判定は変えていない)/`docs/design_v8.md`(**17章を末尾に追加**。16章は見積の行の当てはめが使用中)。
  - CIが緑になり次第マージ(判定を変えない機能追加で、おーちゃんから明示の許可あり)。
- **判断待ちに追加(コーディネータより): 「何に基づくか」の4値はこのPRでは作っていない(既存の2値read/assumedのまま)。** `queue3_redesign.md`8節の設計と重なるため二重に作らなかった。**実際には原則スレッドが`estimating/basis.py`としてPR #29で入れ済みなので、乗り換え作業として残る**という整理。コーディネータから当該スレッドへ伝達済み。
- **【2026-09-22 07:13 完了・main マージ済み】優先順位2番、完了。** マージコミット `b093f1c`。CI緑、全件**870件パス(+1 skip、OCRエンジン未インストールのため)**。
  - 完了: スキャンページのOCR読み取り、意味の4欄、未校正の手法として登録(自動確定は起きない)、報告書`docs/ocr_scanned_pages_report.md`、design_v8 17章。
  - 残り: **スキャンの建具表を「表」として読む経路**(上記「次にやるべきことの提案」7番、このスレッドの次の推し)、`estimating/basis.py`の4値への乗り換え、実図面での計測(Codex側、報告書7節)。
  - main取り込み時、抜き取り監査の階層1拡大(PR #10)と`intake/drawing_intake.py`で**4か所衝突**(import/`IntakeConfig`の欄/`IntakeResult`の欄/`IntakeResult(...)`の引数)。**どれも別機能の追加だったため両方を残す形で解決、判定のしかたには触れていない。** 解決後に全件テストで確認済み。
  - **落とし穴として記録: PRに衝突があるあいだ、GitHub Actionsは1本も走らない**(`pull_request`のマージ参照が作れないため)。今回、衝突を解決するまで新しいheadのcheck runが0件だった。「CIが始まらない」ときはまず衝突を疑う。
- **【2026-09-22 07:17 続きの位置(現物を確かめて停止)】**
  1. **スキャンの建具表を「表」として読む経路(次の一手)**: 意味づけは作り直し不要。建具表・仕上表を読む処理は升目(`TableRegion`)を受け取るだけの形で、PDFを開いているのは入口の1関数だけ。**同じ升目を画像から組み立てて渡せば、その先は今のまま動く見込み。** OpenCVは既に入っており追加導入は不要。最初の一手は「画像から罫線の格子を作り、OCRの文字を升目に割り振る」。**止まる可能性が高いのは表の作りではなく文字の読み取りの質**(建具番号・種別〈日本語〉・幅高さ〈数字〉が同時に要るが、日本語モデルは数字が壊れ、中国語モデルは日本語の語が化けるためどちらでも1行が揃わない)。**⚠ 見出しの文字が1つでも化けると、その表はまるごと「建具表ではない」として黙って落ちる**(見出し「建具番号」の一致が条件、落ちた理由はどこにも残らない。実測で見出し「高さ」は全条件で一度も返らなかった)。**着手するなら「読めなかった」と記録が残る形に直すのが先。**
  2. **4値(何に基づくか)への乗り換え — 思っていたより小さい**: OCRで読んだ値も数量に直す段階で自動的に4区分が付く(`estimating/quantities.py`)。OCRは未校正登録なので自動確定せず「推論に基づく」止まり。残りは2点: そうなることを固定するテストが無い/意味の4欄が数量側では覚え書きの中の文字列で持ち回されており正式な欄になっていない。
  3. **Codex側の実図面計測に渡すときの前提**: 合成した紙で測った数字で、**誤り率の測定ではない**(29語の小標本)。2モデル突き合わせで誤り0件だが**通るのは29語中7〜8語**。実図面でこの割合がどうなるかが最も知りたい数字。日本語モデルは配布元から別途取得が必要で既定環境に無い(エンジン一式で環境が415MB増)。300dpiで測定、A1・A0での所要時間は未測定。前処理(傾き直し・二値化)は一切かけていない状態の数字。測ってほしいこと5件は`docs/ocr_scanned_pages_report.md`7節。
  - このスレッドはここで停止済み。
- 次の一手: このキューは完了・停止。乗り換え作業(basis.py対応)・建具表の表読み取りは別途着手時に新規キューとして記録。

## 次にやるべきことの提案(キュー5完了により確定)

コーディネータから、おーちゃんが戻ったときの材料として次の4観点を挙げるよう指示があった。

1. **スキャンページから文字を読む処理(OCR)。** P011は34ページ全部スキャンで、建具表・仕上表の実装(キュー2)も表題欄の読み取り(キュー1)も実案件では1件も効かない。いま実図面に対して最も効く追加はこれ。→ **【2026-09-22 06:20承認・着手済み】キュー7参照。**
2. **手法の校正。** 確定が起きない理由は独立性ではなく「どの手法も未校正」に変わった(キュー1・2で判明)。校正を通す仕組みと、その前に「人の入力と印字が一致しただけで確定してしまう」ことをどう防ぐか。→ 判断待ち3(回答あり、現状は両方未校正のまま維持)。
3. **室の輪郭を取る実装。** 面積25項目中22項目がここで止まっている。→ 未着手のまま。
4. **抜き取り監査を本番の処理に差し込む**(判断待ち2・5)。いまは階層1・2とも実際の監査件数0。→ **【2026-09-22 06:24着手】設計に着手済み。キュー4の追記参照。**
5. **CIワークフローの非推奨警告2件を片付ける。** → **【2026-09-22 06:24完了・マージ済み `de395e9`】** `actions/checkout@v4→v7`、`actions/setup-python@v5→v7`。Node.js 20の警告2件は消えたことを確認済み。`ubuntu-latest`のUbuntu 26移行(2026-10-19)は案A(何もしない)のまま、最終確認だけ判断待ち23として残す。
6. **【2026-09-22 06:45追加・原則スレッドの提案】図面に書かれた寸法の数字を読む経路を作る。** 06:40の再定義で「基準点が無くても寸法の数字から計算してよい」と決まったが、読む手段が無いので効かない。ベクターのページでも読めていないので、スキャンの文字読み取り(キュー7・OCR)とは別に必要(違反一覧6番、重みが増したもの)。→ **【2026-09-22 06:51承認・優先順位1番・着手済み】キュー9参照。**
7. **【2026-09-22 06:55追加・OCRスレッドの提案】スキャンの建具表を「表」として読む経路が無い(罫線を画像から見つける実装が必要)。** P011でいちばん効くのはここ。`schedule_tables._as_door_schedule()`は`TableRegion`を受け取る純粋な関数なので、画像から`TableRegion`を作れれば意味づけはそのまま使える。→ 未着手。

## 判断待ち(おーちゃんの帰還後)

### 1. 群合計制約の修正(PR #6)をマージするか

- 何を決めてほしいか: 実装・CIとも完了、9点の標本では悪化なしだが、これは効果の測定ではない。Codex側での効果測定(`71420fd`対修正後)を待たずにマージするか、測定完了まで待つか。
- 推奨: 測定完了まで待つ(現状の扱いのまま)。
- 理由: 既存の判定のしかたを変える変更であり、自走ルールで「マージせずPRのまま確認を待つもの」に該当する。
- **【2026-09-22 06:20 おーちゃんより回答】** 「群合計の修正は、凍結済みの合格基準でCodexの固定ベンチマークを測ってからマージします。今はCodexが止まっているので、プルリクエストのまま待機してください」→ **PR #6は保持継続。Codex側の測定を待つ。**

### 2. 抜き取り監査を本番経路に組み込むか(キュー3完了後も未解決、5と同一の論点)

- 何を決めてほしいか: 抜き取り監査(階層1・階層2とも)は本番処理から一度も呼ばれておらず、キュー3で階層1に対応する部品を作った後も監査される件数は両階層とも0件のまま。差し込むならどこで走らせるか、正解をどこから引くかが未定(PR #10本文と設計書に記載)。
- 選択肢: A. 別作業として本番経路への組み込みを行う / B. 現状のまま(部品としてのみ存在)を維持する
- 推奨: A(別作業として切り出して組み込む)。監査が存在するだけで運用上は何も検査されていない状態が続くため。
- 理由: キュー3は「階層1・階層2を別々に設定できるようにする」という設計変更が主眼で、本番経路への接続は別の検証(どこから呼ぶか、既存の確定フローとの順序)が要るため。
- **【2026-09-22 06:20 おーちゃんより回答】** 「抜き取り検査を本番の処理に組み込んでください。まず設計を status.md に書いてください」→ **A(組み込む)で確定。着手済み(キュー4の追記参照、設計が先・実装は設計を送ってから)。**

### 3.【訂正】どの手法を「校正済み」に切り替えるかの判断(旧: 次にどの独立データ源へ進むか)

- 何を決めてほしいか: 当初「図面1枚だけでは階層1に到達しない」という構造上の限界だと理解していたが、キュー1の追加依頼(スタートキット)で訂正された。**人の入力(基準点)は図面とは別の独立データ源であり、2つ目の独立データ源はすでに存在する。** 確定が起きていないのは「基準点由来の縮尺」と「印字由来の縮尺」がどちらも未校正だから。どちらか(または両方)を校正済みに切り替えるかどうかの判断が必要。
- 選択肢: A. 現状のまま両方未校正で維持する(人の入力だけを根拠に自動確定させないというおーちゃんの指示を厳格に守る) / B. 一定の条件(例: 複数案件での一致率が十分高い)を満たしたら校正済みに切り替える基準を作る
- 推奨: (記録係としては提案しない。校正済みへの切り替えは「人の入力だけを根拠に自動確定させない」という指示の範囲に関わる判断のため、おーちゃんの帰還後に決めていただく項目)
- 理由: 校正済みに変えた瞬間に階層1へ到達する設計になっており(「裏返しの危険」としてキュー1に記録済み)、誤って切り替えると指示に反する自動確定が起きる。
- **【2026-09-22 06:20 おーちゃんより回答】** 「原則に反している所の修正に入ってよいです。ただし、既存の判定のしかたが変わる修正はプルリクエストにして待機してください。意味の欄や『何に基づくか』の欄を追加するだけの修正はマージしてよいです」→ **手法を校正済みに切り替えることは「判定のしかたを変える変更」に該当するため、この回答の対象外(明示的な校正の可否判断は保留のまま)。着手してよいのは意味欄・Basis欄の追加のみ。**

### 4. 面積の選択(専有延床か施工床か)を誰がいつ答えるのか

- 何を決めてほしいか: 現状は面積の選択を人がJSONに置く前提だが、回答を集める画面も訂正の経路も無い。
- 推奨: (記録係としては提案しない。UI/運用設計に関わるため、おーちゃんの帰還後に決めていただく項目)
- 理由: キュー1の入口実装の前提(「案件ごとに1回だけ質問して使い回す」)を運用でどう成立させるかという判断。
- **【2026-09-22 06:20 おーちゃんより回答(判断待ち8と同一の回答)】** 「人が前提を入力する画面は、今は作らなくてよいです。3方式の比較ができる段階で改めて扱います」→ **画面は作らない。3方式(A/B/混合)の比較ができる段階まで保留。**

### 5. 群合計制約の修正(キュー4)と抜き取り監査の階層1拡大(キュー3)、どちらもPRのまま保持している理由の再確認

- 何を決めてほしいか: キュー4は既存の判定を変える変更のため自走ルールで明確にマージ対象外。キュー3は既定値を変えていないため自走ルールの文面上はマージしてよい部類に見えるが、コーディネータの判断で未マージのまま保持されている(判断待ち2で述べた「監査が本番から呼ばれていない」という点が理由と考えられる)。この整理が正しいか、キュー3をマージしてよいかを確認してほしい。
- 選択肢: A. キュー3(PR #10)をこのままマージする / B. 判断待ち2(本番組み込み)の方針が決まるまで保持する
- 推奨: B(保持を継続)。マージ自体は安全だが、「監査が機能しているように見えて実際は0件」という状態を本人の確認なしに main に固定してしまうため。
- 理由: 記録係としては安全側の判断を優先した。
- **【2026-09-22 06:20 おーちゃんより回答】** 判断待ち1・2への回答(群合計はCodex測定待ちで保留、抜き取り監査は本番組み込みへ着手)のとおり、**どちらもPRのまま保持を継続**(PR #6はCodex測定完了まで、PR #10は本番組み込み設計・実装が終わるまで)。

### 6. 表に単位が書かれていない寸法の扱い

- 何を決めてほしいか: 建具表・内装仕上表の寸法は現状mmに直さず、印字された文字列だけを残している。
- 選択肢: A. 現状のまま(単位を確定しない) / B. シート全体から「単位:mm」の記載を探す / C. 案件ごとに人へ1回聞く
- 推奨: C(担当スレッドの推奨)。Bは取り違えると1000倍ずれるため危険。Cは面積の選択(判断待ち4)と同じ仕組みが既にある。
- 理由: 誤って1000倍ずれた値をハード制約に入れるリスクの方が、人に1回聞く手間より重い。
- **【2026-09-22 06:20 おーちゃんより回答】** 「単位が書かれていない寸法は、mm と仮定した『仮説に基づく』扱いにし、基準点との突き合わせで確かめてください」→ **推奨Cではなく、mm仮定+仮説based+基準点突き合わせの方式に確定。**

### 7. 建具表の数量と平面図の開き戸検出数が食い違ったときの扱い

- 何を決めてほしいか: 現状は建具表由来の数量(`建具数量::<番号>`)と開き戸の円弧検出を別々の対象として出すだけで、突き合わせをしていない。突き合わせを入れるなら仲裁層の判定のしかたを変える変更になる。
- 選択肢: A. 現状のまま(別対象として出すだけ) / B. 突き合わせを実装する(判定のしかたの変更として別途PRで提案)
- 推奨: (記録係としては提案しない。仲裁層の判定を変える設計判断のため、おーちゃんの帰還後に決めていただく項目)
- 理由: 自走ルールで「既存の判定のしかたを変える変更」はマージせず確認を待つ対象と定められている。
- **【2026-09-22 06:20 おーちゃんより回答】** 「建具表の数と平面図の検出数が食い違った場合は、どちらかを選ばず、食い違いとして人に示してください」→ **A(現状の「別対象として出すだけ」に加え、食い違いとして人に示す表示を追加)で確定。突き合わせて自動で選ぶ実装は行わない。**

### 8. スタートキットを人がどうやって入力するか

- 何を決めてほしいか: 画面は未着手で、今は関数の引数としてしか受け取れない。判断待ち1(面積の選択を誰がいつ答えるか)と同じ問題。
- 推奨: (記録係としては提案しない。UI/運用設計に関わるため、おーちゃんの帰還後に決めていただく項目)
- 理由: 判断待ち1と合わせて、まとめて運用設計を決める方が効率的。
- **【2026-09-22 06:20 おーちゃんより回答】** 「人が前提を入力する画面は、今は作らなくてよいです。3方式の比較ができる段階で改めて扱います」→ **画面は作らない。3方式(A/B/混合)の比較ができる段階まで保留(判断待ち4と同一の回答)。**
- **合わせて回答: 人が先に入力するものの範囲について** 「人が先に入力するものを基準点だけに絞るのは、混合方式を既定にする形で今から設計に入れてください。3方式は切り替えられるままにしてください」→ **混合方式(基準点だけ人が先、残りはAIが先)を既定として設計に入れる。A/B/混合の3方式は切り替え可能なまま維持(→判断待ち18も参照)。**

### 9. 基準点(人が指す2点)の位置の誤差をCodex側で測る

- 何を決めてほしいか: 基準点から求めた縮尺の許容差は、ファイアウォールが長さに使っている暫定±5%を借用しているだけで、実測の裏づけが無い。人が指す2点の位置の誤差を測る必要がある。
- 推奨: Codex側で測るべき項目として記録し、実測してから許容差を見直す。
- 理由: おーちゃんの新ルールにより、重い計測はClaude Codeでは行わない。
- **【2026-09-22 06:20 おーちゃんより回答】** 「2点の誤差の計測は、画面ができてから Codex 側で行います。保留にしてください」→ **保留。前提入力の画面ができてから測る(判断待ち4・8の画面着手待ちと連動)。**

### 10. 質問選定の高速化(キュー5)で「1問あたり5秒」に1問目を含めるか

- 何を決めてほしいか: 暫定目標は「1問あたりP95 5秒以内」だが、1,000要素では1問目だけで127秒かかる(Codex実測)。1問目を目標の計測に含めるかどうかで、目標に届いているかの判定が変わる。
- 選択肢: A. 1問目も含める(現状の目標のまま、127秒は届いていないと判定) / B. 1問目を除く(2問目以降だけで判定)
- 推奨: (記録係としては提案しない。目標設定そのものに関わる判断のため、おーちゃんの帰還後に決めていただく項目)
- 理由: 採否の判定基準を事前に決めてから計測するというおーちゃんのルールに関わる。
- **【2026-09-22 06:20 おーちゃんより回答】** 「20〜22. 質問選定の高速化は、引き続き保留です」→ **保留継続。判断は行わず、キュー6(旧キュー5)は止めたままの内容を維持。**

### 11. まとまりが1つになる形(群合計の制約が入る実案件に近い形)を目標の対象にするか

- 何を決めてほしいか: キュー5の設計案が最も効くとしている「まとまりに分けて計算する」案は、群合計の制約が1本入って全部が1つのまとまりになると効果がほぼゼロ(実測1.2〜1.3倍)になる。実案件はその形に近い可能性がある。この形を高速化の目標に含めるかどうかで、設計の優先順位が変わる。
- **【2026-09-22 04:19 追加】実測が出た: 全体が1つのかたまりの形では、いまの3案では1,000要素・目標5秒に対し20倍届かない(127秒→約98秒)。かたまりが分かれている形では20倍前後の効果が出ている(実測、32要素で32.6→1.6秒)。** これは「分からない」から「この形を目標に含めるなら、まとまり分割以外の対策(範囲計算の作り直しなど)を優先しないと届かないことが実測で確認された」への更新。
- 推奨: (記録係としては提案しない。設計方針の判断のため、おーちゃんの帰還後に決めていただく項目)
- 理由: 含めるなら「まとまりに分ける」以外の対策(範囲計算の軽量化など)を優先する必要があることが、実測により裏づけられた。
- **【2026-09-22 06:20 おーちゃんより回答】** 「20〜22. 質問選定の高速化は、引き続き保留です」→ **保留継続。**

### 12. 危険の大きい案(まとまり間の評価結果の使い回し等)に着手するか

- 何を決めてほしいか: キュー5の設計案の3段階(①まとまり限定 ②まとまり間の評価結果の使い回し ③範囲計算の軽量化)のうち、②はまだ試作段階で未検証。着手するかどうか。
- 推奨: (記録係としては提案しない。実装の優先順位に関わる判断のため、おーちゃんの帰還後に決めていただく項目)
- 理由: 設計案は実装しない前提で作られており、着手は新たな作業キューの立ち上げになる。
- **【2026-09-22 06:20 おーちゃんより回答】** 「20〜22. 質問選定の高速化は、引き続き保留です」→ **保留継続。着手しない。**

### 13. 同じ見積の行に複数の数量が当たったときの扱い(キュー3新規)

- 何を決めてほしいか: 現状は足さず衝突として人へ回すだけ。既存と新設が同じ行に当たった場合など、スタートキットの現況/計画の区別につないで自動で分けられる余地がある。
- 推奨: スタートキットの現況/計画の区別につなぐ。ただし別作業として切り出す。
- 理由: 担当スレッドの推奨。今のキュー3の範囲を広げず、まず「足さない・人へ回す」安全側の動きを確立してから拡張する方が段階を踏める。
- **【2026-09-22 06:20 おーちゃんより回答】** 「同じ行に複数の数量が当たった場合は、自動で合計や選択をせず、食い違いとして示し、確定させないでください」→ **担当スレッドの推奨(現況/計画の区別につなぐ拡張)は採用せず、現状の「足さず食い違いとして示す」安全側の動きのまま確定。**

### 14. 行と数量の単位の次元が違うときに換算を許すか(キュー3新規)

- 何を決めてほしいか: 現状は単位の次元が合わない規則は行を作らず理由を残すだけで、換算はしていない。歩掛(係数)を使った換算を許すかどうか。
- 推奨: 許さないまま。歩掛は別の層として扱う。
- 理由: 担当スレッドの推奨。勝手な換算は判断待ち6(単位が書かれていない寸法の扱い)と同種の「取り違えると大きくずれる」リスクを持つ。
- **【2026-09-22 06:20 おーちゃんより回答】** 「同じ次元の単位換算(mm と m など)は許可します。次元が変わる換算(長さから面積など)は換算ではなく計算なので、必要な前提(高さなど)を明示し、前提が無ければ『仮説に基づく』にしてください」→ **同一次元の換算は許可(推奨どおり歩掛は別層のまま)。次元の異なる換算は「計算」として扱い、前提を明示、前提が無ければ仮説based。**

### 15. 積算規則ファイルを案件ごとに持つか、会社で1つにするか(キュー3新規)

- 何を決めてほしいか: 規則はJSONの外部ファイルとして設定で渡す作りになっているが、案件ごとに別ファイルを持つか、会社共通の1ファイルにするかは未定。
- 推奨: (記録係としては提案しない。運用設計に関わるため、おーちゃんの帰還後に決めていただく項目)
- 理由: 判断待ち8(スタートキットの入力方法)と合わせて、運用側の設計をまとめて決める方が効率的。
- **【2026-09-22 06:20 おーちゃんより回答】** 「規則のファイルは、会社で1つを基本にし、案件ごとの前提は別のファイルに分けてください。今の3層の設計どおりです」→ **会社で1つの規則ファイルを基本とし、案件ごとの前提(`CasePremise`)は別ファイルに分離。既存の3層設計のまま。**

### 16. 面積の基準が未回答のとき、両方の値で「仮説に基づく」行を2本出すか、今どおり出さないか(キュー0新規)

- 何を決めてほしいか: `drawing_intake.py:978` は面積の基準が未回答だと `施工対象床面積` を出さない。原則5「前提が揃うまで止めるのではなく、何に基づくかを区別して出す」の1行目と逆になっている。
- 推奨: 出す。ただし「仮説に基づく」の印と依存する仮説を必ず付け、確定には絶対に上げない。
- 理由: 担当スレッドの推奨。止めるより出すほうが原則5に合うが、確定への格上げを防ぐ仕組みが先に要る。
- **【2026-09-22 06:20 おーちゃんより回答】** 「面積の基準が未回答のときは、両方の値で『仮説に基づく』行を2本出してください。どちらも自動確定はさせず、どの仮説に依存しているかを記録してください」→ **推奨どおり確定(専有延床・施工床の両方を仮説based行として出す。依存する仮説を記録、確定への格上げは不可)。**

### 17. 「区画ごとの基準」の粒度(キュー0新規)

- 何を決めてほしいか: 原則3-1は「画像を区画に分けて、区画ごとに基準を持てる形にする」と定めるが、細かいほど歪みに強い一方で人のクリック回数が増え、原則5末尾の「負担は操作の回数と時間で測る」とぶつかる。
- 推奨: 既定はページ1組のまま、必要なときだけ区画を足せる形。
- 理由: 担当スレッドの推奨。
- **【2026-09-22 06:20 おーちゃんより回答】** 「区画ごとの基準は、まずページ単位の縦と横で始め、図面に書かれた寸法との突き合わせで食い違いが出た場合だけ区画を細かくする形にしてください」→ **担当スレッドの推奨を採用。既定はページ単位の縦横1組、寸法との突き合わせで食い違いが出たときだけ区画を細かくする。**

### 18. 原則4に反している3つ(ページの種類・現況と計画の対応・面積の選択)をいつ動かすか(キュー0新規)

- 何を決めてほしいか: 原則4「先に入力してよいのは基準点だけ」に対し、いま先に受け取っているのは4つ。基準点以外の3つ(ページの種類・現況と計画の対応・面積の選択)は条件を満たしていない。
- 推奨: 原則6の方式切り替えを実装するときにB方式側へ一緒に移す。それまでは任意入力のまま残し、原則4に反していると明記。
- 理由: 担当スレッドの推奨。
- **【2026-09-22 06:20 おーちゃんより回答】** 「人が先に入力するものを基準点だけに絞るのは、混合方式を既定にする形で今から設計に入れてください。3方式は切り替えられるままにしてください」→ **今から着手。混合方式(基準点だけ人が先)を既定として設計に入れる。担当スレッドの推奨(方式切り替え実装時にB方式側へ移す)より前倒しで、いま設計へ反映。**

### 19. 「見積書は提案書である」を設計上どこで表すか(キュー0新規)

- 何を決めてほしいか: 原則1「見積書は提案書である」を表す場所がまだどこにも無い(未着手)。
- 推奨: (記録係としては提案しない。判断材料が足りないため、おーちゃんの帰還後に決めていただく項目)
- 理由: 担当スレッドも判断材料不足として保留にしている。
- **【2026-09-22 06:20 おーちゃんより回答】** 「『見積書は提案書である』は、今は設計に入れなくてよいです。見積書を出力する段階で扱います。status.md に保留として記録してください」→ **保留(明記のとおり)。見積書の出力段階で扱う。**

### 20. 「現地確認が必要」を誰が付けるか(キュー0新規)

- 何を決めてほしいか: `Basis` の4値のうち「現地確認が必要」を、AIが付けるか規則ファイル側に書くかが未定。
- 推奨: 規則ファイル側に書き、AIには付けさせない。
- 理由: 担当スレッドの推奨。
- **【2026-09-22 06:20 おーちゃんより回答】** 「『現地確認が必要』は、システムが根拠を付けて提案し、人が確認する形にしてください」→ **担当スレッドの推奨(規則ファイル側に書く)ではなく、システムが根拠(ページ番号・座標・元の文字列など)を付けて提案し、人が確認する形に確定。**

### 21. 意味の4欄(what/where/phase/purpose_link)を必須にする時期(キュー0新規)

- 何を決めてほしいか: 意味の4欄を今必須にすると出る数量が0件になるため、目的(3-2)の実装が入るまで必須にできない。いつ必須化するか。
- 推奨: (記録係としては提案しない。目的〈判断待ち19に関連〉の実装順序と絡むため、おーちゃんの帰還後に決めていただく項目)
- 理由: 担当スレッドの設計見直しの前提として記載されているが、時期そのものは決めていない。
- **【2026-09-22 06:20 おーちゃんより回答】** 「意味の4欄は、新しく書くコードでは今から必須にしてください。既存のコードは、修正のついでに順に入れてください」→ **新規コードは今から必須。既存コードは修正のついでに段階的に追加(一斉移行ではない)。**

### 22. 前提(`CasePremise`)を差し替えたときに作り直す範囲(キュー0新規)

- 何を決めてほしいか: 仮説などの前提を差し替えたとき、どこまでを連動して作り直すか(依存する行だけか、関連する層全体か)が未定。
- 推奨: (記録係としては提案しない。設計判断のため、おーちゃんの帰還後に決めていただく項目)
- 理由: 担当スレッドの設計見直しの前提として記載されているが、範囲そのものは決めていない。
- **【2026-09-22 06:20 おーちゃんより回答】** 「前提を差し替えたときは、その前提に依存している行だけを作り直してください」→ **依存する行だけを作り直す(層全体の再構築はしない)ことで確定。**

### 23. CIの`ubuntu-latest`移行(2026-10-19)への対応(コーディネータ提案・2026-09-22 06:24追加)

- 何を決めてほしいか: `.github/workflows/pytest.yml` が使う `ubuntu-latest` は 2026-10-19 にUbuntu 26へ移行する。コーディネータの判断で案A(何もしない)を選んだが、最終的な可否はおーちゃんの確認待ち。
- 選択肢: A. 何もしない(移行時に自動追従、問題が起きたら対応) / B. 先に `ubuntu-24.04` などバージョンを固定する
- 推奨: A(コーディネータの判断どおり)。
- 理由: 移行まで時間があり、テスト内容(Python/pytest)への影響は現時点で不明。急ぎ対応する理由が無い。

### 24. 数が違うとき、排他の組で2本出すか「区分未定」1本にするか(キュー8新規・差分設計)

- 何を決めてほしいか: 現況3件・計画2件のように数が違うとき、`docs/principles/scope_of_work_diff.md`4-3節の提案どおり排他の組(`exclusive_group`)で「撤去1件」と「撤去3件+新設2件」を両方出すか、`区分未定`1本にまとめるか。
- 推奨: 排他の組で2本出す。
- 理由: `区分未定`1本にすると「撤去が3件あるかもしれない」という情報が消え、人が見積を組むときの手がかりが無くなるため。面積の回答16(両方出す)と同じ向きだが、こちらは行の構成そのものが変わるので組が大きくなる点に注意。

### 25. 「既存のまま」を見積の行として出すか、数量の一覧にだけ残すか(キュー8新規・差分設計)

- 何を決めてほしいか: 4-2節。「既存のまま」の工事内容を、見積の行として出すか、行にはしない一覧のみに留めるか。
- 推奨: 工事内容としては出し、見積の行にするかは規則ファイル側で決める(コードで決めない)。
- 理由: 会社によって「現況のまま=記載しない」と「現況のまま=確認済みとして記載する」が分かれるため。

### 26. 排他の組を、集計でどう数えるか(キュー8新規・差分設計)

- 何を決めてほしいか: `estimating/mapping.py`の`summary()`/`basis_counts_text()`は行を数えるので、排他の組が入ると数が実際より多く見える。組を1件として数えるか、内訳を含めて数えるか。
- 推奨: 組を1件として数え、内訳は組の中に畳む。
- 理由: 「100件のうち確定0件」という要約の分母が、排他の組のせいで膨らむのを避けるため。

### 27. 対応づけの前提が変わったときに、どこまで作り直すか(キュー8新規・差分設計)

- 何を決めてほしいか: 5-5節。回答10は「依存している行だけを作り直す」だが、対応づけ(ページの組)の前提が変わると差分の組み合わせ自体が変わるため、行だけでは足りない。
- 推奨: 対応づけの前提だけは、差分の突き合わせから全部やり直す。
- 理由: キュー3見直しの判断待ち(全部作り直し、変わった行だけを見せる)と同じ理由で、作り直しは安く、取りこぼしは高い。

### 28. 解体図しか無いときの撤去を、そのまま出してよいか(キュー8新規・差分設計)

- 何を決めてほしいか: 4-4節。現況も計画も無く解体図だけがあるとき、そこから読んだ数量を撤去として出してよいか。
- 推奨: 出す。基づきは「推論に基づく」にし、確定には上げない。
- 理由: 解体図は撤去が直接描かれた図面なので、現況からの差分より根拠が近い。

### 29. 現況の把握の申告が「部分によって混在」のとき、範囲をどの粒度で受け取るか(キュー8新規・差分設計)

- 何を決めてほしいか: 5-2節。ページ単位か、ページの中の区画かで、人の操作回数が変わる。
- 推奨: 既定はページ単位。区画は、差分が食い違ったところだけ後から足せる形にする。
- 理由: 基準点の区画についての回答(区画は食い違ったときだけ細かく)と同じ向き。原則5末尾「人の負担は操作の回数と時間で測る」と直接ぶつかるため、既定は粗くする。

### 30. 目的の受け皿が無かった間、`purpose_link`を「意味が付けられない」扱いにして緩めてよいか(キュー9新規・寸法読み取り)

- 何を決めてほしいか: 寸法読み取りスレッドが意味の4欄(`axes/reading/meaning.py`)を実装した時点では目的の受け皿がまだ無く、`purpose_link`に「目的未受領」の明示の印を置き`is_complete`をFalseにする形で原則2(意味が付けられない値は数量に使わない)を緩めた。目的の受け皿はその後PR #35で main に入った(`e4eaa65`)ため、乗り換えれば緩めは要らなくなる可能性がある。乗り換えを今すぐ指示してよいか。
- 推奨: 乗り換えを指示する。「目的未受領」の明示の印という設計自体は無駄にならず、受け皿からの値が来たときにそのまま`is_complete`をTrueにする経路として使える。
- 理由: 原則2を緩めた状態を残す期間を最小にできる。目的の受け皿(PR #35)が完了・マージ済みのため乗り換えのコストは低いと見込まれる(実装側の確認は必要)。

### 31. OCRは2つのモデルが同じに読んだ語だけを通す方式を既定にしてよいか(キュー7・OCR)

- 何を決めてほしいか: 中国語・英語モデルと日本語モデルを突き合わせ、両方が同じに読んだ語だけを採用する方式(誤り0件だが29語中7〜8語しか通らない)を既定にしてよいか。
- 推奨: 既定にする。
- 理由: 誤り0件という実測結果は「文字化けを自動確定に持ち込まない」という原則と直接合う。通る語が少ない(7〜8/29語)ことは実利用での取り込み量の問題だが、誤って確定するより安全側。実図面での割合はCodex側の計測待ち。

---

### 32. ページ種類宣言の修正(PR #37)をマージしてよいか(キュー0・ページの種類の宣言の修正)

- **【2026-09-22 07:43 案Aで確定、残るはマージ可否】** 「建具表と宣言されたら開き戸は0件」を固定していた`tests/test_intake_start_kit.py:440`は、指示どおり案A(削除して新しい11件に置き換え)で処理済み(コミット`81c009e`、全件880件パス・失敗0)。書き換えではなく削除にした理由(名前と中身を書き換えると何を取り消したのか履歴から読めなくなるため)、削除が心配していた「建具表宣言のページで円弧が罫線・記号を拾いうる」という懸念は実際に起きるため0件にせず自動確定させず人に回す形へ移したことも確認済み。
- 何を決めてほしいか: 判定のしかた(宣言だけで開き戸を探すのをやめる、から、食い違いを両方出す)が変わる変更のため、PR #37をマージしてよいか。
- 推奨: マージしてよい。
- 理由: 変更後の振る舞い(宣言が間違っていても黙って0件にせず、食い違いを人の確認事項として残す)は原則4(図面の読み方を縛らない)に沿う方向の修正であり、旧テストが固定していた振る舞い自体が欠陥だった。新テスト11件・壊し試験8通りで固定済み。

---

### 33. 寸法読み取り(PR #39)をマージしてよいか(キュー9・寸法読み取り)

- 何を決めてほしいか: PR #39は判定のしかた(階層条件・許容差・群合計・質問の選び方)そのものは変えていないが、縮尺の決め方に3つめの独立した読み(寸法どうしの一致)が加わるため、ページごとの結論が変わる(いままで縮尺が出なかったページで出るようになる、いままで無かった食い違いの警告が新しく出るページが生じる)。マージしてよいか。
- **【2026-09-22 07:43 CI緑・衝突なしでマージ可能な状態】** おーちゃんの判断待ちのまま。
- 推奨: マージしてよい。
- 理由: 新しく出る2つの動き(縮尺が出る/食い違いを警告する)はどちらも安全側(自動確定はどの手法も未校正のままで起きず、食い違いは黙って握らず人に示す)。既存の判定のしかた自体は変えていないため、README.mdの「マージしてよいもの」の基準(既存の判定のしかたを変えない機能追加)に沿う。

---

### 34. ページの種類の宣言に「確かさ」を付けられるようにするか(キュー0・ページの種類の宣言の修正、所見のみ・未実装)

- 何を決めてほしいか: ページの種類の宣言(平面図・建具表など)に「確か/たぶん」のような確かさの欄を追加できるかという所見が出た。技術的には入る(欄を1つ足すだけ、前例〈現況の把握〉あり、使い先〈食い違い判定〉も1か所に寄っている)が、「何に効かせるか」を先に決める必要がある。
- 選択肢: A. 追加しない(いまは見送る) / B. 追加するが「食い違ったときどちらを疑うか」だけに効かせる(食い違いを人に聞くかどうかには効かせない、別PRで判定を変える提案として提出) / C. 追加して「確か」な宣言では食い違いを人に聞かない
- 推奨: A(いまは見送る)、必要になればB。
- 理由: **Cは危険。** 確かさを付けられるようにすると「確か」と言われた宣言を単一の根拠で自動確定させる入口になりえ、原則2(単一の根拠で自動確定しない)に直接反する。Bは筋が通るが判定のしかたを変えるため別PRでの提案が必要で、いま急ぐ理由もない。「確かさが付いても、人の入力を単一の根拠で自動確定させないことは変わらない」と先に書き留めてから着手するのが安全。

---

### 35. 目的が未入力のとき「目的は分からない」という前提を作るか(キュー0・原則の3層構造)

- 何を決めてほしいか: 第2層(未入力は「分からない」と同じに扱う)を目的にも当てるなら、目的が未入力のとき現況と同じ形で前提を作るか。
- 推奨: 作る。
- 理由: 現況は既にそう作られている(`condition::unstated`)。同じ考えを目的にも当てるのが第2層の趣旨に沿う。

---

### 36. 平面図と建具表の突き合わせをいつ入れるか(キュー0・原則の3層構造)

- 何を決めてほしいか: 建具表の数量と平面図の開き戸検出数の食い違いを、いつ・どの形で扱うか。
- 推奨: まず数量にせず「確かめるべき問い」として出すところから。
- 理由: 第2層(軸から出るのは確かめるべき問いであって事実ではない)にそのまま沿う。数量に先に変換すると問いの性質が消える。

---

### 37. 確かさの欄をどの入力から足すか(キュー0・原則の3層構造)

- 何を決めてほしいか: 「確か/たぶん/分からない」の欄を5つの人の入力のうちどれから足すか。
- 推奨: 基準点から。
- 理由: 基準点はいま既定で「強い根拠」として扱われており、確かさが無いまま強い根拠として使われている度合いが最も大きい。**ただし判定のしかたが変わるためPRのまま待機になる。**

---

### 38. 精密/概算モードの呼び名が既存の質問モードと衝突している(キュー0・原則の設計、#44)

- 何を決めてほしいか: PR #44(精密/概算モードの設計、実装なし)が使う呼び名が、既存の質問モード(段階0.5・方式A/A2など)の呼び名と衝突している。呼び名をどう整理するか。
- 推奨: 実装に入る前に呼び名だけ先に揃える(PR #44側で改名、既存側は変えない)。
- 理由: 既存の質問モードは通しテストで固定されており、動かす理由が無い。新しい概念に既存語を流用すると、報告や記録で取り違えが起きやすい。

---

### 39. 要約資料の中身を読む実装を先に入れるか(キュー0・原則の設計)

- 何を決めてほしいか: 目的の情報を受け取れる読み取りの部品が0件という一覧が出た。土台から無いもの(部屋の認識、凡例・線の種類の読み取り)より先に、要約資料(仕様書・仕上表・工事概要書)の中身を読む実装を優先するか。
- 推奨: 先に入れる。
- 理由: 要約資料は文字主体で位置つきの読み取り経路(建具表・仕上表で確立済み)を流用しやすく、部屋の認識・凡例の読み取りより実装コストが低いと見込まれる。要約資料が読めれば「要確認」と「キラークエスチョン」の区別も実データで検証できる。

---

最終更新: 2026-09-22T10:13:00Z
