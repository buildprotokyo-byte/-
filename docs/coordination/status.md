# 作業状況(おーちゃん不在時、8時間の自走)

ルール・マージ可否・作業キューの原文は [README.md](./README.md) を参照。
このファイルは記録係スレッドのみが更新します。

**再開時の読み方:** 会話の履歴を読み返さず、README.md とこのファイルだけを読んで、
「現在の状況(要約)」に書かれているキューの続きから再開してください。

## 現在の状況(要約)

- **同時に動かしているのは2本まで**(おーちゃんの追加ルール、2026-09-22 03:06)。
- **動いている(2本):**
  - キュー1「実図面を読む本番の入口」— Opus 5 スレッド、作業中
  - キュー4「群合計制約の修正」— PR 作成済み・凍結数値の切り分けで終了、マージしない(待機)
- **順番待ち(一時停止、順番が来たらコーディネータが再開):**
  - キュー3「抜き取り監査を階層1へ拡大」
  - キュー5「質問選定の高速化(設計案まで)」— 参考情報は受領済み(下記参照)
- **未着手:** キュー2「建具表・内装仕上表の構造化」(キュー1が main に入るまで開始待ち)
- **次の一手:** キュー1またはキュー4のいずれかが終了(またはブロック)したら、キュー3かキュー5のどちらかを再開する。

## 作業ごとの状況

### 記録係(このスレッド)

- スレッド名: 記録係(コーディネーション記録)
- 使用したコミット: PR #5(ブランチ `claude/coordination-log`)
- 完了したこと: docs/coordination/README.md と status.md の作成、追加ルール(2本制限・再開手順)の反映
- 残っていること: コーディネータから届く各作業の進捗を、その都度このファイルに反映する。このスレッドは自分から動かず、更新が届いたときだけ動く。

### キュー1: 本番の入口(最小構成)

- スレッド名: Opus 5 スレッド(実図面を読む本番の入口)
- 状態: **動いている**
- 使用したコミット: まだ無し
- 完了したこと: (作業中)
- 残っていること: 入口関数、ページ種別判定(ラスターは「未対応」として記録)、縮尺・面積・開き戸の接続、根拠付きの証拠として InferenceOrchestrator へ渡す、面積選択を案件ごとに1回だけ質問して使い回す、合成ベクターPDFでの通しテスト
- 次の一手: 実装継続。完了条件(通しテスト・全件テスト・CI)を満たしたらマージしてよい。

### キュー2: 建具表・内装仕上表の構造化

- スレッド名: 未着手
- 状態: 未着手
- 使用したコミット: 無し
- 完了したこと: 無し
- 残っていること: キュー1がmainに入るまで開始待ち(キュー1の入口に証拠として接続する構成のため)
- 次の一手: キュー1がmainにマージされたら着手する。

### キュー3: 抜き取り監査を階層1へ拡大

- スレッド名: 未起動
- 状態: **順番待ち(一時停止)**。2本制限のため、キュー1またはキュー4が空くまで開始を保留。
- 使用したコミット: 無し
- 完了したこと: 無し
- 残っていること: 階層1・階層2の抽出率を別々に設定できるようにする、監査記録に階層を残す、階層1の既定値はPR本文で提案のみ
- 次の一手: 順番が来たらコーディネータが Opus 5 スレッドを起動する。

### キュー4: 群合計制約の修正

- スレッド名: 別スレッド(group-total-masking)
- 状態: **動いている扱い(実装は終了済み、PRは待機中)**
- 使用したコミット: `c3d053192ed9d560e40ee0272b326b50a7f3dfb5`(ブランチ `claude/group-total-masking`)
- 完了したこと: 実装済み。586件パス、壊し試験5通り。検出用と確定用で範囲を分離、停止した要素も必ず変数登録、精密モードは確定用だけに変更。
- 残っていること: **おーちゃんの指示によりPRのまま待機、マージしない。** 効果測定はおーちゃんの帰還後にCodex側で実施。中心値検査の凍結数値(`docs/center_agreement_effect_result.json`)が変わった件の切り分けが未完了(下記「判断待ち」参照)。
- 次の一手: 切り分け(コードと差分の確認)が終わったら status.md を更新し、このスレッドは終了扱いにしてよい。

### キュー5: 質問選定の高速化(設計案とテストまで)

- スレッド名: 未起動
- 状態: **順番待ち(一時停止)**。2本制限のため開始を保留。
- 使用したコミット: 無し
- 完了したこと: 無し
- 残っていること: 設計案とテストの作成。選ばれる質問の順番が現行エンジンと完全一致すること、決定性を壊さないこと。暫定目標: 1,000要素で1問あたりP95 5秒以内・全体10分以内。キュー4と同じファイルを触る場合は重なる箇所を明記する。
- **参考情報は受領済み**(2026-09-22 03:08、おーちゃんより): Codex がリポジトリ外で試作・測定(コミット `71420fd`、コードは未変更)。
  - 候補の評価を dependency_graph の components に限定し、まとまりごとの solver を質問ごとに1回作って使い回す試作で、7種類の構造(独立・橋渡し・合計・鎖・星・幅の広い定義域・混合)すべてで、今のエンジンと質問の列・スコア・最終状態が完全一致
  - 時間: 30要素 14秒→4.2秒、50要素 80秒→11秒、100要素 打ち切り→42秒で全解決。300・1,000要素はまだ180秒で打ち切り(1,000要素は最初の1問に127秒)
  - 残り時間の62〜74%は範囲の計算(`_tighten`/`_optimize`)、Z3コンテキストの作成は7〜17%
  - まとまり間で候補評価の結果を質問間で使い回す案は未検証
  - 再開時の指示: 設計案では①まとまり限定 ②まとまり間の評価結果の使い回し ③範囲計算の軽量化、の3段階の順で効果とリスクを書く。実装はしない(キューのとおり)。
- 次の一手: 順番が来たらコーディネータが Opus 5 スレッドを起動し、上記参考情報を渡す。

## 判断待ち

### 1. 群合計制約の修正で、中心値検査の凍結数値が変わった

- 何を決めてほしいか: 凍結数値(`docs/center_agreement_effect_result.json`、中心値検査30試行)が群合計制約修正の前後で変わった原因が、①修正が挙動を変えたことによるものか、②計測スクリプトの数え方がずれていることによるものか。
- 選択肢:
  - A: 修正のコードと数値の差分だけを見て切り分ける(重い再計測はしない)
  - B: Codex側の固定ベンチマークで測り直す
- 推奨: A(コードと差分での切り分け)を先に行い、それでも切り分けられない場合にBを「Codex側で測るべき項目」として書き出す
- 理由: おーちゃんの新ルールにより、重い計測(数十分以上かかる試行の繰り返し)はClaude Codeでは行わないため

---

最終更新: 2026-09-22T03:13:00Z
