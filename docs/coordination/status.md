# 作業状況(おーちゃん不在時、8時間の自走)

ルール・マージ可否・作業キューの原文は [README.md](./README.md) を参照。
このファイルは記録係スレッドのみが更新します。

**再開時の読み方:** 会話の履歴を読み返さず、README.md とこのファイルだけを読んで、
「現在の状況(要約)」に書かれているキューの続きから再開してください。

## 現在の状況(要約)

- **同時に動かしているのは2本まで**(おーちゃんの追加ルール、2026-09-22 03:06)。
- **動いている(2本):**
  - キュー1「実図面を読む本番の入口」— 実装完了、PR #7 の CI 待ち。通ればそのスレッドがそのまま main にマージする。
  - キュー3「抜き取り監査を階層1へ拡大」— 再開済み、作業中(ブランチ `claude/tier1-audit-sampling`、コミット `98c4759`)。
- **完了:**
  - キュー4「群合計制約の修正」— 実装・PR #6 の CI とも完了。**おーちゃんの判断待ちで未マージのまま保持。**
- **順番待ち(一時停止):**
  - キュー5「質問選定の高速化(設計案まで)」— キュー3が終わり次第再開。
- **未着手:** キュー2「建具表・内装仕上表の構造化」— キュー1が main に入り次第、Opus 5 で起動する。
- **次の一手:** PR #7 の CI が通ったらマージ確認 → キュー2 を起動。キュー3 が終わったらキュー5 を再開。

## 作業ごとの状況

### 記録係(このスレッド)

- スレッド名: 記録係(コーディネーション記録)
- 使用したコミット: PR #5(マージ済み、`a8682c0`)。以降の更新はこのブランチ(`claude/coordination-log-2`)で行う。
- 完了したこと: docs/coordination/README.md と status.md の作成・追加ルールの反映、キュー1・3・4・5 の報告の反映
- 残っていること: コーディネータから届く各作業の進捗を、その都度このファイルに反映する。このスレッドは自分から動かず、更新が届いたときだけ動く。

### キュー1: 本番の入口(最小構成)

- スレッド名: Opus 5 スレッド(実図面を読む本番の入口)
- 状態: **完了、PR #7 の CI 待ち**
- 使用したコミット: `d8414e8`(ブランチ `claude/production-drawing-intake`、main `7d21245` から分岐)
- 完了したこと: 依頼の7項目すべて。全件テスト 575→601件、全件パス。壊し試験5通りで検出を確認。群合計スレッド(キュー4)とのファイル単位の重なりは無し(`arbitration/` で触ったのは `inference_orchestrator.py` と `method_policies.py` のみ)。
- **最重要の所見(構造上の事実、通しテストで固定済み): この入口を通しても確定する数量は0件。**
  - 不具合ではない。階層1は独立した強いデータ源2つ以上を求め、独立性はファイル内容の指紋で判定されるため、1つの図面PDFから何手法で読んでも独立数は1にしかならない。つないだ2手法(縮尺読み・開き戸検出)はどちらも未校正でハード制約に入らない。
  - **図面1枚だけでは、精度を上げても階層1には到達しない。** 確定させるには図面以外の独立データ源(文章軸・過去実績軸・人の入力)が要る。→ 判断待ち3。
  - どちらかの手法を「校正済み」に変えるとこの通しテストが落ちるよう固定されている。
- 拾えなかったもの(そのまま記録): 引戸・折戸は円弧を描かないため検出0件(合成図面に引戸2つを描き、期待値0で固定)。室の輪郭からの面積計算は未実装(面積25項目中22項目がここで止まる)。スキャンページの中身は未読で「未対応」として記録に残る。
- Codex側で測るべき項目として報告書に記載済み: `pdf_text_area` の校正、開き戸手法の適合率・再現率(独立データで)、34ページの実行時間とメモリ。
- 次の一手: PR #7 の CI が通ったら、そのスレッドが main にマージする(完了条件を満たしているため、自走ルールの「マージしてよいもの」に該当)。マージされたら記録係がこのファイルを更新し、キュー2 を起動する。

### キュー2: 建具表・内装仕上表の構造化

- スレッド名: 未着手
- 状態: 未着手
- 使用したコミット: 無し
- 完了したこと: 無し
- 残っていること: キュー1がmainに入るまで開始待ち(キュー1の入口に証拠として接続する構成のため)
- 次の一手: キュー1がmainにマージされたら、Opus 5 で着手する。

### キュー3: 抜き取り監査を階層1へ拡大

- スレッド名: Opus 5 スレッド(再開済み)
- 状態: **動いている**
- 使用したコミット: `98c4759`(ブランチ `claude/tier1-audit-sampling`、作業中)
- 完了したこと: (作業中)
- 所見: **抜き取り監査は現時点で本番の処理から一度も呼ばれていない。** テストと計測スクリプトからしか呼ばれておらず、階層1に広げても実際に監査される件数は両階層とも0のまま。本番経路への組み込みは別作業(→ 判断待ち2)。
- 「階層を混ぜると的中率が薄まる」という前提は、的中率を1つの数字にまとめる場合だけ成り立つ。階層ごとに分けて集計すれば問題は起きない。
- 残っていること: 階層1・階層2の抽出率を別々に設定できるようにする、監査記録に階層を残す、階層1の既定値はPR本文で提案のみ
- 次の一手: 実装継続。完了条件(既存・新規テスト、CI)を満たしたら、既定値に触れない範囲でマージしてよい。終了したらキュー5を再開する。

### キュー4: 群合計制約の修正

- スレッド名: 別スレッド(group-total-masking)
- 状態: **完了(実装・CIとも)。おーちゃんの判断待ちで未マージのまま保持。**
- 使用したコミット: `c3d053192ed9d560e40ee0272b326b50a7f3dfb5`(ブランチ `claude/group-total-masking`、PR #6)
- 完了したこと:
  - 実装済み。PR #6 の CI(`test`チェック)成功、**587件全件パス(143.74秒)**。手元と別環境で同じ件数が出たことを独立に確認。
  - PR は `mergeable: clean`(main と衝突なし)。件数書き出しステップも動作し、要約に passed/failed/error/skipped/合計の表が出る。
  - PR説明文にあった「このリポジトリにはPRに対して走るCIがない」の一文は、別スレッドでCIがmainに入ったため事実でなくなり削除済み。
  - **中心値検査の凍結数値が変わった件の切り分け完了。原因は「両方」だった:**
    - ①計測スクリプトの数え方のずれ: 「人が確かめる要素」を「変数登録されていない要素」で数えていたため、停止要素を変数登録すると恒久的に0になる不具合。19要素・シード1で 0.8947→0.3158、数え方の修正だけでは 0.6842 までしか戻らない。
    - ②実装側の本物の欠陥: 停止した要素が読み取り由来の狭い定義域を持ったままで、未校正の読み取りがクラスタ等価を通って他の要素を誤った値に確定させていた(台所群の例: 停止要素の誤読(3,3)が kitchen_2(正解4)を(3,3)に潰していた)。`detection_range` は矛盾検出の向きしか直していなかった。
  - 対処: 検出の向きで分けた。誤りが人へ回る向きでは読み取りの狭い範囲を使い、黙って確定させる向きでは何も主張しない。
  - 3シード×3条件で9点すべてが `71420fd` 以上。**ただしこれは9点の標本であり効果の測定ではない。** 効果の測定(`71420fd` 対 修正後、同一環境・同一評価データ)は設計メモ9章に「Codex側で測るべき項目」として記載済み。
- 残っていること: おーちゃんの帰還後、Codex側で効果測定を実施。測定が終わるまでマージしない。
- 次の一手: 記録係はこのキューについて追加の作業はしない。おーちゃんの判断(マージするか)を待つのみ。

### キュー5: 質問選定の高速化(設計案とテストまで)

- スレッド名: 未起動(設計案のブランチ `claude/killer-question-speedup-design` は既存)
- 状態: **順番待ち(一時停止)**。2本制限のため、キュー3が終わるまで再開を保留。
- 使用したコミット: 無し(設計メモのみ、ブランチ `claude/killer-question-speedup-design`)
- 完了したこと: 連鎖構造の実測: 4要素 0.19秒、8要素 0.92秒、16要素 5.7秒、24要素 16.9秒、32要素 37.7秒(倍増ごとに約6〜7倍)。32要素でsolverの再構築が約52万回。
- **重要な警告: 「まとまりごとのsolverを質問ごとに1回作って使い回す」案は、決定性を守るために入れた「解くたびに新しいZ3コンテキストを作る」修正(`03b8eac`)と正面からぶつかる。設計案ではこの衝突をどう扱うかを必ず書くこと。**
- 参考情報(2026-09-22 03:08、おーちゃんより。Codexがリポジトリ外で試作・測定、コミット`71420fd`、コードは未変更):
  - まとまり限定の試作で、7種類の構造(独立・橋渡し・合計・鎖・星・幅の広い定義域・混合)すべてで今のエンジンと質問の列・スコア・最終状態が完全一致
  - 時間: 30要素 14秒→4.2秒、50要素 80秒→11秒、100要素 打ち切り→42秒で全解決。300・1,000要素はまだ180秒で打ち切り(1,000要素は最初の1問に127秒)
  - 残り時間の62〜74%は範囲の計算(`_tighten`/`_optimize`)、Z3コンテキストの作成は7〜17%
  - まとまり間で候補評価の結果を質問間で使い回す案は未検証
  - 再開時の指示: 設計案では①まとまり限定 ②まとまり間の評価結果の使い回し ③範囲計算の軽量化、の3段階の順で効果とリスクを書く。実装はしない。
- 残っていること: 上記の Z3コンテキスト衝突の扱いを含めて設計案とテストを仕上げる。
- 次の一手: キュー3が終わったらコーディネータが再開させる。

## 判断待ち(おーちゃんの帰還後)

### 1. 群合計制約の修正(PR #6)をマージするか

- 何を決めてほしいか: 実装・CIとも完了、9点の標本では悪化なしだが、これは効果の測定ではない。Codex側での効果測定(`71420fd`対修正後)を待たずにマージするか、測定完了まで待つか。
- 推奨: 測定完了まで待つ(現状の扱いのまま)。
- 理由: 既存の判定のしかたを変える変更であり、自走ルールで「マージせずPRのまま確認を待つもの」に該当する。

### 2. 抜き取り監査を本番経路に組み込む作業を、キュー3に含めるか別作業に分けるか

- 何を決めてほしいか: 抜き取り監査(階層2、今回拡大する階層1)は現状テスト・計測スクリプトからしか呼ばれておらず、本番処理からは呼ばれていない。階層を広げても監査件数は実質0のまま。
- 選択肢: A. キュー3の範囲を広げて本番経路への組み込みも行う / B. 別作業として切り出す
- 推奨: B(別作業として切り出す)。キュー3は「階層1・階層2を別々に設定できるようにする」という設計変更が主眼で、本番経路への接続は別の検証(どこから呼ぶか、既存の確定フローとの順序)が要るため。
- 理由: 範囲が広がるとキュー3の完了条件(既存・新規テスト、CI)が曖昧になる。

### 3. 図面1枚だけでは階層1に到達しないという構造上の事実を受けて、次にどの独立データ源へ進むか

- 何を決めてほしいか: キュー1の通しテストで「1つの図面PDFからは独立数1にしかならず、階層1に確定する数量は0件」であることが構造的に固定された。次に着手する独立データ源(文章軸、過去実績軸、人の入力)をどれにするか。
- 推奨: (記録係としては提案しない。設計判断のため、おーちゃんの帰還後に決めていただく項目)
- 理由: v8設計の軸配分そのものに関わる判断であり、自走ルールの範囲を超える。

### 4. 面積の選択(専有延床か施工床か)を誰がいつ答えるのか

- 何を決めてほしいか: 現状は面積の選択を人がJSONに置く前提だが、回答を集める画面も訂正の経路も無い。
- 推奨: (記録係としては提案しない。UI/運用設計に関わるため、おーちゃんの帰還後に決めていただく項目)
- 理由: キュー1の入口実装の前提(「案件ごとに1回だけ質問して使い回す」)を運用でどう成立させるかという判断。

---

最終更新: 2026-09-22T03:15:40Z
