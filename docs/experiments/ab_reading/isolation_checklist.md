# 読み方の比較実験 — 漏れ口の決まりと、毎回通す手順

> **この文書は「気をつける」ための文書ではない。**
> 2026-09-22 の 1 回目で、気をつけていたのに 2 つの経路を見落とした。
> **決まりを書いただけでは同じことが起きる。**毎回、機械で確かめる。

道具は `benchmarks/ab_reading_isolation_check.py`、テストは
`tests/test_ab_reading_isolation_check.py`（否定対照つき 39 件）。

## 1. 何が起きたか（決まりの理由）

1 回目の 5 回で、読む側のスレッドに渡ってはいけないものが 2 つの経路で渡っていた。

| 経路 | 何が渡っていたか | 気づき方 |
|---|---|---|
| 渡すフォルダの中身 | パッケージを作ったスクリプトに、**正解と見積があるフォルダのパス**が書いてあった | 渡したファイルを 1 つずつ読み直して気づいた |
| **記憶の本文** | **実験の狙い、正解ファイルの名前と置き場所、正解と一致することが確認済みの面積 2 値** | セッションの記録に `memory_recall` があり、選ばれた記憶を現物で読んで気づいた |

**どちらも「読む側は渡したフォルダしか開いていない」という突き合わせを通っていた。**
ファイルを開いたかどうかを見るだけでは足りない。**渡る経路は 4 つある。**

## 2. 漏れ口は 4 つ（これで全部とは限らない）

| # | 経路 | 止め方 | 機械で確かめられるか |
|---|---|---|---|
| 1 | **渡すフォルダの中身** | 読む材料以外を置かない。外を指すパスを 1 つも書かない | **できる**（`scan_package`） |
| 2 | **前の回の答案** | 1 回終わるごとに引き上げる。**次を立てる前に必ず空にする** | **できる**（`scan_runs_dir`） |
| 2b | **前の回のスレッドの本文** | ファイルを引き上げても、前の回のスレッドには報告の文（出した件数など）が残る。スレッドは他のスレッドを読める | **半分**（7 の突き合わせで、`fetch_thread` / `list_thread_sessions` を呼んだかを見る） |
| 3 | **共有フォルダのファイル名の一覧** | ハーネスが自動で文脈に入れる。**中身を隠しても名前は見える。**名前に手がかりを持たせない | **できる**（`scan_shared_root`） |
| 4 | **記憶の本文** | ハーネスが自動で選んで入れる。**索引は全文が入る。**仮説・正解・採点の決まり・**どちらのやり方が良いかの判定**を記憶に書かない | **半分**（記憶の側は `scan_memory`。届いたかどうかは捨てスレッドに聞く） |

**4 が一番危ない。** ファイルを動かしても、指示で禁止しても止まらない。
**記憶に書いてあるものは読む側に届くと考える。**

### 4 について: 索引は毎回すべてのセッションに渡る

**個別の記憶ファイルだけを見ても足りない。** `MEMORY.md`（索引）は
**全文が毎回どのセッションにも渡る。**2026-09-22、正解の数値も照合結果も消したあとに
`memory_recall` の記録を 1 回ごとに突き合わせて、やっと索引の 1 行に気づいた。
そこには**どちらの読み方を既定にしたかが書いてあった。**
片側どころか、**それまでの全部の回が同じ予断を持って走っていた。**

検査は索引も見る（`_iter_text_files` が `*.md` を全部拾う）。
**これはテストで固定してある**（`test_index_file_is_scanned`）。
「見ているはず」で済ませない。

### 4 について: 「どちらの読み方が良いか」も外す

**外すのは判定だけで、手順や原則は外さない。**

| | 扱い |
|---|---|
| **どちらのやり方が良いかを述べたもの**（判定・順位・点数・「既定はこちら」） | **外す。**片側だけに来るか毎回来るかによらず、**答えの向きそのものを決めてしまう** |
| **やり方の中身を述べたもの**（手順・原則・設計の考え方） | **残す。**両方の条件に等しく来ていることを 8 で記録し、結果に添える |

**残すほうの理由**: 手順の知識は、片方の条件をもう片方に近づける向きに効く。
つまり**差を小さくする側**なので、それでも差が出たなら本物である。
**判定は逆で、差の向きそのものを作ってしまう。**

検査の側では `scan_reading_method_verdicts()` が、**「読み方を指す語」と
「優劣を述べる語」が同じ行に同居していること**を漏れ口とみなす。

**途中で見つけたら、残りを直すのではなく、そこまでを全部捨ててやり直す。**
途中で条件を変えると、前半と後半が比べられなくなる。

### 4 について: 数値を伏せるだけでは足りない

2026-09-22、記憶から正解の数値を抜いたあとに受け取る側へ聞いたところ、
**数値は知らないと答えた一方で、「この案件の床面積 2 値は正解と完全一致した」という
照合の結果そのものは知っていた。**

数値が無くても、これは「この数量は当たりだ」という手がかりで、
読む側がその数量に重みを置く理由になる。だから次の 3 つは、数値と同じ種類のものとして
記憶から外し、リポジトリの報告書だけに置く。

1. **正解と突き合わせた結果**（一致した／外れた、何件中何件）
2. **正解の項目名・項目数**
3. **正解として使っている実案件の数量の実数**（室数・建具の本数など）

**残すのは手法についての知見**（何が効かなかったか、なぜか）だけにする。

検査の側では `scan_match_claims()` が、**「正解を指す語」と「一致したと述べる語」が
同じ行に同居していること**を漏れ口とみなす。語を 1 つ並べるだけでは効かない
（「一致」は Z3 の再現性や OCR の 2 エンジン一致など、無害な文脈で頻出する）。

**この検査で見つけられないもの**（広げると無害な行を毎回拾うので、広げない）:
正解を指す語が無い言い方 ―「全部的中した」「3/3 と 0/3 に分かれた」。
**ここは決まりの側で防ぐ。**この限界は `tests/` に
`test_known_blind_spot_match_claim_without_golden_word` として書いてある。

### 4 について、記録からは分からないこと

`memory_recall` のイベントに残るのは `path` と `scope` と `mode` だけで、**本文は残らない。**
だから「記録に出てこない＝届いていない」とは言えない。
**確かめる唯一の方法は、受け取る側に写させること。**

2026-09-22 に捨てスレッドで確かめた: ファイルを 1 つも開かせずに
「いま自分の中に入っているものを原文のまま写して」とだけ頼んだところ、
**索引の全文と、選ばれた記憶の本文を写してきた。ツール呼び出しは 0 件。**

## 3. 毎回通す手順

### 実行の前（1 回ごと）

1. **前の回の答案を引き上げる。**`ab_reading_runs/` に `*.json` を残さない。
2. **検査を通す。**

       python3 -m benchmarks.ab_reading_isolation_check \
           --package /mnt/project-files/ab_reading_input_P011 \
           --runs-dir /mnt/project-files/ab_reading_runs \
           --shared-root /mnt/project-files \
           --memory-dir /tmp/claude/memory \
           --allow ab_reading_input_P011 --allow ab_reading_runs

   **0 件でなければスレッドを立てない。**
3. **記憶が、前に写しを取ったときから変わっていないかを確かめる。**

       python3 -m benchmarks.ab_reading_isolation_check … \
           --expect-memory-fingerprint <写しを取ったときの指紋>

   **変わっていなければ、前の写しがそのまま根拠になる。**変わっていたら、
   捨てスレッドで写しを取り直す。指紋の計算に手間はかからないので、**毎回やる。**

   写しの取り方: 作業をさせず、ファイルも開かせず、「文脈に入っている記憶の
   名前と本文を原文のまま写す」ことだけを頼む。その写しを `--probe-transcript` に通す。
   取った直後に `--print-memory-fingerprint` で指紋を控える。**0 件でなければ立てない。**

   **なぜ毎回は立てないか**: 捨てスレッドは 1 本ぶんの手間がかかる。
   一方で**記憶の中身の検査（`scan_memory`）は毎回ただで通せる。**
   捨てスレッドでしか分からないのは「ファイルに書いてあるものが、本当にそのまま
   届くか」であって、これは記憶が変わらない限り変わらない。
   **だから、変わったときだけ取り直す。**
   このプロジェクトでは他のスレッドがいつでも記憶を書き換えるので、
   「変わっていないはず」を当てにせず、毎回指紋で確かめる。
4. ブリーフは固定したものをそのまま使う。**直すのは実行 ID の 1 か所だけ。**
   **最初の一言も固定する。実行 ID だけを変える。**(2026-09-23 おーちゃんが追加、K-07 4 番。
   「条件が読む側に見えないようにします。」)読む側のスレッドを立てるときの最初の
   メッセージも、ブリーフの一部として扱う。回の数え方(「3 回目」「これで 6 回そろいます」)や
   読み方の名前(「全部読む読み方」「要約資料から絞る読み方」)を書かない。
   読み方の比較では、ブリーフ本文は固定されていたのに、スレッドの最初のメッセージに
   回ごとに違う一言が付き、その中に読み方の名前が入っていた(`access_audit.md` 4 節)。

### 実行の後（1 回ごと）

5. **答案を引き上げる**（次のスレッドから見えないように。次を立てる前に）。
6. **答案の中身を検査する。**

       python3 -m benchmarks.ab_reading_run_validator <答案>.json --pages 34

   記録欄の欠け、金額が混ざっていないこと、読んだページ ＋ 開かなかったページ ＝
   全ページ、渡したフォルダの外を指していないこと。

   **使い捨てのスクリプトを毎回書かない。** 1 回目はそうしていて、
   **仕様では `null` でよい欄（`数量`・`単位`・`途中で変えた`）を欠けと数えて
   337 件の偽の欠けを出した。**検査の書き方が毎回ぶれるなら、
   6 回の答案を同じ物差しで見たことにならない。
7. **セッションの記録と突き合わせる。**`list_events` を先頭から末尾まで辿り、
   `tool_use` の入力（`Read` の `file_path`、`Bash` の `command`、`Glob`/`Grep` の `path`）を
   全部拾って、渡したものの一覧と照合する。
   **ファイルの最終アクセス時刻は使えない**（共有フォルダは読んでも変わらないことを実測済み）。
   **`fetch_thread` / `list_thread_sessions` / `add_repo` / GitHub 系を呼んでいないか**も
   あわせて見る（前の回のスレッドを読みに行っていないかの確認）。
8. **`memory_recall` で何が選ばれたかを、毎回記録する。**
   A と B で選ばれ方が偏っていないかを、最後にまとめて見る。
   **条件ごとの記憶の名前の表を採点の報告に必ず載せ、食い違いが 1 件でもあれば
   結果と一緒に報告する。**(2026-09-23 追記。この段は行われ、偏りは結果の文書の但し書きに書かれていたが、
   採点の報告には載っていなかった。`memory_asymmetry.md` 3 節)

## 4. 決まりにしないこと（できないことを正直に）

- **本気で探しに行くスレッドから隠し切ることはできない。**
  スレッドは他のスレッドの記録を読める。この手順は「うっかり渡る」を止めるためのもので、
  故意については 7 の突き合わせで**後から気づく**形にしかならない。
- **記憶の投入そのものは止められない。**このプロジェクトの記憶は
  「図面をどう読むか」の知見で埋まっている。読む仕事のスレッドには読み方の知見が選ばれる。
  **測ろうとしているものと、渡ってしまうものが同じ分野である。**
  実験のために本物の知識を捨てるのは本末転倒なので、**消さずに、
  両方の条件に同じように渡っていることを 8 で記録して、結果に添える。**
- この 4 経路で全部とは限らない。**5 つ目が見つかったら、この表と検査に足す。**

## 5. 1 回目の 5 回の扱い

捨てない。**「汚染あり」として残す。**本来の目的（読み方の差）の答えには使わない。
やり直しの結果と比べれば、**狙いと正解を知っていたことが出力をどれだけ動かすか**が
見えるので、それ自体が材料になる。
